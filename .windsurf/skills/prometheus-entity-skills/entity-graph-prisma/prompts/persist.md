# Persist — Documentation and generator state

- [ ] Document API contract (`where`/`orderBy` JSON) in README
- [ ] Note env vars (`DATABASE_URL`)
- [ ] If generator added: commit `schema.prisma` generator block + generated files policy
- [ ] ADR: why REST + Prisma vs GraphQL (if both exist)

Store KBD/OpenSpec handoff if applicable.
