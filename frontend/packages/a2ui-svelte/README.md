# UAR A2UI Svelte renderer

`A2uiSvelteSurface` renders a `web_core` `SurfaceModel` with the certified nine-component `uar.a2ui/1` semantics. Pass the model as `surface`. Unknown catalog components fail closed.
