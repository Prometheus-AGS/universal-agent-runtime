import { render } from "@testing-library/svelte";
import { describe, expect, it } from "vitest";
import { Catalog, MessageProcessor, type ComponentApi } from "@prometheus-ags/a2ui-core/v0_9";
import { ButtonApi, CardApi, CheckBoxApi, ChoicePickerApi, ColumnApi, DividerApi, RowApi, TextApi, TextFieldApi } from "@prometheus-ags/a2ui-core/v0_9/basic_catalog";
import A2uiSurface from "../src/A2uiSurface.svelte";
const catalog = new Catalog<ComponentApi>("test", [TextApi, ButtonApi, TextFieldApi, CheckBoxApi, ChoicePickerApi, RowApi, ColumnApi, CardApi, DividerApi]);
function surface() { const processor = new MessageProcessor([catalog]); processor.processMessages([{ version: "v0.9", createSurface: { surfaceId: "s", catalogId: "test" } }, { version: "v0.9", updateComponents: { surfaceId: "s", components: [{ id: "root", component: "Column", children: ["text", "check"] }, { id: "text", component: "Text", text: { path: "/label" } }, { id: "check", component: "CheckBox", label: "Ready", value: { path: "/ready" } }] } }, { version: "v0.9", updateDataModel: { surfaceId: "s", path: "/", value: { label: "Hello", ready: true } } }]); return processor.model.getSurface("s")!; }
describe("A2uiSvelteSurface", () => { it("renders web_core surface semantics", async () => { const model = surface(); const result = render(A2uiSurface, { surface: model }); await new Promise<void>((resolve) => queueMicrotask(resolve)); expect(result.container.textContent).toContain("Hello"); expect(result.container.querySelector<HTMLInputElement>('input[type="checkbox"]')?.checked).toBe(true); }); it("reacts to bound updates", async () => { const model = surface(); const result = render(A2uiSurface, { surface: model }); model.dataModel.set("/label", "After"); await new Promise<void>((resolve) => queueMicrotask(resolve)); expect(result.container.textContent).toContain("After"); }); });
