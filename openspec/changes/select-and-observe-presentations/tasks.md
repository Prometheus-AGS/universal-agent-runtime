## 1. Implementation and phase acceptance

- [x] 1.1 Define optional client rendering negotiation, mode resolution and legacy-compatible request contracts.
- [x] 1.2 Integrate frozen eligible template snapshots and host-governed rendering; apply output ceilings to legacy A2UI too.
- [x] 1.3 Persist/project requested and effective selection, fallback reasons and rendered template revision; display through typed domain hooks.
- [x] 1.4 At phase end, prove text/A2UI/hybrid/legacy runs and observable lifecycle with browser and host regression evidence.

Task1.1 implementation evidence: typed optional presentation_mode/client_rendering and explicit Legacy/Auto/Text/A2UI/Hybrid resolution are carried through primary chat, OpenAI, native run/resume and ACP into RunExecutionRequest/context. A2UI action continuation preserves negotiation and rejects corrupt saved values. Independent contract and implementation source review found no blockers. `cargo check --locked --no-default-features --features server-full` exited0 in2m01s for the resolver and43.88s after mapping, without warnings; scoped `git diff --check` exited0. All serialization/runtime tests remain phase-end work. Output enforcement and immutable snapshots are still1.2; durable publication provenance/UI is still1.3. The stored admission mode is not a render receipt.

Task1.2 implementation evidence: full frozen templates, narrowed local/remote ceilings, strict pure preparation, single-use host-held untruncated receipts and native/graph/direct/event publication gates are wired. Remote typed wire presence preserves legacy zero-authority contracts without dropping negotiated restrictions. Ordinary JSON artifact projection and canonical title persistence are corrected through Impeccable4.2.0 dual critique and fresh adversarial review. Final backend source audit found no implementation blockers. Latest Cargo checks exited0 without warnings (2m09s,2m40s,21.21s); frontend typecheck/lint and scoped diff checks exited0. Runtime, old-peer digest, storage/reload and browser acceptance remain whole-phase task1.4, not claimed here. Durable provenance/UI remains1.3.

Task1.3 implementation evidence: host-owned version1 observations and publication receipts persist through existing run events, including cursor-safe independent replay and exact-tenant stream authorization. The new PresentationProvenance platform domain strictly decodes host records in wire order, owns local-history subscription leases and atomic graph ingestion, and exposes narrow typed hooks. Presentation run details show requested/admitted output, fallback, terminal outcome, surface publication and exact published template revisions separately from unconfirmed client display. Missing, unsupported and failed local-history reads do not become empty publication claims. Impeccable4.2.0/UIUX Pro Max approved direction and independent domain/UI source reviews found no blockers. Two final `pnpm typecheck && pnpm lint` runs exited0; scoped diff check exited0. The initial interface/graph type mismatch was corrected before those passes. All runtime, storage, replay, browser and accessibility acceptance remains phase-end task1.4.

Task1.4 acceptance (2026-09-05): full local server-full suite passed, including frozen edit/disable/delete snapshots, single-use host preparations, renderer replay-before-artifact publication, exact template revision receipts, output ceilings, terminal outcomes, retained history/cursor ordering, tenant SSE access and legacy wire/digest regressions. Five real HTTP/SSE runs against the isolated host/local stub passed strict legacy, text, unsupported-profile, A2UI and hybrid assertions; A2UI/hybrid produced actual renderer artifacts plus revision1 receipts and text summaries. Browser legacy chat completed and its typed Presentation details survived full page reload. Desktop1440/mobile390 confirmation measured no document/trace horizontal overflow; long JSON scrolls locally. Keyboard tab traversal/activation worked. Impeccable dual critique, fresh adversarial direction review and independent finish review approved bounded generated-surface wording and inspector grid/tab sizing fixes. The policy-summary exclusion is explicit, template receipts remain distinct and display is unconfirmed. Final typecheck/lint passed; inspector regression cohort21/21 passed8.52s; final build passed14.20s with four existing PGlite eval warnings. Earlier full frontend suite462/462 passed before this final copy/layout correction. No real-provider, live-peer, comprehensive zoom/contrast or release certification is claimed. Browser negotiation controls were not added; negotiated clients are verified through the actual HTTP contract. Deferred429 and coverage warnings remain. See the phase execution record for fixture isolation and required operator credential rotation.
