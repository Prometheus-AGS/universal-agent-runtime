# Verification — `explain-agent-model-status`

Source baseline: `e2def00966b1aab339850d03b52c9386a151e800`

Results are limited to the React production bundle installed into the local macOS LaunchAgent with the existing UAR 1.0 `server-full` release binary. They transfer to no other profile, platform, browser, provider registry, or deployment.

| Requirement | Command or evidence | Observed result | Limit | Source SHA | Profile |
|---|---|---|---|---|---|
| Provider metadata includes the effective system default model | `pnpm --dir frontend exec vitest run --project=unit src/features/providers/model/providers-store.test.ts -t "loads catalog\|sets default optimistically"` | Exit `0`; `2 passed`, `6 skipped`; initial hydration and a live default change both produced `ProviderMeta:current` with `default_id: stub` and `default_model: stub-model`. | Focused normalized-projection assertions only. | `e2def00966b1aab339850d03b52c9386a151e800` + working-tree change | React unit |
| Frontend compiles and satisfies static rules | `pnpm --dir frontend typecheck`; `pnpm --dir frontend lint`; `pnpm --dir frontend build` | TypeScript and ESLint exited `0`; final Vite build transformed `8104` modules and completed in `2.23s`. | Compile, lint, and bundle construction; not runtime behavior. PGlite emitted its existing direct-`eval` build warning. | `e2def00966b1aab339850d03b52c9386a151e800` + working-tree change | React production bundle |
| OpenSpec contract is valid | `openspec validate explain-agent-model-status --strict` | Exit `0`; `Change 'explain-agent-model-status' is valid`. | Change-artifact structure and scenario consistency only. | `e2def00966b1aab339850d03b52c9386a151e800` + working-tree change | OpenSpec |
| Agents inheriting a valid system route do not show yellow warnings and explain the effective route | Installed the built `static/` bundle with `packaging/native/macos/install.sh`, opened `http://localhost:1906/admin/agents` in the in-app browser, hovered the Default Assistant row, then focused the same button with a keyboard event | Both Default Assistant and Orchestrator rendered neutral information icons labeled `Using system default kimi-for-coding/k3`; pointer hover and keyboard focus each exposed `Uses system default kimi-for-coding/k3.` Browser console warning/error query returned `[]`. | Local macOS LaunchAgent and configured Kimi route only; no inference request was made. | `e2def00966b1aab339850d03b52c9386a151e800` + working-tree change | Installed React UI + `server-full` |
| A genuinely absent system route shows the yellow warning with an actionable hover explanation | Started an ephemeral loopback reverse proxy on `127.0.0.1:1907` that changed only `GET /api/uar/providers` to `{providers: [], default_id: null}`, opened its Agents page, and hovered the Default Assistant row | Both agents rendered amber triangles labeled `No model configured`; hover exposed `No usable model route. Assign a provider and model to this agent, or configure a system default.` The proxy was then stopped, port 1907 refused requests, and the real provider registry still reported `kimi-for-coding` / `k3`. | Functional negative control against the installed bundle; it did not mutate UAR configuration or persistence. | `e2def00966b1aab339850d03b52c9386a151e800` + working-tree change | Installed React UI + isolated provider-response control |
| Installed service remains operational after bundle replacement | `launchctl print gui/$UID/com.prometheus.universal-agent-runtime`; `curl -fsS http://127.0.0.1:1906/healthz`; provider-registry readback | Final LaunchAgent state `running`, PID `11420`; health returned `{"status":"ok"}`; real default read back as `kimi-for-coding` / `k3`; final browser reload again exposed the inherited-route tooltip. | Service liveness and configuration readback only. The existing `time` and `tavily` MCP reconnect warnings remain in the operational log and are unrelated to this UI change. | Installed release binary plus working-tree bundle | Local macOS LaunchAgent |

## Non-gating broader-test result

An initial `pnpm --dir frontend test -- <file>` invocation did not honor the intended file filter and ran all configured Vitest projects. It observed `323 passed` and `13 failed`: eight existing entity-story schema failures, two existing ChoicePicker story failures, one run-trace timing threshold failure, and two existing provider-store configure tests whose mock omits `updateProvider`. This accidental broader run is not reported as a passing gate and none of those unrelated failures was changed. The corrected explicit focused command is recorded in the first row.
