## 1. Implementation and phase acceptance

- [x] 1.1 Implement non-widening Presentation eligibility in global/agent/conversation/turn policy and compatibility deserialization.
- [x] 1.2 Integrate enabled owner-accessible catalog resources and delegated-thread ceilings into host resolution.
- [x] 1.3 Add graph-backed assignment controls showing inheritance and unavailable selections.
- [x] 1.4 At phase end, prove scope intersection, denied/disabled/foreign resources and delegated ceilings.

Task 1.1 implementation evidence: `cargo check --locked --no-default-features --features server-full` exited 0 in 1m01s without warnings. The independent artifact critic found no concrete defect in scoped resolution, old-policy deserialization or captured child/remote ceilings. Runtime tests remain 1.4. Policy-universe constructors intentionally remain empty for Presentations until task 1.2 wires verified host admission; session assignment DTOs/UI remain 1.3.

Task 1.2 implementation evidence: verified principal catalog admission excludes disabled, invalid and foreign records; read failures close Presentation eligibility. Runtime root/remote admission and discovery use this universe; delegated ceilings remain captured. Verified conversation policies use separate principal tables/maps and cache namespace, with read-only legacy fallback stripped of Presentation intent. Resets shadow legacy state; editor reads return 503 on unavailable storage. Independent source review found and then cleared namespace collision, reset fallback and stale-success read defects. Final `cargo check --locked --no-default-features --features server-full` exited 0 in20.42s without warnings; scoped `git diff --check` exited0. Database behavior, migrations and regression tests remain phase-end work, not evidence inferred from compilation.

Task1.3 implementation evidence: session, agent and global controls now use typed graph-owned assignment intent/drafts with current owner/catalog-generation admission, retained inactive selections, unavailable-ID removal, exclusions, reset, conditional host writes and explicit recovery without replay. Global restrictions require instance-wide confirmation. Agent edits use a strict persisted GET; HTTP execution honors persisted built-in restrictions and propagates storage failures. Final backend Tier0 check exited0 in28.74s without warnings; final `pnpm typecheck && pnpm lint` and scoped `git diff --check` exited0. Independent backend/UI source findings cleared. All tests, migrations, concurrency behavior, browser checks and Impeccable visual finish remain task1.4/whole-phase acceptance, not inferred from these static results.

Task1.4 acceptance (2026-09-05): full local server-full suite passed (744 library tests, 1 ignored; BDD9scenarios49steps; broad integration94passed/1ignored). Explicit Presentation scope intersection, parent None/exclusions, legacy policy deserialization, frozen child narrowing and same-subject cross-tenant admission regressions passed. Frontend phase suite passed462/462 across82files, including graph-owned assignment and six exact closed-label cases. Browser agent selection, confirmed global None/reset and conversation selection saved/read back in an isolated database. Independent Impeccable critiques and fresh source review cleared assignment corrections; desktop/narrow captures retained in /tmp/uar-presentation-evidence.gBZ6BG. Final typecheck/lint passed. This establishes local scoped eligibility and assignment acceptance, not live-peer, billing, release or all-backend certification. Deferred429, existing coverage limits and fixture credential-rotation notice remain in the phase execution record. Catalog PostgreSQL acceptance remains with its own task.
