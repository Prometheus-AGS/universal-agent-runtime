## 1. Establish the evidence contract

- [x] 1.1 Add a testing-history manifest with four guides, seven evidence classes, profile/source limits, and required methodology markers.
- [x] 1.2 Add local validation and isolated controls, register package commands, and compose validation into publication validation.

## 2. Publish testing history and practice

- [x] 2.1 Publish the dated methodology history from coverage/unit emphasis through recorded-provider diagnostics to bounded genuine-model functional acceptance.
- [x] 2.2 Publish the evidence taxonomy with a `proves` and `doesNotProve` boundary for every class.
- [x] 2.3 Publish negative-control practice with bounded inversion, observed failure, exact restoration, positive rerun, and retained output.
- [x] 2.4 Publish local verification timing with Tier 0–3 boundaries, delivery-first cadence, profile/source-SHA limits, and deployment-only Actions.
- [x] 2.5 Mark ADR-0003's routine-CI consequence superseded while retaining its local 60% coverage target as historical/currently configured intent rather than measured completion.

## 3. Verify after content completion

- [x] 3.1 Observe isolated failures for missing evidence limits, synthetic inference certification, missing negative control, profile transfer, duration-only soak, routine Actions testing, and raw-history copying before the complete source passes.
- [x] 3.2 Run testing-history, publication, truth, architecture-history, README, Actions policy, and Docusaurus TypeScript validation locally without product tests or the production build.
- [x] 3.3 Run strict OpenSpec and artifact-refiner content validation and correct every finding.
- [x] 3.4 Audit the diff for runtime, tests, React application, dependency, lockfile, workflow, frozen route, vendor, submodule, and raw-history changes.
- [x] 3.5 Write row-form verification with commands, observed output, limits, source SHA, and documentation profile.
- [x] 3.6 Transition KBD, refresh the handoff, verify the next command is `/opsx:new certify-and-publish-uar-docs`, and commit independently without pushing.
