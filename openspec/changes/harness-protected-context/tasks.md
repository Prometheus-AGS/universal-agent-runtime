## 1. Workflow and planning prerequisites

- [x] 1.1 Confirm the protected-context scope and scenario-to-fixture matrix; enumerate exact edit paths, acquisition/storage limits and legacy completeness migration in Plan, with a reviewed artifact proving every context strategy and entry point is covered.
- [x] 1.2 Create deterministic canonical receipt/history fixtures and final-request capture helpers; verify they reproduce tool-pair loss, pre-history truncation and empty-text assistant loss where present.

## 2. Implementation units after Plan

- [x] 2.1 Preserve canonical native/MCP/graph/terminal receipts before display formatting and enforce acquisition limits truthfully; verify byte/value identity and incomplete-result scenarios.
- [x] 2.2 Replace destructive normalization/reduction and persist meaningful empty-text turns; verify pending versus terminal records, parallel groups, repeated turns and invalid-history outcomes.
- [x] 2.3 Add the pure budget planner and minimum production request-boundary enforcement for explicitly resolved counting/output contracts; verify final envelope/output limits and every entry point with supported fixture dispatch plus invalid/overflow rejection.
- [x] 2.4 Restrict governed summarization to host-marked prose and charge shared usage; verify that protected bodies never enter summarizer inputs and all failure/cancellation cases retain originals.
- [x] 2.5 Integrate checkpoint/protection metadata and authorization invalidation; verify raw digests, legacy-incomplete behavior and revoked resume.

## 3. Integration evidence

- [x] 3.1 Run the applicable local unit checks for completed units and archive exact outputs; demonstrate final serialized-request fit or explicit outcome across all context strategies without claiming profile-wide certification.


## Plan bindings

The 44-task phase execution map at `.kbd-orchestrator/phases/runtime-harness-gap-closure/execution-map.json` binds this change’s numbered tasks to exact files, proposed fixture names and verification commands. Research decisions are now recorded in plan-research.md and plan-library-candidates.json; prerequisite tasks verify that evidence and complete the still-pending baseline/provider/environment gates before behavior edits. Checkboxes remain pending until their full task exit criteria pass.
