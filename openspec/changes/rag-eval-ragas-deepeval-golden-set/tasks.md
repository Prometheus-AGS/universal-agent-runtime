## 1. Golden-set format + seed items
- [x] 1.1 `evals/rag-golden-set/schema.json` — JSON Schema (draft 2020-12) for one item, field names mirroring ragas' `EvaluationDataset` columns (`user_input`, `reference_contexts`, `reference`).
- [x] 1.2 `evals/rag-golden-set/golden-set.seed.jsonl` — 14 items, all 5 stratification categories represented (`config-system` x3, `rag-pipeline` x5, `governance-licensing` x2, `model-routing` x2, `api-usage` x2), each grounded in and cited to an actual repo file (`example.config.yaml`, `src/uar/rag/{pipeline,verification,decomposition,retrieval}.rs`, `CONTRIBUTING.md`, `docs/ARCHITECTURE.md`, `CLAUDE.md`, `evals/README.md`). Validated: every item passes both the pydantic model and `schema.json` (`tests/test_schema.py`, 5/5 green).
- [x] 1.3 `evals/rag-golden-set/README.md` — format docs, freeze discipline (append-only after `frozen_at`), the 5-category stratification table, and an explicit **"Status: seed set, not the frozen 300"** section explaining the scope decision.
- [ ] 1.4 **Deferred, explicitly out of scope for this change**: growing the golden set from 14 seed items to the full 300. This is ongoing content-curation work (~25 human-equivalent hours per analysis.md 4.5), not a coding task — it needs a human (or a dedicated curation pass with human review) to write and verify 286 more question/context/ground-truth triples against real product knowledge. Owner: whoever owns RAG product quality going forward. Tracking: this task line + `evals/rag-golden-set/README.md`'s "Status" section are the paper trail until a dedicated follow-up change exists.

## 2. `prometheus-eval` harness package
- [x] 2.1 `tools/prometheus-eval/pyproject.toml` — `hatchling` build (matches `sdks/python/pyproject.toml`'s convention), pins `ragas>=0.4,<0.5`, `deepeval>=4.1,<5`, `beir>=2.2,<3`, and `langchain-community<0.4` (upstream ragas/langchain-community compatibility fix, verified against a live install — see task 2.2).
- [x] 2.2 Installed `ragas==0.4.3` + `deepeval==4.1.0` + `beir==2.2.0` into a scratch venv and actually imported them: found and fixed a real `ModuleNotFoundError` (ragas 0.4.3 eagerly imports `langchain_community.chat_models.vertexai.ChatVertexAI`, removed from `langchain-community>=0.4`); confirmed the non-deprecated `ragas.metrics.collections.{Faithfulness, AnswerRelevancy, ContextPrecisionWithReference, ContextRecall}` import path; confirmed `deepeval.test_case.LLMTestCase`'s real field names and `deepeval.models.GPTModel(model=, temperature=)` for pinning judge temperature.
- [x] 2.3 `prometheus_eval/schema.py` — `GoldenItem` pydantic model + `load_golden_set()` + `validate_against_json_schema()`.
- [x] 2.4 `prometheus_eval/uar_client.py` — `UarClient`: `.search()` / `.chat()` / `.run()` against a live UAR server, plus `.search_scored()` / `.create_knowledge_base()` / `.upload_document()` for the BEIR path. Field names (`SearchResponse.results[].{content,score,document_id}`, `DocumentResponse.id`) verified against `src/uar/api/knowledge.rs`'s actual struct definitions, not assumed.
- [x] 2.5 `prometheus_eval/ragas_runner.py` — wraps the 4 RAGAS metrics named in analysis.md ("faithfulness, answer_relevancy, context_precision, context_recall"); resolves result-dataframe columns by substring match (not hardcoded exact names) since the exact column names for these specific metric instances were not confirmed end-to-end without a live judge LLM.
- [x] 2.6 `prometheus_eval/deepeval_runner.py` — wraps DeepEval's `FaithfulnessMetric` / `AnswerRelevancyMetric` / `ContextualPrecisionMetric` / `ContextualRecallMetric` with a pinned-temperature `GPTModel`.
- [x] 2.7 `prometheus_eval/config/judge.yaml` + `config/judge_prompt.md` — frozen judge model (`openai/gpt-4o-mini`), temperature (`0.0`), seed, and `prompt_version` (`v1`); rubric prompt with a strict JSON-only output contract.
- [x] 2.8 `prometheus_eval/gate.py` — pure-stdlib regression comparison (`compare()`, `mean_scores()`, `load_scores()`); no ragas/deepeval/beir dependency, so it's testable and runnable even when those aren't installed.
- [x] 2.9 `prometheus_eval/cli.py` — `prometheus-eval validate|run|gate` (argparse); `validate` and `gate` verified to actually run end-to-end locally (see task 4).
- [x] 2.10 `prometheus_eval/beir_bench.py` — `ingest_corpus()` / `make_search_fn()` / `run_beir_dataset()` / `write_report()` / a `python -m prometheus_eval.beir_bench` CLI entry point for the monthly workflow.

## 3. CI workflows
- [x] 3.1 `.github/workflows/rag-eval.yml` — `structural` job (keyless: schema validation + `pytest tests/`) runs on every relevant PR; `real-model` job builds the server, runs `prometheus-eval run`, and gates via `prometheus-eval gate`, guarded on the `UAR_EVAL_JUDGE_API_KEY` secret (skips, does not fail, when absent — mirrors `eval-nightly.yml`'s pattern for the existing generic-suite harness) and fails loudly ("blocked until seeded") when the key is present but no baseline is committed.
- [x] 3.2 `.github/workflows/rag-benchmark-monthly.yml` — scheduled (`cron: '0 6 1 * *'`) + `workflow_dispatch`; builds the server, runs the BEIR benchmark across all 4 datasets, opens a PR with the resulting report via `peter-evans/create-pull-request`.
- [ ] 3.3 **Deferred, needs operator action**: configuring the `UAR_EVAL_JUDGE_API_KEY` repository secret and running `rag-eval.yml` with `update_baseline: true` to seed `evals/results/rag-golden-set.baseline.json`. Until then the gate job intentionally fails loudly rather than passing silently (same design as the existing `evals/starter.yaml` gate).
- [ ] 3.4 **Deferred, needs operator action**: the first scheduled or manually-dispatched run of `rag-benchmark-monthly.yml` to publish `docs/rag-benchmark/2026-XX.json`.

## 4. Verification (what was actually run)
- [x] 4.1 `python3 -m py_compile` on every new `.py` file — clean.
- [x] 4.2 `python3 -m pytest tools/prometheus-eval/tests/ -v` — 12/12 passing (`test_gate.py` x7, `test_schema.py` x5), no ragas/deepeval/beir install required for these.
- [x] 4.3 `prometheus-eval validate --golden-set evals/rag-golden-set/golden-set.seed.jsonl` — run directly, printed `OK: 14 items, categories=[...]` (all 5).
- [x] 4.4 `prometheus-eval gate --baseline ... --current ...` — run directly against hand-crafted fixture JSON, confirmed both the pass and fail paths (a 15-point synthetic regression correctly fails; a 1-point one correctly passes).
- [x] 4.5 `pip install -e tools/prometheus-eval[dev]` (plus the full `ragas`/`deepeval`/`beir` deps) into a scratch venv — succeeds after the `langchain-community` pin (task 2.2); confirms the dependency graph resolves, not just that the pins are syntactically valid.
- [ ] 4.6 **Not run, needs credentials/infra unavailable in this environment**: an actual `prometheus-eval run` against a live UAR server with a real judge API key; an actual RAGAS/DeepEval score on the golden set; an actual BEIR benchmark run. See `tools/prometheus-eval/README.md` "Verified against a live install" for the precise verified/unverified boundary.
- [x] 4.7 `openspec validate rag-eval-ragas-deepeval-golden-set --strict` passes.
