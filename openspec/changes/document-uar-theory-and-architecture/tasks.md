## 1. Establish the architecture authority contract

- [x] 1.1 Add `docs/publication/architecture.json` with exactly the seven planned guides, stable routes, explicit profiles, classified specification records, current source authorities, and required concept markers; verify the manifest parses and every referenced source path exists.
- [x] 1.2 Add the bounded architecture validator and isolated control script, register their local package commands, and compose the validator into the final publication entrypoint; defer executing the controls until all architecture content is code-complete.

## 2. Write the conceptual spine

- [x] 2.1 Rewrite `architecture/intro` as the source-grounded purpose and system map, including the problem UAR solves, runtime theory, authority flow, conceptual navigation, and text explanations for every Mermaid diagram; verify its manifest markers and provenance are present after the section is complete.
- [x] 2.2 Add the trust-boundary and execution-lifecycle guides, distinguishing intent from effects and request/turn/step/tool/terminal flow; verify each states the agent/host boundary, failure semantics, profile limits, and current authority.
- [x] 2.3 Add the state-and-events and profiles guides, distinguishing normalized events, configured persistence, conversational state, `server-full`, `minimal`, and `embedded-mobile`; verify no evidence or capability claim transfers silently between profiles.
- [x] 2.4 Add the protocols and delegation guides, distinguishing typed entrances from shared execution control and the simple tool loop from current graph execution; verify deferred subagent-provider architecture and future protocol work are not described as delivered.

## 3. Reconcile architecture navigation and truth

- [x] 3.1 Update the Architecture category metadata and cross-links so all seven routes are reachable in conceptual order and all links resolve to current documents; verify no unrelated portal section or product navigation is changed.
- [x] 3.2 Audit every present-tense statement against the authority manifest and current checkout, remove or qualify unsupported claims, and verify no raw `.prometheus`, KBD event/session payload, machine-local path, credential, private proposal text, or absent `versions.toml` claim enters the public pages.

## 4. Verify only after architecture code and content are complete

- [x] 4.1 Run the isolated architecture controls and observe every missing-page, missing-source, invalid-profile, missing-provenance, missing-profile-limit, missing-trust-boundary, and missing-diagram-explanation negative control fail before the complete source fixture passes.
- [x] 4.2 Run the Docusaurus TypeScript check and bounded documentation-source validators locally, recording commands and observed output without running the phase-level production build or browser certification.
- [x] 4.3 Run `openspec validate document-uar-theory-and-architecture --strict` and the artifact-refiner content gate; correct findings until the OpenSpec bundle, refiner schemas, and referenced evidence files pass.
- [x] 4.4 Audit the diff for runtime, React application, provider/model, realtime, dependency, vendored, README, non-documentation lockfile, and raw `.prometheus` changes; remove anything outside this change's permitted documentation/process surface.
- [x] 4.5 Write row-form `verification.md` with requirement, command, observed result, limit, source SHA, and documentation profile, explicitly deferring full build, visual, accessibility, deployment, and cross-profile claims.
- [x] 4.6 Transition the registered KBD change through the canonical runtime, refresh the human handoff, verify the exact next command names `document-inference-skills-knowledge-and-agents`, and commit the complete change independently without pushing the partial phase.
