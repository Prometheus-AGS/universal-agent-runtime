## 1. CI Contract

- [x] 1.1 Align the primary CI Clippy command with the locked `server-full` no-dependency release gate.
- [x] 1.2 Align the adjacent Cargo check with the authoritative locked `server-full` checkpoint.

## 2. Static Validation

- [x] 2.1 Extend the supply-chain validator to assert the exact CI lint and check commands.
- [x] 2.2 Run workflow syntax, static contract, OpenSpec, and cohesive Cargo validation.

## 3. Candidate Supersession

- [ ] 3.1 Merge the isolated correction and create a newly signed immutable RC4 from the resulting `main` SHA.
