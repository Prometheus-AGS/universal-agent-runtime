# Verification — `gap-03-a2a-tenant-partitioning`

Date: 2026-08-14
Profile scope: `server-full` only. These results transfer to no other profile.

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| Tenant identity is distinct from user identity and is derived only from verified claims. | `verified_tenant_claim_becomes_typed_principal_identity` passed: verified `sub=user-123` produced typed tenant `tenant-a`, and the subject remained distinct. `TenantId` has no public string constructor; production creation requires a proof value whose constructor is private to the verifier module. | Not a fail-closed assertion. The unverified-token rejection remains A1 evidence; A2 does not parse unverified claims. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib tenant -- --test-threads=1` | Exit 0. Named verifier assertion passed within `10 passed; 0 failed`; `563 filtered out`. |
| A tenant supplied in the HTTP body, query, or header is ignored. | `body_query_and_header_tenant_values_cannot_override_verified_tenant` supplied `tenant-b` in all three locations while the verified context carried `tenant-a`; the request continued tenant A's task and left tenant B's task unchanged. | Not a fail-closed assertion. Tenant selection has one input: the middleware-established `Extension<UserContext>`; the request values are not extractors or fields in that selection. | Same focused tenant command. | Exit 0. Named spoofing assertion passed. |
| Task records are partitioned by `(tenant, task_id)`, including gRPC access. | Store and gRPC assertions passed for same-tenant read and cross-tenant denial. | With task lookup ignoring the tenant key, the gRPC cross-tenant read returned tenant A's working task and the store denial assertion failed. | Same focused tenant command; literal control in `evidence/fail-closed-negative-controls.md`. | Positive exit 0. Inverted exit 101: `6 passed; 3 failed`; task-ID and gRPC denial assertions failed. |
| The context index is partitioned by `(tenant, context_id)`. | Tenant A's context lookup succeeded and tenant B's same context lookup returned no task. | With both context-index selection and resulting task retrieval ignoring tenant keys, the exact context assertion failed. | Exact `partitions_task_and_context_lookup_by_tenant` command in `evidence/fail-closed-negative-controls.md`. | Positive exit 0: `1 passed; 0 failed`. Inverted exit 101: `0 passed; 1 failed`. |
| Cross-tenant cancel is denied and leaves the owning tenant's task unchanged. | `cross_tenant_cancel_does_not_mutate_task` and the gRPC partition assertion passed; tenant A's task remained `working`. | With cancel ignoring the tenant key, `!store.cancel(tenant_b, task_a)` failed. | Focused tenant command; literal control in `evidence/fail-closed-negative-controls.md`. | Positive exit 0. Inverted exit 101 at the cancel denial assertion. |
| A tenant-scoped surface fails closed when JWT verification is required but no tenant is established. | `required_jwt_without_verified_tenant_is_rejected` returned JSON-RPC `INVALID_REQUEST` (`-32600`) and `verified tenant claim required`. `grpc_required_jwt_without_verified_tenant_is_rejected` presented a valid token without `tenant_id` and observed gRPC `Unauthenticated` with the same message. | With only the HTTP guard disabled, its exact assertion observed `TASK_NOT_FOUND` (`-32001`) instead of `INVALID_REQUEST`. The separate gRPC guard control is recorded in `evidence/fail-closed-negative-controls.md`. | Exact HTTP and gRPC required-tenant commands in `evidence/fail-closed-negative-controls.md`. | HTTP positive exit 0 and inverted exit 101. gRPC positive/control results recorded separately. |
| The published C-21 exclusion is replaced by a real two-tenant live assertion covering task ID, context ID, cancel, and same-tenant access. | The exclusion function was replaced and the integration target compiled. The live assertion has not run because Tier 2 is prohibited before all six phase changes are implemented. | Deferred with the live assertion to phase completion. The focused store/gRPC controls above do not substitute for the live negative control. | `cargo check --locked -p universal-agent-runtime --no-default-features --features server-full --test integration` | Compile-only exit 0 in 1m32s. **Not yet satisfied as live evidence.** |
| Final A2 source passes change-level Tier 0/Tier 1 checks without a new A2-owned warning. | Package check, package/library/no-deps Clippy, focused tenant tests, and integration-target compile all exited 0. | Not a fail-closed assertion. | `cargo check --locked -p universal-agent-runtime --no-default-features --features server-full`; `cargo clippy --locked -p universal-agent-runtime --no-default-features --features server-full --lib --no-deps`; focused tenant command; integration-target check. | Check exit 0 with 3 recorded pre-existing warnings. Clippy exit 0; the only A2-owned doc warnings were removed and the compact rerun reported none in `task_store.rs`. Focused tenant result: `10 passed; 0 failed`. |
| The OpenSpec delta and current implementation ledger remain strict-valid. | Strict validation passed after the C-21 tasks were left unchecked for their deferred live evidence. | Not a fail-closed assertion. | `openspec validate gap-03-a2a-tenant-partitioning --strict` | Exit 0: `Change 'gap-03-a2a-tenant-partitioning' is valid`. |
| Independent adversarial review accepts the implementation slice without claiming A2 completion. | The history-free judge passed the isolated implementation commit with canonical A2 left in progress. The critic initially blocked verifier provenance, gRPC no-tenant evidence, and premature C-21 task checks; all three were corrected and the critic returned PASS on re-review. | Not a fail-closed assertion. | Independent artifact critic and judge review of the current A2 artifact. | Judge PASS; corrected critic PASS. Artifact-refiner remains deferred with A2 completion. |
| A2 stays inside its permitted surface and does not scope runs, memory, or knowledge bases. | Behavioral edits are limited to security claims/verifier/middleware and A2A store/transports, with server wiring and operator-approved compile-only `tenant_id: None` additions. | Not a fail-closed assertion. | `git diff --name-only`; focused source inspection. | No A2 stop condition fired. Memory/run/KB behavior is unchanged. |

Phase Tier 2 and artifact-refiner QA have not run. A2 remains in progress until
the live C-21 assertion and its live negative control are observed at phase
completion.
