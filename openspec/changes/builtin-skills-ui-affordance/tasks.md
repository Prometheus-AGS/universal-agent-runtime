## 1. Skill list

- [x] 1.1 Skill row renders Built-in pill (Shield icon + label) when `origin === "builtin"`.
- [x] 1.2 Disabled Edit + Delete buttons with explanatory `title` for built-ins.
- [ ] 1.3 Filter chips wired to URL query — deferred (out of scope for this minimal affordance).

## 2. Skill detail

- [ ] 2.1 `kind` + AOT marker display — deferred (no detail panel today; current page is list-only).

## 3. Tests

- [ ] 3.1 Snapshot tests — deferred to integration-tests-and-docs.
