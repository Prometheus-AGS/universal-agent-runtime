# Verification — `ship-skill-pack-install-path`

Date: 2026-08-18
Profile scope: `server-full` on macOS, plus the separately named pack CLI build.
These results transfer to no other profile or platform.

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| The default source is public HTTPS and resolves the UAR-pinned commit without SSH credentials. | A fresh temporary Git repository fetched `c25561548aeb9ca656fdb942ab34378beedc2fe2` from the public HTTPS URL; the parent submodule URL now uses the same URL. | A clean checkout one commit beyond the pin was refused before build. | Public fetch command and installer control are recorded in `evidence/positive-verification.md` and `evidence/negative-controls.md`. | Public fetch exited 0 and resolved the exact pin. Wrong-commit installation exited nonzero and printed `does not match UAR pin`. |
| The installer performs the real locked Rust build and installs a verified version. | The default network path cloned recursively, built `prometheus-cli` with `cargo build --locked --release`, and installed 311 manifests. The installed binary reported `prometheus 1.7.0`; `UAR_PACK_COMMIT` matched the pin. | A forced Cargo exit 42 stopped before activation; the target version directory did not exist. | `CARGO_TARGET_DIR=/Users/gqadonis/Library/Caches/cargo-build/uar-skill-pack-real scripts/install-uar-skill-pack.sh --prefix <temporary-prefix>` | Real build/install exited 0: `Finished release profile ... in 4m 53s`; version `1.7.0`; commit matched; 311 manifests. The later staging-only correction is covered by the current deterministic installer and API replays. |
| Failed verification/build never exposes a partial version, and replacement is atomic. | The final script stages beside—not within—the loader's two-level search root, activates by same-filesystem rename, restores the prior complete version on activation failure, and cleans abandoned staging. | Wrong-commit and build-failure controls both stopped without an activated version. | `bash scripts/tests/test-install-uar-skill-pack.sh` | Exit 0: clean-prefix install PASS; wrong-commit negative control PASS; failed-build negative control PASS. |
| A clean installed prefix feeds the complete default loader inventory to the admin API as non-deletable built-ins. | The integration test installed into a temporary clean home, forced installed-plugin resolution, required exactly 147 discovered skills, compared the exact discovered and `/api/skills` ID sets, and required every origin to equal `builtin`. All 311 manifests are copied; the existing `skills/imported/` opt-in remains unchanged. | Removing or invalidating the installed cache would make the source assertion, exact count, or exact ID-set equality fail; this is an inventory assertion rather than a new fail-closed security guard. | `cargo test --quiet --locked -p universal-agent-runtime --no-default-features --features server-full --test skill_pack_install_path -- --test-threads=1` | `1 passed; 0 failed; 0 filtered out`. |
| Installation and upgrade are documented without silently bootstrapping or using sudo. | The guide names Git/Rust prerequisites, public URL, pin behavior, default/custom roots, API verification, side-by-side upgrade, rollback, and exact-pin offline source mode. | Not a fail-closed assertion. | Source inspection and scoped diff check. | Documentation exists at `docs/skill-pack-installation.md`; no remote bootstrap command is executed by the installer. |
| Change-level verification gates pass. | Bash syntax, ShellCheck when installed, formatting, Tier 0, scoped Clippy, strict OpenSpec, artifact-refiner validation, and scoped diff checks exited 0. Independent critic and judge both passed the corrected candidate. | Not a fail-closed assertion. | Exact commands are in `evidence/positive-verification.md`. | Check exited 0 with three known warnings. Clippy exited 0 with 574 warnings; artifact schemas/references/state passed 4/4 and converged; no warning-free claim is made. Phase Tier 2 remains deferred. |
