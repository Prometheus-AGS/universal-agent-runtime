| requirement | assertion observed | negative control observed | command and observed output | source SHA | profile / platform | limit |
|---|---|---|---|---|---|---|
| Service credentials are generated from an allowlist | Unix and PowerShell generators emitted exactly the five resolved canonical provider names; canonical Kimi won over its present alias; Unix mode was `600`. | A Kimi value containing a newline exited nonzero and the previous service environment hash remained unchanged. | `generate-provider-env.sh --output <temp>` plus redacted name/value comparison → `canonical and alias precedence PASS`, `environment permissions PASS`, `invalid credential fail-closed and previous file preserved PASS`; PowerShell equivalent → `PowerShell provider bootstrap behavior PASS`. | `c394f13615afa1f8979dccf4e06d217ced2ad116` plus this change's worktree diff | native bootstrap / macOS host; Windows script executed in PowerShell 7 only | Synthetic sentinel values only; values never printed or retained; no supervisor/runtime claim. |
| Native YAML enables both loopback listeners | Default YAML parsed with host `127.0.0.1`, HTTP `1906`, and gRPC `50051`. | Existing operator YAML with host `0.0.0.0`, port `1906`, and default model `openai/gpt-4o` retained those values while only missing `grpc_port` and providers were added. | Python YAML assertions → `additive provider YAML and secret exclusion PASS`; operator fixture → `existing operator listener and default-model values preserved PASS`. | `c394f13615afa1f8979dccf4e06d217ced2ad116` plus this change's worktree diff | native bootstrap / macOS host | Static YAML/merge claim only; live loopback listeners are deferred to change 5. |
| Native provider seed contains only concrete supported models | Live proxy inventory was non-empty; merged YAML contained `local-openai-proxy`, `kimi-for-coding`, `minimax`, `alibaba`, `zai`, and `moonshotai`; no service-env value appeared in YAML. | Generator input included only approved aliases/canonicals; output name-set equality rejected any tool-only, RunPod, endpoint-less, or alias name. | `curl .../v1/models | jq ...` → `local proxy inventory endpoint PASS`; merge assertions → `additive provider YAML and secret exclusion PASS`; second merge hash matched → `provider merge idempotency PASS`. | `c394f13615afa1f8979dccf4e06d217ced2ad116` plus this change's worktree diff | provider bootstrap / macOS host | Inventory lookup only; no inference request; proxy unavailability omits rather than fabricates models. |
| Provider bootstrap preserves persistent settings authority | Merge changed only absent top-level server keys and absent provider IDs; repeated merge was byte-identical. No database file or persisted provider row was accessed. | Operator fixture retained its existing host, port, and selected LLM model rather than accepting native defaults. | `merge-provider-config.sh` twice plus SHA-256 comparison → `provider merge idempotency PASS`; fixture assertion → `existing operator listener and default-model values preserved PASS`. | `c394f13615afa1f8979dccf4e06d217ced2ad116` plus this change's worktree diff | provider bootstrap / macOS host | YAML authority only; persisted database precedence is verified through the installed service in change 5. |
| Bootstrap helpers integrate with native entrypoints | All six common helper/default files existed; macOS, Linux, and Windows install/refresh entrypoints referenced their platform helper paths. Bash, ShellCheck, Python compile, and PowerShell AST checks passed. | N/A — structural integration assertion, not fail-closed behavior. | Static commands → `native bootstrap syntax checks PASS`, `installer helper references resolve PASS`, `default native YAML path PASS`. | `c394f13615afa1f8979dccf4e06d217ced2ad116` plus this change's worktree diff | macOS runtime scripts; Linux/Windows template syntax only | No Linux or Windows service-runtime claim transfers from this host. |
| OpenSpec change is strict-valid | Strict validation accepted both capability deltas and all scenarios. | N/A — schema validation assertion. | `openspec validate bootstrap-native-provider-model-configuration --strict` → `Change 'bootstrap-native-provider-model-configuration' is valid`. | `c394f13615afa1f8979dccf4e06d217ced2ad116` plus this change's worktree diff | OpenSpec / platform-neutral | Change 4 only. |
