# Verification — `emit-rag-retrieval-events`

Date: 2026-08-18
Profile scope: `server-full` on macOS, Chromium through the BDD dev stack, and
PostgreSQL 17 with pgvector 0.8.6 where named. These results transfer to no
other profile, browser, database version, or platform.

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| Retrieval hits are visible provenance events. | The normalized event and AG-UI mapping carried knowledge-base ID, document ID, and document name. The browser scenario displayed the retrieved answer, source badge, and hover-card filename. | `empty_matches_produce_empty_stream` observed that no citation event is produced without a retrieval hit. | `cargo test --quiet --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::api::sse::tests::maps_rag_citation_with_knowledge_base_and_document_provenance -- --exact --test-threads=1`<br>`cargo test --quiet --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::rag::citation_stream::tests::empty_matches_produce_empty_stream -- --exact --test-threads=1`<br>`pnpm exec playwright test -c tests/bdd/playwright.config.ts --grep "Retrieval-influenced response" --workers=1` | SSE: `1 passed; 0 failed; 605 filtered out`.<br>Empty-hit control: `1 passed; 0 failed; 605 filtered out`.<br>Browser: `1 passed (17.8s)`. |
| Chat uses the hardened retrieval pipeline. | The chat manager invokes `RagRetrievalPipeline`; the focused suite observed decomposition passthrough, cross-query deduplication, verification filtering/default behavior, post-score result limiting, and the structured `rag.retrieval.decision` event with KB identity and returned count. The browser scenario observed retrieved content in the outgoing prompt. | `drop_uncorroborated_filters_when_enabled` observed an unrelated high-scoring chunk being excluded when the verification guard is enabled; the dedup test observed the lower-scoring duplicate being discarded. | `cargo test --quiet --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::rag::pipeline::tests -- --test-threads=1`<br>`cargo test --quiet --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::rag::pipeline::tests::drop_uncorroborated_filters_when_enabled -- --exact --test-threads=1` | Pipeline suite: `6 passed; 0 failed; 600 filtered out`.<br>Verification control: `1 passed; 0 failed; 605 filtered out`. |
| Ingestion status reaches indexed. | A document began pending and was read back as indexed through embedded SurrealKV. The existing PostgreSQL lifecycle test passed against a fresh isolated PostgreSQL 17 database with pgvector. | Not a fail-closed assertion. The assertion compares the persisted precondition (`Pending`) with the observed postcondition (`Indexed`) on each named provider. | `cargo test --quiet --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::persistence::providers::surreal::tests::document_status_reaches_indexed_on_embedded_surrealdb -- --exact --test-threads=1`<br>`DATABASE_URL=postgres://127.0.0.1:55439/uar_rag_test cargo test --quiet --locked -p universal-agent-runtime --no-default-features --features server-full,postgres-backend --test knowledge_base_integration test_document_lifecycle -- --exact --test-threads=1` | SurrealKV: `1 passed; 0 failed; 605 filtered out; finished in 0.15s`.<br>PostgreSQL: `1 passed; 0 failed; 9 filtered out; finished in 0.11s`. |
| Change-level gates pass. | Rust formatting, Tier 0, scoped Clippy, TypeScript, ESLint, frontend build, BDD generation, strict OpenSpec, artifact schemas/references/state, and scoped diff checks are the required change-level gates. | Not a fail-closed product assertion. Independent critic and judge are the adversarial control and remain required before convergence. | Exact commands and outputs are recorded in `evidence/positive-verification.md`; behavioral controls are recorded in `evidence/negative-controls.md`. | Current local gates pass. Clippy exits 0 with 571 warnings; no warning-free claim is made. Phase Tier 2 remains deferred. |
