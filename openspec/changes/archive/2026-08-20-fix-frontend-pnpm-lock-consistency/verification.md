# Verification

Date: 2026-08-20
Profile: nested frontend pnpm workspace on macOS, pnpm 11.15.0; lifecycle scripts disabled for installation controls

| Requirement | Command | Observed result | Limit |
|---|---|---|---|
| Nested workspace accepts frozen installation | Run `pnpm --dir frontend install --lockfile-only --frozen-lockfile --ignore-scripts`; then in a detached worktree with no `frontend/node_modules`, run `pnpm --dir frontend install --frozen-lockfile --ignore-scripts`; hash both locks before and after | Exit `0`; the clean command linked 1,191 packages and preserved nested SHA-256 `43c00bbfe5b85e42c12a5fda74ab987750863794f00104a12ecd24a59f822593` and root SHA-256 `645e3af883e8d62b74d13be20453c083431ed3cf2ef3ca20a5b1a84152273350`. Full output: `evidence/frozen-install-verification.md`. | Proves nested graph materialization, supply-chain policy acceptance, and both-lock immutability with lifecycle scripts disabled for source `1274039a` plus the candidate lock. It does not prove build scripts or browser behavior. |
| Nested workspace submodule advances without lock reconciliation | In clean source `1274039a`, run `pnpm --dir frontend install --frozen-lockfile --ignore-scripts` against the committed nested lock | Negative control: pnpm exited `1` with `ERR_PNPM_OUTDATED_LOCKFILE`, reporting 17 added and 12 mismatched entity-management dependencies; the stale lock hash remained unchanged. Full output: `evidence/stale-lock-negative-control.md`. | Proves rejection of the stale nested lock for the exact source and pin. No build or product assertion was run. |
| Nested lock reconciliation preserves unrelated resolutions | Regenerate twice in independent detached worktrees; apply the exact three-body accepted-to-raw patch and verify the reverse raw-to-accepted transformation; classify every mutation with `evidence/audit-lock-delta.mjs`; resolve every anchor against the actual manifest section | The historical regenerations were byte-identical at `0a7145d6…`; the retained patch reconstructs that raw hash from candidate `43c00bbf…`; the audit classified all 693 mutations against specific dependency-graph paths from 44 named pinned-manifest edges with 0 unclassified, 0 blanket all-edge anchors, 0 empty anchors, 0 missing manifest paths, 0 candidate-specifier mismatches, 0 common package-body mutations, and only 3 causal common snapshot-body mutations. Full output: `evidence/deterministic-regeneration.md`, `evidence/head-candidate-delta-audit.md`, and `evidence/lock-delta-classification.json`. | The original scratch worktrees were later reused, so the raw bytes are reconstructed from the retained exact forward patch and verified against the originally observed hash; reverse transformation is asserted by the audit. Removed pnpm auto-peer projections have a current manifest anchor but no candidate importer specifier. This is a causal lock-delta comparison, not a dependency freshness, security, or cross-platform verdict. |
| Every command uses the lock for its actual execution root | Name and hash `frontend/pnpm-lock.yaml` before nested frozen commands; separately hash root `pnpm-lock.yaml` | Every nested command named the ten-project frontend scope and retained both recorded hashes; a successful root-only check was not substituted. Full output: `evidence/frozen-install-verification.md`. | Covers only the nested frontend execution root and the root-lock non-mutation check. |
| Parent TypeScript and focused SSE compatibility | Under `set -euo pipefail`, run frozen nested metadata validation, `pnpm typecheck`, `pnpm lint`, and `pnpm -C frontend test src/entities/sync.test.ts`; compare both lock hashes before/after | Exit `0`; typecheck and lint emitted their expected commands; focused Vitest reported 1 file and 4 tests passed; both locks remained unchanged. Full output: `evidence/tier-zero-and-focused-unit.md`. | Covers the repository TypeScript/lint profile and four SSE adapter unit tests. It does not replace parent build or browser certification. |
| Child implementation changes no manifest, root lock, gitlink, product source, generated output, or parent evidence | Under `set -euo pipefail`, assert immutable hashes/gitlink and clean diffs, inspect every untracked child text artifact for whitespace/conflict defects, and assert an empty index | Immutable inputs retained their recorded hashes; gitlink remained `0352c83d…`; 37 untracked child text artifacts were read; all assertions passed; the index was empty. Full output: `evidence/scope-integrity.md`. | Scoped dirty-worktree assertion only; pre-existing denied-path changes are enumerated and excluded. |
| OpenSpec child contract is strict-valid | `openspec validate fix-frontend-pnpm-lock-consistency --strict --no-interactive` | Exit `0`: `Change 'fix-frontend-pnpm-lock-consistency' is valid`. | Validates the active child change before independent review and archive. |
| Artifact state converges and persists only after independent review | Validate manifest, constraints, converged iteration-4 state, eleven chronological checkpoints, five satisfied constraints, references, current hashes, active/history byte identity, strict OpenSpec, and scoped text/diff checks | Exit `0`; critic and judge both returned PASS; all five constraints, eleven checkpoints, final hashes, schemas, references, and active/history identity passed. Full output: `evidence/artifact-refiner-validation.md`. | Direct-content artifact gate for this child only; no registry rewrite, parent browser certification, or release verdict. |

No parent browser certification, runtime test, release test, install-script test,
or cross-platform claim is made by this child. Parent certification must mint a
new immutable bundle from the child commit.
