# Verification

Date: 2026-08-20
Profile: root pnpm workspace on macOS, pnpm 11.15.0, lifecycle scripts disabled for installation checks

| Requirement | Command | Observed result | Limit |
|---|---|---|---|
| Frozen installation accepts the committed workspace | Run `pnpm install --lockfile-only --frozen-lockfile --ignore-scripts`, then in a disposable external worktree with no root or frontend `node_modules`, run `pnpm install --frozen-lockfile --ignore-scripts`; hash before and after each command | Exit `0`; the clean command validated 1,482 supply-chain entries, linked 1,345 packages, and preserved `645e3af883e8d62b74d13be20453c083431ed3cf2ef3ca20a5b1a84152273350`. Full output: `evidence/frozen-install-verification.md`. | Proves clean frozen graph installation, supply-chain lock-policy acceptance, and lock immutability with lifecycle scripts disabled. It does not prove builds, browser behavior, or install-script behavior. |
| Workspace submodule manifest advances without lock reconciliation | In clean source `fa4ffb96af63131b831c4f30a1b2c16aca599808`, run `pnpm install --frozen-lockfile` | Negative control exited `1` with `ERR_PNPM_OUTDATED_LOCKFILE`, reporting 17 added dependencies and 12 mismatched specifiers for the entity-management manifest. Full output: `evidence/stale-lock-negative-control.md`. | Proves the stale committed lock was rejected for that exact source. No build or product assertion was run. |
| Lock reconciliation preserves unrelated resolved versions | Parse and compare `HEAD:pnpm-lock.yaml` with the corrected candidate; separately compare the twice-identical clean regeneration | All 102 importer specifier changes are inside the advanced submodule; common package mutations are `0`; only three common snapshots remain changed and each retargets causal peer contexts. The unchanged config-array stays on minimatch 10.2.5 and unchanged `y-webrtc` stays on `ws` 8.21.0; the changed sync importer separately uses its direct `ws` 8.21.1 pin. Full output: `evidence/head-candidate-delta-audit.md` and `evidence/candidate-comparison.md`. | This is a causal lock-delta comparison, not a dependency security or freshness verdict. |
| Root-lock repair changes no manifest, submodule pin, product source, generated bundle, or parent certification artifact | `git ls-tree HEAD frontend/packages/prometheus-entity-management`; scoped `git status --short`; `git diff --check` on child surfaces | Git link remained `0352c83d7b386db56ffea8304ffdf3e2edb00fc8`; child status contained only `pnpm-lock.yaml` and child artifacts; `git diff --check` exited `0` with no output. Pre-existing denied-path changes were enumerated as commit exclusions. Full output: `evidence/scope-integrity.md`. | This is a scoped dirty-worktree assertion. It does not claim unrelated operator/parent paths are clean. |
| TypeScript Tier 0 | `pnpm typecheck && pnpm lint` | Exit `0`; observed `$ pnpm -C frontend typecheck`, `$ tsc -b`, `$ pnpm -C frontend lint`, `$ eslint .`. Full output: `evidence/tier-zero.md`. | Covers the repository TypeScript and lint profile only. It does not replace the parent build or browser certification. |
| OpenSpec child contract is strict-valid | `openspec validate fix-pnpm-lock-submodule-consistency --strict --no-interactive` | Exit `0`: `Change 'fix-pnpm-lock-submodule-consistency' is valid`. | Validates the active child change before independent review and archive. |
| Artifact gate converges after independent review | Validate manifest, constraints, converged state, and seven checkpoints against the canonical schemas; verify references, five constraint IDs, active/history identity, strict OpenSpec, and scoped diff | Exit `0`; three primary schemas, seven checkpoint schemas, five constraint IDs, 5/5 convergence, 15 active/history files, and strict OpenSpec passed. Fresh history-free critic and judge both returned PASS after their first BLOCK reviews were corrected. Full output: `evidence/artifact-refiner-validation.md`. | Direct-content evidence gate only; it adds no product or cross-profile result. |

The first clean-install correction failed closed on a missing direct `ws`
record; its command and output remain in
`evidence/clean-install-failed-control.md`. No parent browser certification, runtime test, release test, or cross-platform
claim is made by this child. The parent must certify a new immutable source
commit with a frozen installation after this child is committed.
