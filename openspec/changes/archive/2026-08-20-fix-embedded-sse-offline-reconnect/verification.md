# Verification — `fix-embedded-sse-offline-reconnect`

Date: 2026-08-20
Profile scope: local Chromium, Vite frontend, UAR `server-full`, embedded
SurrealDB SSE, and the source `prometheus-entity-management` workspace. Results
transfer to no other browser, runtime profile, provider, deployment, or
platform. Recovery resumes delivery from the replacement connection; it does
not replay or claim lossless delivery for the disconnected interval.

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| Named embedded events map through the canonical graph contract. | A valid `entity.change` for `knowledge_bases` produced one `KnowledgeBase` change with the supplied record. Transport-only, unnamed, unknown, and malformed events produced no changes while the subscription remained usable. | Independent review of the first candidate dispatched scalar `record: "not-an-object"` and observed `{"scalarRecordDeliveries":1}`. The corrected focused test dispatches transport-only, unnamed, unknown-table/action, missing-ID, and scalar-record payloads and observes zero graph changes before a later valid event succeeds. | `pnpm -C frontend test src/entities/sync.test.ts` | Corrected exit 0: 1 file passed, 3 tests passed, 0 failed. |
| Recovery closes the failed source, opens one replacement, and does not duplicate delivery. | The unit control observed `connecting` → `connected` → `error` → `connecting` → `connected`, one replacement EventSource, and one graph change from one post-reconnect event. | Before the timer advanced, the failed source was closed and no replacement existed. After the replacement event, the received-change count was exactly one; a parallel or retained predecessor would fail the source-count/closed/count assertions. | `pnpm -C frontend test src/entities/sync.test.ts` | Exit 0: reconnect and exactly-once assertions passed inside the 3-test focused file. |
| Unsubscribe owns complete cleanup. | Unsubscribe removes the named listener and callbacks, closes the active source, clears its pending retry, reports `disconnected`, rejects a late event from the closed source, and opens no later source. | Independent review of the first candidate observed `{"sourceClosed":true,"deliveriesAfterUnsubscribe":1}`. The corrected control schedules recovery, unsubscribes, dispatches through the closed fake source, advances all timers, and requires zero deliveries and one total source. | `pnpm -C frontend test src/entities/sync.test.ts` | Corrected exit 0: the late-event and pending-retry cancellation assertions passed. |
| Existing-ID entity updates rerender source-package views. | Both `useEntityView` and `useEntityQuery` now subscribe their projected items to stable entity snapshots; the same IDs rendered `After` after the graph update. | Before the upstream source repair, both focused tests failed because the rendered value remained `Before`. | `pnpm --filter @prometheus-ags/prometheus-entity-management exec vitest run src/view/use-entity-view.test.tsx --reporter verbose` in the upstream worktree, before and after implementation | Negative: 2 tests failed, both retained `Before`. Positive: 1 file passed, 2 tests passed, 0 failed. Upstream source fix merged in PR #19 and is contained by source/compatibility head `0352c83d7b386db56ffea8304ffdf3e2edb00fc8`. |
| The upstream workspace admits this pnpm 11 consumer without changing its pinned default. | The upstream contract accepts pnpm 11.15.0 while the root `packageManager` remains integrity-pinned to pnpm 10.33.0. UAR typecheck and lint both ran under pnpm 11.15.0. | Before widening the engine range, the named pnpm 11 assertion failed with `false !== true`, and UAR dependency preparation was refused by the nested engine constraint. | `pnpm exec cucumber-js --config cucumber.mjs --tags '@v3-main-ci-baseline' --name 'pnpm 11 consumer workspaces are admitted'`; then UAR `pnpm typecheck` and `pnpm lint` | Upstream: 1 scenario passed, 2 steps passed, 0 failed. UAR: typecheck exit 0; lint exit 0. Compatibility PR #20 is open at head `0352c83d7b386db56ffea8304ffdf3e2edb00fc8`. |
| The requested upstream package version is tested and delivered without publication. | Changesets generated all 12 fixed packages at `3.0.0-rc.2`; the React package passed typecheck/build/tests and its 203-export ledger before the canonical version branch was pushed. | No registry publication, npm tag, or dist-tag was performed; a version PR is not represented as a published package. | `pnpm changeset version`; `pnpm --filter @prometheus-ags/prometheus-entity-management prepublishOnly`; `pnpm install --frozen-lockfile` | Generated version gate passed. Canonical release PR #21 is open at `5afa07ba568ac44e4989847ee7db17d02f607752`; UAR intentionally pins the separate source/compatibility head. |
| BDD preparation builds source declarations in dependency order. | Turbo selected `@prometheus-ags/entity-graph-core` and `@prometheus-ags/prometheus-entity-management`, built core before React declarations, then the Vite/static/Rust/BDD preparation completed. | The previous direct React-only `tsup` command failed with `TS2724`: `@prometheus-ags/entity-graph-core` had no exported member `getGraphSyncStatus` in stale declarations. | `pnpm test:bdd:prepare` | Exit 0 after correction: 2 Turbo build tasks succeeded; Vite built; the static bundle validator reported 13 assets; the locked `server-full` Rust build and BDD generation completed. |
| The registered application adapter visibly recovers without reload, probe, store injection, or manual replay. | The browser created a Knowledge row through the product UI, delivered the initial named update through the application's captured EventSource, forced its error, observed a second real `/api/uar/sync/stream` request/open, delivered one replacement update, and rendered that recovered row once. | Before the source-package reactivity repair, two exact browser attempts failed at the first visible update: expected the card text `initial`, but it remained `Embedded SSE fixture 0 documents`. The final scenario also fails if no second stream opens, the page reloads, `replayRuntime` is called, or the recovered card count differs from one. | `CI=1 pnpm exec playwright test -c tests/bdd/playwright.config.ts tests/bdd/.features-gen/features/local-first-resilience.feature.spec.js --grep 'embedded SSE connection'` | Exit 0 against submodule `0352c83d7b386db56ffea8304ffdf3e2edb00fc8`: 1 test passed in 3.8 seconds; 1 passed in 14.9 seconds total. |
| Frontend build completes at child Tier 2. | The production frontend bundle compiled successfully. | Not a fail-closed assertion. | `pnpm build` | Exit 0: Vite built 8,094 modules and completed successfully. |
| The full frontend test command is recorded truthfully. | The command completed and exposed known failures outside this child rather than being represented as a pass. | Not a fail-closed assertion. | `pnpm test` | Exit 1: 69 files passed, 2 failed; 328 tests passed, 10 failed. All 10 failures were existing Storybook/A2UI Zod-validation cases under `packages/a2ui-uar`; this child does not claim the full suite passed. |
| The source candidate is bound to the recorded receipts. | Final child source hashes are recorded below; the UAR submodule resolves to the reviewed upstream head. | A different source or submodule pin will fail the hash/pin replay. | `shasum -a 256 frontend/src/entities/sync.ts frontend/src/entities/sync.test.ts tests/bdd/features/local-first-resilience.feature tests/bdd/steps/local-first-resilience.steps.ts package.json`; `git -C frontend/packages/prometheus-entity-management rev-parse HEAD` | Exit 0. Hashes and pin match the values below. |
| The OpenSpec change is strict-valid. | Proposal, design, completed implementation/evidence tasks, delta, receipts, and limits validate together. | Not a fail-closed assertion. | `openspec validate fix-embedded-sse-offline-reconnect --strict --no-interactive` | Exit 0: `Change 'fix-embedded-sse-offline-reconnect' is valid`. |
| The artifact-refiner gate converges on truthful progressive evidence. | Five blocking constraints, five progressive iteration-3 checkpoints, one manifest reference, registry identity, and 13 active/history files match; fresh critic and judge both returned PASS. | The first schema-valid checkpoint set was rejected because all five snapshots already claimed every phase complete. Those files were removed and the correction cycle was checkpointed progressively. | Commands and actual output are retained in `evidence/artifact-refiner-validation.md`. | Exit 0: 3 primary schemas, 5 checkpoint schemas, 5 constraint IDs, 5 progressive phase snapshots, convergence, registry, references, and active/history byte identity passed. |

## Final source receipts

- `frontend/src/entities/sync.ts`: `6b459d814c4741f329e037e62ec6394499f5bcc767f932d6bdc874534964e2f7`
- `frontend/src/entities/sync.test.ts`: `819075f331f2d3158fff9bbff9817eabcfa7ac24a736485c1c365bd1d5c992b5`
- `tests/bdd/features/local-first-resilience.feature`: `8a5703ea4be9a014aba9bf9fd3ec9b04d86f8d0ac97caa6f3e3670106c1ea7f8`
- `tests/bdd/steps/local-first-resilience.steps.ts`: `ab673e4df357706bff5374daa1030dfb123d8a6fd815f7058b55a54aa93196a9`
- `package.json`: `b612aef90c6d8d08c5d768bf08c06683d40c127b0d591c957fc6d105ad470184`
- `frontend/packages/prometheus-entity-management`: `0352c83d7b386db56ffea8304ffdf3e2edb00fc8`

The browser proof is deliberately narrower than durable offline replay. It
proves named-event delivery, one controlled replacement connection, and visible
post-reconnect projection. It does not prove that events emitted while the
client was disconnected are recovered.
