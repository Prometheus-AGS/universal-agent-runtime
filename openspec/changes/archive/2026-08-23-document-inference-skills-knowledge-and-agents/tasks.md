## 1. Establish the product-workflow authority contract

- [x] 1.1 Add `docs/publication/product-workflows.json` with exactly the seven planned guides, stable routes, positions, profiles, classified records, existing current authorities, required concept markers, and diagram requirements; verify the manifest parses, matches the fixed guide order, and every referenced source path exists.
- [x] 1.2 Add the bounded product-workflow validator and isolated control script, register their local package commands, and compose the validator into the final publication entrypoint; syntax-check the scripts but defer executing all controls until the seven guides and compatibility page are code-complete.

## 2. Document provider, model, and genuine-inference workflows

- [x] 2.1 Add `providers/configuration` with catalog-versus-configured distinctions, prerequisites, packaged UI and API steps, credential/local-provider boundaries, observable save/health outcomes, realtime-versus-reloadable state, and profile limits; verify its manifest markers and source provenance are present.
- [x] 2.2 Add `providers/models` with default and explicit `provider/model` addressing, catalog capability and availability distinctions, model selection through UI/API, failure boundaries, realtime-versus-reloadable state, and profile limits; verify it does not turn catalog breadth into an execution-support claim.
- [x] 2.3 Add `providers/inference` with packaged API and UI paths, genuine-model evidence criteria, illustrative-response labeling, observable success/failure, and a sanitized bounded synthesis of retained `server-full` observations; verify every observation names its provider, model, packaged boundary, source SHA, and non-transfer limit without exposing raw private evidence.

## 3. Document agents and skills

- [x] 3.1 Add `agents/overview` with create, configure, select, run, and inspect workflows for the packaged server UI/API plus the separate embedded-host boundary; verify effective model/context inputs, runtime-state visibility, authority boundaries, and profile limits are explicit.
- [x] 3.2 Add `skills/overview` with built-in, configuration-provisioned, and API-created provenance; conversation, agent, and global precedence; live-next-request and restart behavior; installation/authoring links; and tombstone/restore safety; verify reconciliation never hard-deletes and never tombstones built-in or API-created skills.
- [x] 3.3 Replace `website/docs/skills.md` with a concise compatibility landing page that points to `skills/overview` and the separate installation and authoring authorities; verify `/docs/skills` remains present without duplicating the lifecycle contract.

## 4. Document knowledge and memory

- [x] 4.1 Add `knowledge/overview` covering knowledge-base creation, document ingestion and processing, retrieval, attachment to execution, citation/context visibility, bounded genuine-model use, durable authority, and profile limits; verify the guide identifies each boundary in the retained knowledge-backed workflow without copying private evidence.
- [x] 4.2 Add `memory/overview` covering opt-in initialization, capture, recall/context injection, mutation, admin/API and memory-MCP surfaces, storage authority, and profile limits; verify it distinguishes memory records from knowledge resources, selected model context, live events, and process-local history.
- [x] 4.3 Add local category metadata and cross-links so all seven guides are reachable in dependency order and every local link resolves; verify shared global navigation, branding, and `docs/publication/routes.json` remain unchanged.

## 5. Reconcile truth and publication safety

- [x] 5.1 Audit every present-tense workflow statement against current implementation and canonical OpenSpec, qualify conflicts with older narrative material, and verify `versions.toml` is not cited as inspected authority while no planned behavior is described as delivered.
- [x] 5.2 Audit all public guide and compatibility-page content for raw `.prometheus` or KBD material, machine-local paths, credentials, private keys, event/session payloads, and exact private-source copies; verify retained observations are reviewed synthesis with profile, checkout, date, and evidence limits.

## 6. Verify only after product-workflow code and content are complete

- [x] 6.1 Run the isolated product-workflow controls and observe missing-guide, absent/unclassified-authority, missing packaged-surface, missing profile/state limit, synthetic-as-genuine inference, missing skill safety, knowledge/memory conflation, missing diagram explanation, and unsafe private-excerpt controls fail before the complete source fixture passes.
- [x] 6.2 Run the Docusaurus TypeScript check and bounded architecture, brand, product-workflow, and composed publication control suites locally, recording commands and observed output without running the phase-level production build or browser/accessibility certification.
- [x] 6.3 Run `openspec validate document-inference-skills-knowledge-and-agents --strict` and the artifact-refiner content gate; correct findings until the OpenSpec bundle, refiner schemas, referenced evidence, and finalized active/history state pass.
- [x] 6.4 Audit tracked and untracked diffs for runtime, React application, provider/model behavior, realtime behavior, dependency, vendored, shared route/navigation, README, non-documentation lockfile, raw `.prometheus`, and deployment-workflow changes; remove anything outside this change's documentation/process surface.
- [x] 6.5 Write row-form `verification.md` with requirement, command, observed result, limit, source SHA, and documentation profile, explicitly deferring the full build, visual, accessibility, deployment, runtime, fresh inference, and cross-profile claims.
- [x] 6.6 Transition the registered KBD change through the canonical runtime, refresh the human handoff, verify the exact next command names `document-security-tenancy-governance-and-operations`, and commit the complete change independently without pushing the partial phase.
