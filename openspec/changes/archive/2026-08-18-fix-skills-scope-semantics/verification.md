# Verification — `fix-skills-scope-semantics`

Date: 2026-08-18
Profile scope: `server-full` only. These results transfer to no other profile.

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| Persisted session agent-config constrains the next run before overlay injection and activation emission. | The selected run emitted `SkillActivated` and included `SESSION_POLICY_OVERLAY_MARKER`; after the persisted conversation policy selected another skill, the next run emitted neither for the excluded skill. | Removing the run-manager policy filter made the excluded run emit `SkillActivated`; the exact test exited 101. | `cargo test --quiet --locked -p universal-agent-runtime --no-default-features --features server-full --test skill_scoped_governance persisted_session_skill_selection_gates_overlay_and_activation -- --exact --test-threads=1` | Restored candidate: `1 passed; 0 failed; 2 filtered out`. Inversion: `0 passed; 1 failed; 2 filtered out`, exit 101. Literal tails and hashes are in `evidence/negative-controls.md` and `evidence/positive-verification.md`. |
| Built-in and installed-pack skills are immutable but remain disableable. | The service and HTTP tests refused a built-in edit, left its title unchanged, then disabled it through the existing toggle path. The built-in loader assigns `SkillOrigin::Builtin` to installed-pack skills. | Removing the service origin guard made the HTTP update return 200 and mutate the title; the exact test exited 101. | Service and HTTP exact commands are recorded in `evidence/positive-verification.md`; the inversion is recorded in `evidence/negative-controls.md`. | Service: `1 passed; 0 failed; 601 filtered out`. HTTP: `1 passed; 0 failed; 26 filtered out`. Inversion: expected 409 but received 200, exit 101. |
| The admin UI exposes disable while withholding edit and delete for built-ins. | Existing source computes `isBuiltin` from the serialized origin, disables Edit and Delete, and leaves the toggle bound to `handleToggle`. | This is presentation of the API-enforced boundary, not a new fail-closed guard. | `rg -n "const isBuiltin\|disabled=\\{isBuiltin\\}\|handleToggle" frontend/src/features/skills/ui/skills-page.tsx` | Observed `isBuiltin` at line 245, disabled edit/delete at 287/299, and the active toggle at 310. No UI file was changed. |
| Durable agent/conversation precedence, cold restart, origin serialization, and delete behavior remain delivered. | The archived `skill-scoped-governance` receipt records process-reopen persistence, global/agent/conversation precedence, live-effect stability, origin serialization, built-in delete refusal, and user deletion durability. | Its two fail-closed inversions exited 101 and are retained with literal output in the archived evidence. | Commands and results are retained in `openspec/changes/archive/2026-08-15-skill-scoped-governance/verification.md`. | Historical current-code foundation: scoped service `7 passed; 0 failed`; cold restart and live-effect exact tests each `1 passed; 0 failed`, with their inversions failing. |
| M4 does not expand this release-blocking reconciliation. | The observed H2/H3/O1 failures are closed without changing `SkillMatchingAlgorithm::Llm`; the assessment names its keyword fallback as a separate medium item. | Not a fail-closed assertion. | Source and assessment review. | Explicitly deferred as unrelated to the H2/H3/O1 acceptance boundary; no matching algorithm behavior was changed. |
| Change-level verification gates pass. | Formatting, Tier 0, scoped Clippy, strict OpenSpec, and scoped diff checks exited 0 on the restored candidate. | Not a fail-closed assertion. | Exact commands and output tails are in `evidence/positive-verification.md`. | Check exited 0 with three known warnings. Clippy exited 0 with 572 warnings; no warning-free claim is made. OpenSpec reported the change valid. Phase-level Tier 2 remains deferred. |
