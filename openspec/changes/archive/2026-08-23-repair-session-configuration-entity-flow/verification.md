# Verification — `repair-session-configuration-entity-flow`

Date: 2026-08-23

Source baseline: `4b7ea2142cf12e10c4c8a9f55d837952c7c57618`

Profile scope: local UAR React frontend source and UAR `server-full` static
compilation only. Installed-service, browser, responsive-style, persistence,
provider-routing, and inference behavior are deliberately deferred to
`prevent-session-configuration-regressions`, after all UAR phase code is
complete. These results transfer to no deployment or other runtime profile.

| requirement | assertion observed | negative control observed | command and observed output | source SHA | profile | limit |
|---|---|---|---|---|---|---|
| Use one Entity Management 3.0.2 application authority. | The preceding dependency commit pins `@prometheus-ags/prometheus-entity-management` and `@prometheus-ags/entity-graph-core` exactly to registry release `3.0.2`. This change exposes graph operations and the four session-configuration entity contracts through `platform/entities`; production architecture scanning reported zero violations. | The boundary negative-fixture runner rejected all 10 prohibited import/ownership patterns. | `node scripts/check-frontend-boundaries.mjs` → `Frontend boundary gate passed (0 production violations).`; `node scripts/test-frontend-boundaries-negative.mjs` → `Frontend boundary negative fixtures passed (10 rules rejected).` | `4b7ea2142cf12e10c4c8a9f55d837952c7c57618` + working-tree change | React frontend source | The current gate allows package imports anywhere beneath the platform facade. A dedicated session-configuration ownership rule remains change 3 work. |
| Register the session-configuration entity domain once. | Boot code calls the idempotent registration function after schema registration. The registration source declares remote `ConfiguredProvider`, `ConfiguredModel`, and `AgentSession` transports plus local `AgentSessionDraft`; typecheck accepts every transport and entity contract. | No runtime registration negative control ran in this static-only change. | `pnpm_config_minimum_release_age=0 pnpm typecheck` → exit 0; `tsc -b` completed without diagnostics. | same | React frontend source | Boot introspection and duplicate-registration behavior are not claimed until the bounded functional gate. |
| Load a bounded configured-model projection. | `ModelSelector` now consumes `useConfiguredModels`. Its selected source path contains no `/api/models`, catalog-load, or `ModelsStore` reference. The registered transport projects enabled configured models and synthesizes a provider's independently routable configured default only when that ID is absent from the raw configured model list; a present disabled model is not revived. Configured providers absent from the static catalog remain eligible. A saved route absent from the projection is labeled `Unavailable` and remains clearable. | No browser request capture or 7,248-row publication control ran yet. | `rg -n "api/models|loadCatalog|ModelsStore" frontend/src/features/models/model/use-model-selector.ts frontend/src/features/models/ui/model-selector.tsx frontend/src/features/chat/session-config-panel.tsx frontend/src/platform/entities/session-configuration` → no matches; typecheck and lint exit 0. | same | React frontend source | No request-count, publication-count, two-second-interactivity, or installed-browser claim. Those are final change-3 functional assertions. |
| Keep canonical session state distinct from editor drafts. | Canonical `AgentSession` and local `AgentSessionDraft` have separate entity types and keys. Per-session mutations and loads share one queue, saves fetch the latest server state and merge only dirty fields, already-started loads discard stale responses by graph revision, and draft generations plus abort controllers prevent an old save from deleting or resurrecting a reopened/canceled draft. Model and approval subscriptions live in separate control components; the sheet shell subscribes only to save status/error. Agent selection updates canonical state synchronously, persists through the same queue, and uses generation-checked rollback. The old feature-owned hook, store, service, and store test were deleted. | A source search for the retired owners returned no matches. History-blind review rejected the initial unversioned whole-config flow and then the load-after-optimistic-selection hole; the corrected source serializes the load behind pending persistence. No render-count or concurrent-editor runtime control ran. | `rg -n "useChatSessionConfig|chat-session-config-store|session-config-api" frontend/src` → no matches; `pnpm_config_minimum_release_age=0 pnpm typecheck` and `pnpm_config_minimum_release_age=0 pnpm lint` → exit 0. | same | React frontend source | Static ownership, sequencing, and type evidence only; subscriber isolation and concurrent editor behavior remain functionally unobserved. Server-side cross-tab CAS is not introduced or claimed. |
| Use the backend's typed session contract and remove ignored controls. | The transport decoder and POST body use only `agent_id`, `model`, `tools`, `skills`, `knowledge_bases`, `mcp_servers`, and `tool_approval`. The panel retains only model override and tool approval. The removed history, memory, capture, and scope controls are accounted for in `control-inventory.md`. | A source search across the panel and session platform returned no `model_override`, `context_strategy`, `history_window`, `inject_memory`, `auto_capture`, or `memory_scope` key. | The negative source search above → no matches; `rg -n "agent_id|model:|tools:|skills:|knowledge_bases:|mcp_servers:|tool_approval:" .../contracts.ts .../session-configuration-api.ts` → every typed wire field present. | same | React frontend source | Load/save/reload behavior and malformed-response rejection were not dynamically exercised here. |
| Make saved session policy participate in final routing. | Chat resolution now resolves the session identity and effective Global→Agent→Conversation→Turn policy before final provider/model selection; a request model is validated into Turn policy and remains highest precedence. The frontend sends the synchronously updated canonical session agent, displays a saved session model as the effective label, and no longer copies the agent default model into every turn. | No real-inference routing negative control ran in this change. The first history-blind review rejected fire-and-forget selection because an immediate turn could send the prior agent; selection now changes canonical state before its first await. On failure, only the `agent_id` is restored from the last server-confirmed agent ID when the selection generation is still current, preserving unrelated queued field updates and avoiding rollback to an earlier optimistic choice. | `cargo check --locked --no-default-features --features server-full` → exit 0 in 10.41s after the warm build; `pnpm_config_minimum_release_age=0 pnpm typecheck` → exit 0. | same | `server-full` static compilation plus React source | No saved-model persistence or genuine inference route is claimed until change 3. Cargo reported three unrelated pre-existing warnings in `fetch_guard.rs` and `wasm_runtime.rs`; this change makes no warning-free claim because those files are outside this change's permitted surface. |
| Apply responsive sheet interior spacing. | The sheet is full-width below the small breakpoint, capped at 400px above it, scrollable, and its body uses `gap-6 px-4 pb-4`; the production frontend remains type- and lint-clean. | No computed-style or overlap negative control ran in this static-only change. | `rg -n "max-w|px-4|pb-4|gap-6" frontend/src/features/chat/session-config-panel.tsx` → the responsive width and body spacing declarations; `pnpm_config_minimum_release_age=0 pnpm lint` → exit 0. | same | React frontend source | No 320/768/1024/1440 computed-style or interaction claim. Those widths remain final browser evidence. |
| Preserve the repository dependency cooldown while using the operator-required new release. | No repository supply-chain policy changed. After the already-reviewed version and registry integrity were established, a command-local `pnpm_config_minimum_release_age=0` override allowed TypeScript and ESLint to run, both exiting 0. | Normal `pnpm typecheck` and `pnpm lint` each exited 1 before product execution with `ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION` for the two 3.0.2 packages published on 2026-08-23. | Normal commands → expected policy rejection; command-local override commands → exit 0 with `tsc -b` and `eslint .`. | same | Local dependency policy and React Tier 0 | The override is process-scoped evidence only. A normal install becomes eligible after the configured 24-hour release age. |
| Keep the OpenSpec change and scoped diff structurally valid. | Strict OpenSpec validation and the scoped whitespace/error check both passed. | Not a fail-closed product assertion; no negative control required. | `openspec validate repair-session-configuration-entity-flow --strict` → `Change 'repair-session-configuration-entity-flow' is valid`; `git diff --check -- frontend/src src/server.rs openspec/changes/repair-session-configuration-entity-flow` → exit 0 with no output. | same | OpenSpec/process | Structural validity does not imply functional UI or runtime correctness. |
| Keep canonical task evidence truthful under the operator's testing rule. | Plan revision 28 supersedes the immutable canonical task-title clauses that named boot, render, computed-style, emitted-request, or genuine-routing observation. The checked implementation tasks now close on source plus Tier 0; change 3 owns the one post-code-completion functional proof. | The first history-blind review found the original task mirror contradicted this verification file; the canonical revision records the correction instead of manually editing generated projections. | `prometheus kbd revise --reason '<recorded deferral>' --exact-next-work '/kbd-execute fix-broken-session-configuration-ui'` → revision 512, plan revision 28. | same | KBD process | Canonical task titles are immutable historical labels; the later plan revision is authoritative for their evidence boundary. |
| Apply the available QA and adversarial gates. | The repository-approved direct artifact-refiner substitute passed every named static gate. After multiple blocking review rounds and corresponding source corrections, the final history-blind critic returned PASS and the independent judge returned APPROVE. | The initial critic/judge rejections are retained in `adversarial-review.md`; they demonstrate that the gate rejected the earlier lossy/racy implementation instead of always passing. | Commands and observed output are summarized in `qa-validation.md`; final reviewer verdicts are retained in `adversarial-review.md`. | same | Local source/process QA | The artifact-refiner PMPO runtime remains unavailable; no claim is made that `/refine-validate` executed. Review is not functional runtime evidence. |

## Deferred functional evidence

- The installed release at `http://localhost:1906` has not been rebuilt or
  opened for this change.
- Browser responsiveness, network request bounds, graph publication bounds,
  field-subscriber isolation, save/reload/cancel behavior, unavailable-model
  presentation, computed spacing at four widths, and genuine inference routing
  remain explicitly unverified.
- Those assertions belong to the single short post-code-completion HTTP/browser
  sequence in `prevent-session-configuration-regressions`; no unit campaign,
  broad suite, soak, or GitHub Actions job was run.
