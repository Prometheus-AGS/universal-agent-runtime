# Verification — `package-native-service-installers`

Source baseline: `d0d319b32aa829e764645cf9764779c7c14a1994` plus the pending change-3 worktree.

This change provides packaging source and static validation only. Installation and runtime behavior remain deferred until all implementation changes are code-complete.

| requirement | assertion observed | negative control observed | command and observed output | source SHA | profile / platform | limit |
|---|---|---|---|---|---|---|
| macOS package declares the user LaunchAgent and required paths. | The rendered template names `com.prometheus.universal-agent-runtime`, port 1906, loopback host, gRPC 50051, `~/.uar` program/config/env paths, restart-after-failure semantics, and stdout/stderr/operational files below `~/.prometheus/logs/universal-agent-runtime/`. All macOS shell entrypoints parse. | N/A — this is static package validation, not a fail-closed runtime assertion. | `bash -n packaging/native/macos/*.sh` → PASS for five scripts; `plutil -lint packaging/native/macos/com.prometheus.universal-agent-runtime.plist.in` → `OK`; retained at `.prometheus/logs/uar-native-service-deployment/change3-local-structure-checks-r2.txt`. | `d0d319b32aa829e764645cf9764779c7c14a1994` + change-3 worktree | macOS package source. | LaunchAgent installation and runtime are not claimed here. |
| Linux package declares direct systemd execution and state boundaries. | Ubuntu 24.04 `systemd-analyze verify` accepted the unit after fixture files were placed at its declared absolute paths. The unit uses direct `ExecStart`, `/etc/uar`, `/var/lib/uar`, SIGTERM, `Restart=on-failure`, and `.prometheus` file outputs. ShellCheck and Bash parsing exited 0. | The first verifier run, without the declared executable fixture, exited 1 with `Command /usr/local/lib/uar/universal-agent-runtime is not executable: No such file or directory`, retained at `.prometheus/logs/uar-native-service-deployment/change3-systemd-verify-negative-control.txt`. The same unit passed once the expected installation paths existed. | Local Docker Ubuntu 24.04 with `systemd-analyze verify /work/uar.service` → exit 0; retained at `.prometheus/logs/uar-native-service-deployment/change3-systemd-verify.txt`. `shellcheck packaging/native/macos/*.sh packaging/native/linux/*.sh` → exit 0; retained at `change3-shellcheck.txt`. | `d0d319b32aa829e764645cf9764779c7c14a1994` + change-3 worktree | Linux systemd template validated from macOS. | No Linux installation or service-runtime claim. |
| Windows package uses native SCM and platform filesystem boundaries. | PowerShell parsing passed for install, upgrade, control, credential refresh, and uninstall scripts. The installer uses Program Files for binary/static files, ProgramData for config/state/`.prometheus` logs, native `New-Service`/`sc.exe`, LocalService identity, and restart-on-failure actions; `.cmd` wrappers invoke each entrypoint. | N/A — parser validation is not a Windows SCM runtime control. | PowerShell AST parser over `packaging/native/windows/*.ps1` → five PASS lines; retained at `.prometheus/logs/uar-native-service-deployment/change3-local-structure-checks-r2.txt`. | `d0d319b32aa829e764645cf9764779c7c14a1994` + change-3 worktree | Windows package source parsed on macOS. | No Windows installation, SCM execution, ACL-effect, or restart claim. |
| Install, upgrade, uninstall, control, and credential refresh are reversible and self-contained. | Every platform has independently runnable lifecycle entrypoints. Existing config is backed up and preserved; uninstall removes supervisor/program assets while leaving configuration, environment, database state, backups, and logs. Credential refresh accepts a complete operator-supplied file and replaces it atomically without printing values. | N/A — static source inspection; the installed macOS preservation control remains required in change 5. | `rg --files packaging/native | sort` → 21 platform files; `rg -n 'generate-provider-env|merge-provider-config' packaging/native` → no matches and `future-helper reference check PASS`. | `d0d319b32aa829e764645cf9764779c7c14a1994` + change-3 worktree | All native package sources. | Does not prove filesystem effects; no Linux or Windows runtime transfer. |
| OpenSpec change remains structurally valid. | Strict validation reported the change valid after packaging completion. | N/A — schema validation is not a fail-closed product assertion. | `openspec validate package-native-service-installers --strict` → `Change 'package-native-service-installers' is valid`. | `d0d319b32aa829e764645cf9764779c7c14a1994` + change-3 worktree | OpenSpec contract only. | Does not prove installer behavior. |
