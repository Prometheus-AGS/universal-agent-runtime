# Verification: `fix-provider-default-settings-consistency`

Scope: `server-full` on macOS with temporary embedded SurrealKV databases. Results transfer to no other feature profile, persistence backend, or platform.

| Requirement | Evidence command | Observed result | Limit |
|---|---|---|---|
| Supported `local` memory provider initializes settings | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test settings_persistence mgr_initialize_accepts_local_memory_embedding_provider -- --exact --nocapture` | Before the schema edit: exit 101, `0 passed; 1 failed`; initialization failed while seeding `memory.embedding_provider` because `"local" is not one of "openai" or "cohere"`. After the edit: exit 0, `1 passed; 0 failed`. The completion group `... --test settings_persistence mgr_initialize_ -- --nocapture` also exited 0 with `2 passed; 0 failed`. | Proves settings bootstrap and later `llm.default_provider` seeding; it does not exercise embedding generation. |
| Unknown memory provider remains invalid | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test settings_persistence mgr_initialize_rejects_unknown_memory_embedding_provider -- --exact --nocapture` | Exit 0, `1 passed; 0 failed`. The test required initialization to return an error whose chain names `memory.embedding_provider`, `unsupported-provider`, and `JSON Schema validation`. | One deliberately unsupported identifier; the closed enum is inspected separately in the scoped diff. |
| Missing default provider changes no state | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::api::providers::tests::set_default_missing_provider_preserves_defaults -- --exact --nocapture` | Exit 0, `1 passed; 0 failed`. The handler returned 404 and both the pre-request live default and absent durable default were unchanged. | Direct private-handler test; HTTP router serialization is unchanged and not separately exercised. |
| Failed persistence changes no live state | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::api::providers::tests::set_default_persistence_failure_preserves_live_default -- --exact --nocapture` | Before handler reordering: exit 101, `0 passed; 1 failed`; assertion showed live default `Some("provider-b")` instead of prior `Some("provider-a")`. After reordering: exit 0, `1 passed; 0 failed`; the handler returned 500, durable state remained absent, and live default remained `provider-a`. | Persistence failure is induced with an uninitialized settings manager, matching the observed partial-bootstrap failure; arbitrary storage outages are not simulated. |
| Successful default selection survives reconstruction | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::api::providers::tests::set_default_success_survives_fresh_manager -- --exact --nocapture` | Exit 0, `1 passed; 0 failed`. The handler returned 200, live default became `provider-b`, and a fresh initialized `SettingsManager` over the same SurrealKV provider read `provider-b`. | Fresh manager and cache reconstruction over one open persistence provider; this is not a process-restart claim. |
| Focused provider behavior remains coherent | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::api::providers::tests::set_default_ -- --nocapture` | Exit 0, `3 passed; 0 failed`. | Only the three default-selection controls introduced by this change. |
| Per-edit Tier 0 chronology | `cargo check --locked -p universal-agent-runtime --no-default-features --features server-full` after each source/test edit | Retained execution transcript: 2026-08-19 06:53:18Z after the settings tests, exit 0, finished in 1.01s with 3 existing warnings; 06:54:28Z after the schema enum edit, exit 0, finished in 9.59s with the same 3 warnings; 06:58:58Z after the provider tests and their fixture correction, exit 0, finished in 7.42s with the same 3 warnings; 07:01:22Z after the handler ordering edit, exit 0, finished in 8.92s with the same 3 warnings. | Chronological command receipts are retained in the Codex session transcript; this row summarizes their actual outputs rather than claiming a single final check covered the edit sequence. |
| Tier 0 compilation | `cargo check --locked -p universal-agent-runtime --no-default-features --features server-full` | Exit 0. The final run completed in 7.18s with three existing warnings: unused `MAX_BODY_BYTES`, unused `MAX_REDIRECTS`, and missing `Debug` on `WasmHostState`. | Compile evidence only for `server-full`. |
| Package Clippy | `cargo clippy --locked -p universal-agent-runtime --no-default-features --features server-full --lib --no-deps` | Exit 0; `universal-agent-runtime` emitted 571 warnings from the existing pedantic baseline. | Not a warning-free result; no adjacent warning cleanup was attempted. |
| Formatting | `cargo fmt --all -- --check` | Final run exited 0 with no output. | Rust formatting only. |
| Scoped diff integrity | `git diff --check -- src/uar/settings/manager.rs src/uar/api/providers.rs tests/settings_persistence.rs openspec/changes/fix-provider-default-settings-consistency .kbd-orchestrator/phases/perform-the-soak-run-candidate-tag-external-installs-and-ga-promotion/children/fix-provider-default-settings-consistency` | Exit 0 with no output. | Tracked scoped diff at the time of the run; final staged-candidate integrity is checked again before commit. |
| OpenSpec contract | `openspec validate fix-provider-default-settings-consistency --strict --no-interactive` | Exit 0: `Change 'fix-provider-default-settings-consistency' is valid`. | Live change validation before archive. |
| Retained configure/route and secret-retrieval scenarios | No child command. | Not rerun: this child changes neither routed inference nor secret retrieval. | These retained scenarios remain covered by their prior capability evidence and the parent screen certification; this child makes no new claim for them. |

Phase-level browser certification, release builds, and release-level checks were not run. They remain reserved for the resumed parent and outer phase completion points.

The existing no-settings-manager registry-only path was not changed or separately tested. Durable-ordering results in this receipt apply only when settings persistence is configured.
