# Verification — `document-and-deploy-native-services`

Source baseline: `b0b41f83b3fde4d31482036082ab58b26ad1422d` plus the pending change-5 worktree.

Results are requirement-scoped. They do not form an aggregate readiness claim and do not transfer between profiles or platforms.

| requirement | assertion observed | negative control observed | command and observed output | source SHA | profile / platform | limit |
|---|---|---|---|---|---|---|
| Native deployment documentation is complete and buildable. | Root README, product deployment/configuration/provider docs, and branded macOS/Linux/Windows Docusaurus pages cover install, upgrade, uninstall, credential refresh, precedence, paths, controls, logs, exposure, troubleshooting, and platform limits. The optimized site built successfully. | N/A — documentation production build, not a fail-closed runtime assertion. | `npm --prefix website run build` → `[SUCCESS] Generated static files in "build"`; native pages: `index.md`, `macos.md`, `linux.md`, `windows.md`. | baseline + change-5 worktree | Docusaurus production build on macOS. | Local build only; no Pages publication was requested or performed. |
| The final server-full release builds with the pinned catalogs. | The exact release command compiled UAR 1.0.0 and `liter-llm` 1.18.1 after the `models.dev` and `liter-llm` gitlink advances. | N/A — compile assertion. | `cargo build --locked --release --no-default-features --features server-full` → exit 0, `Finished release profile [optimized] ... in 10m 45s`; `.prometheus/logs/uar-native-service-deployment/cargo-release-liter-llm-1.18.1.txt`. | baseline + change-5 worktree | `server-full`; macOS aarch64 release build. | Existing three compiler warnings and nix/redis future-incompatibility notices remain outside scope. |
| The installed macOS release serves the product on port 1906. | LaunchAgent state was `running`; version was `universal-agent-runtime 1.0.0`; `/healthz` returned `ok`; `/readyz` returned `ready`; `/`, `/admin/providers`, `/admin/models`, and emitted asset `/assets/index-eVk_LYNB.js` returned 200. | N/A — installed service availability assertion. | `launchctl print gui/$(id -u)/com.prometheus.universal-agent-runtime`; installed binary `--version`; bounded `curl` requests → the outputs above. | baseline + change-5 worktree | `server-full`; observed macOS user LaunchAgent runtime. | Short observation only; no soak or availability-duration claim. |
| Native listeners are local-machine-only. | HTTP had non-empty LISTEN rows only for `127.0.0.1:1906` and `[::1]:1906`; A2A gRPC had a non-empty row only for `127.0.0.1:50051`. | The verifier exited 1 for an unused port with zero rows and exited 1 for a synthetic wildcard listener; `.prometheus/logs/uar-native-service-deployment/listener-negative-controls.txt`. | `lsof -nP -iTCP:1906 -sTCP:LISTEN` and the equivalent for 50051 → all observed addresses loopback. | baseline + change-5 worktree | `server-full`; macOS runtime. | Socket observation at one point in time. No Linux/Windows listener claim. |
| Native configuration preserves operator authority and migrates only the observed obsolete Alibaba values. | Existing YAML, remote SurrealDB authority, selected/provider settings, and backups remained present. Exact values migrated to `alibaba/qwen3.8-max` and `DASHSCOPE_API_KEY`; Unix and PowerShell outputs matched. | A custom Alibaba fixture remained byte-identical in both Unix and PowerShell controls; non-matching operator model/key/provider values were not broadly rewritten. | Corrective merge scripts → `selected_model=true`, `canonical_key_reference=true`, `provider_default=true`, `provider_model=true`, legacy values absent; backup files observed beneath `~/.prometheus/backups/uar/`. | baseline + change-5 worktree | Native bootstrap; Unix runtime and PowerShell 7 behavior on macOS. | PowerShell execution is script behavior only, not Windows service runtime. No secret value was printed or retained. |
| Qwen 3.8-Max enters through the compile-time catalog architecture. | Pinned `models.dev` `f97df19af` and `liter-llm` `788877f7a` contain the release. `models.dev` is pinned to the newest Qwen-containing revision before its upstream case-colliding Eden AI filenames. The reviewed 316-provider snapshot has SHA-256 `c4704316...ded1bb6`; `/api/models` and the shipped Models screen expose `alibaba/qwen3.8-max` with 1,000,000 context and 131,072 output. | A second `node scripts/refresh-provider-catalog.mjs` run produced the identical SHA-256, rejecting nondeterministic output. The earlier models.dev-only release did not expose Qwen 3.8, proving that the liter-llm-backed snapshot refresh was necessary. | Catalog refresh → 316 providers and identical before/after digest; API query → `configured=true`; BrowserOS visible text → `Qwen3.8-Max`, `alibaba/qwen3.8-max`; retained in `qwen38-catalog-runtime-visibility.txt`. | baseline + change-5 worktree | `server-full`; compile-time catalog plus installed macOS API/UI. | Visibility only. No Alibaba inference was requested or claimed. |
| Credential-conditional providers/models are visible without exposing credentials. | Generated environment names included DASHSCOPE, KIMI, MINIMAX, and ZAI; the configured-provider API showed Alibaba/Qwen, Kimi K3, MiniMax M3, Z.AI/GLM, and the local proxy. Moonshot was correctly absent because no canonical Moonshot credential was generated. | Credential generators reject a multiline value and preserve the prior file; output-name equality rejects aliases, tool-only keys, and undeclared names. | `/api/uar/providers`, `/api/models`, `/admin/providers`, and `/admin/models` observations; provider bootstrap controls in change-4 verification. | baseline + change-5 worktree | `server-full`; installed macOS API/UI. | Provider/model visibility only; credential values were not read into retained evidence. |
| Required installed-UAR genuine inference works through API and shipped UI. | API responses returned `NATIVE_LOCAL_OK` from `local-openai-proxy/gpt-5.4-mini`, `NATIVE_KIMI_OK` from `kimi-for-coding/k3`, and `NATIVE_MINIMAX_OK` from `minimax/MiniMax-M3`. Shipped UI responses returned `UI_LOCAL_OK`, `UI_KIMI_OK`, and `UI_MINIMAX_OK`, with the operational log resolving the same providers/models. | Synthetic, recorded, replayed, and mocked providers were excluded by contract; no such result was accepted as inference evidence. | Six total installed-boundary requests, retained as three API JSON files and `browser-functional-observations.txt`. | pre-Qwen release candidate from this change-5 worktree | `server-full`; installed macOS API and shipped React UI. | Exactly six genuine requests total; each bounded to 120 seconds and 64 output tokens. No Linux, Windows, other profile, or Qwen inference claim. |
| Restart preserves configuration, provider state, database access, graceful stop, and logs. | Before/after evidence preserved default provider `minimax` and the remote SurrealDB response `ws://127.0.0.1:28000`; one representative UI MiniMax inference passed after restart. Logs show HTTP drain, A2A stop, live-stream stops, async cleanup, worker-pool completion, and restart. Final Qwen refresh restart remained healthy without another inference request. | N/A — persistence comparison and lifecycle observation, not a fail-closed input assertion. | `pre-restart-state.txt`, `post-restart-state.txt`, `restart-operational-excerpt.txt`, and final health/readiness/API checks. | baseline + change-5 worktree | `server-full`; macOS LaunchAgent runtime. | One planned restart plus diagnostic/corrective restarts after observed Alibaba/catalog failures. No soak. |
| Explicit environment/log failures terminate before listeners open. | Explicitly missing env file, malformed env file, and unwritable `UAR_LOG_FILE` each exited nonzero and left both service ports unopened. | Each control used the corresponding valid installed binary/config path as the paired passing case; the installed LaunchAgent subsequently opened both expected loopback listeners. | `negative-startup-summary.txt` → `missing env exit=1; malformed env exit=1; unwritable log exit=1; no listeners opened`. | baseline + change-5 worktree | `server-full`; bounded macOS startup controls. | Three isolated startup failures only. |
| Linux systemd package is structurally valid. | Ubuntu 24.04 `systemd-analyze verify` passed after declared absolute-path fixtures were installed; Bash and ShellCheck passed. | Without the declared executable fixture, the verifier exited 1 with `Command /usr/local/lib/uar/universal-agent-runtime is not executable`. | Evidence in `change3-systemd-verify.txt`, `change3-systemd-verify-negative-control.txt`, and `change3-shellcheck.txt`. | `d0d319b32aa829e764645cf9764779c7c14a1994` plus later packaging integration | Linux template validation from macOS. | No Linux installation, process, listener, inference, or systemd runtime claim. |
| Windows native SCM package compiles and parses. | `server-full` cross-check compiled `windows-service` 0.8.1 and UAR for `x86_64-pc-windows-msvc`; all PowerShell lifecycle scripts parsed. Source trace maps SCM Stop/Shutdown to the process cancellation token accepted by `start_server_with_shutdown`. | The GNU-target attempt failed before UAR type-checking because pinned ORT has no GNU prebuilt; this retained failure prevented a false Windows compile claim and led to the documented MSVC target. | `change2-windows-msvc-check.txt` → exit 0; PowerShell AST parser → PASS; source inspection of `src/server/windows_service.rs` and `src/server.rs`. | `64cdcad6519a39b2575a5113b4edd0ab156a958f` plus final locked graph | Windows x86_64 compile/template validation from macOS. | No Windows SCM registration, service control, ACL, listener, or inference runtime claim. |
| All five OpenSpec contracts remain strict-valid. | Each named change reported valid after final Qwen/catalog corrections. | N/A — contract structure validation. | Five `openspec validate <change> --strict` commands → five `Change '<change>' is valid` lines. | baseline + change-5 worktree | OpenSpec v1.5 contract validation. | Does not add a runtime claim. |
