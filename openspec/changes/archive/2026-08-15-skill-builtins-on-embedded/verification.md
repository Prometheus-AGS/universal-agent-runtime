# Verification — `skill-builtins-on-embedded`

Date: 2026-08-14
Profile scope: `server-full` only. These results transfer to no other profile.

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| A fresh embedded database gains the complete built-in catalogue. | `embedded_runtime_seeds_persists_and_deduplicates_builtins` created a fresh temporary SurrealKV directory, started a child process, built an `EmbeddedRuntime`, and found every ID returned by `discover_builtin_skills()` exactly once in the live registry and persisted rows. | Removing the embedded registration block made the seed subprocess and parent assertion fail on a missing built-in. Literal output and source-restoration hashes are in `evidence/fail-closed-negative-control.md`. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test builtin_db_registration embedded_runtime_seeds_persists_and_deduplicates_builtins -- --exact --test-threads=1` | Positive exit 0: `1 passed; 0 failed`. Inverted exit 101: `0 passed; 1 failed`; seed child observed builtin count `0`, expected `1`. |
| Built-ins are durable across process exit. | After the seed process exited and released SurrealKV, a second process reopened the same on-disk database, found every built-in in persisted rows, then built `EmbeddedRuntime` with `seed_defaults(false)` and loaded every built-in exactly once into the registry. | The registration-removal control failed in the seed process, so no later process could obtain the required persisted catalogue. The load process disables seeding, preventing re-registration from masking a load failure. | Same exact subprocess-boundary seed/load/deduplicate command. | Exit 0; the separate load process reopened the database and passed direct-row and seeding-disabled registry assertions. |
| Re-registration does not duplicate built-ins. | After both prior processes exited, a third process reopened the same SurrealKV database with seeding enabled; every discovered built-in remained exactly once in the live registry and persisted rows. | The same branch inversion demonstrates that the three-process assertion depends on the embedded registration path; uniqueness is additionally enforced by the existing skill-ID upsert path, which this change did not alter. | Same exact subprocess-boundary seed/load/deduplicate command. | Exit 0; each expected registry count and database count equalled 1 after enabled re-registration. |
| The existing default-seeding switch controls built-in registration. | `embedded_runtime_honors_disabled_builtin_seeding` built a runtime with `seed_defaults(false)` and found none of the discovered built-in IDs in either the registry or SurrealKV rows. | Not a fail-closed assertion. The test observes the disabled branch directly. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test builtin_db_registration embedded_runtime_honors_disabled_builtin_seeding -- --exact --test-threads=1` | Exit 0: `1 passed; 0 failed`; `2 filtered out`. |
| GAP-05 documentation states the narrow, observed defect. | Only line 445 changed. It records both observed server registration sites (`454`, `517`) and replaces the disproven empty-registry/0% claim with the fresh-embedded-database claim. | Not a fail-closed assertion. | `rg -n "register_builtins" src/server.rs docs/SPECIFICATION.md`; `git diff --numstat -- docs/SPECIFICATION.md` | Observed server sites `454` and `517`; documentation diff `1` insertion and `1` deletion. |
| The B3 implementation passes its change-level compile and lint gates without a B3-owned warning. | Package check and package/library/no-deps Clippy both exited 0 after the final implementation. | Not a fail-closed assertion. | `cargo check --locked -p universal-agent-runtime --no-default-features --features server-full`; `cargo clippy --locked -p universal-agent-runtime --no-default-features --features server-full --lib --no-deps` | Check exit 0 with 3 recorded pre-existing warnings. Clippy exit 0 with the pre-existing workspace warning baseline; no warning points to B3-owned lines. |
| The OpenSpec delta remains strict-valid. | Strict validation passed against the implementation and proof ledger. | Not a fail-closed assertion. | `openspec validate skill-builtins-on-embedded --strict` | Exit 0: `Change 'skill-builtins-on-embedded' is valid`. |
| The artifact-refiner deterministic gate passes. | Manifest, constraint, and state schemas passed; the referenced receipt exists; all four blocking constraints and state consistency passed after the process-boundary correction. | Not a fail-closed assertion. | Exact replay command and literal output in `evidence/artifact-refiner-validation.md`. | Schema PASS; referenced file PASS; constraints 4/4 PASS; consistency PASS. Receipt: `.refiner/artifacts/skill-builtins-on-embedded/`. Finalization succeeded; the optional empty-trigger dispatcher then failed in its own JSON plumbing, with no configured trigger or required action omitted. |
| Independent adversarial review accepts the corrected B3 slice. | The history-free critic and judge initially rejected the live-provider restart fixture. After the three-process correction, the judge verified seed/load-with-seeding-disabled/deduplicate boundaries and the critic verified all four correction items. | Not a fail-closed assertion. | Independent artifact critic and judge re-review of the staged B3 deliverable | Corrected critic PASS; corrected judge PASS. No broad suite was run by either reviewer. |
| Phase-level Tier 2 remains deferred. | The pinned live capability suite was not run because B4 and B5 are not yet implemented. | Not a fail-closed assertion. | No Tier 2 command executed. | Correctly deferred by the execution contract. |
| B3 stays inside its permitted surface. | Behavioral edits are limited to embedded built-in bootstrap and focused tests; the only specification edit is the sanctioned GAP-05 line. Persistence behavior and scoped governance are unchanged. The operator-owned `.claude/settings.local.json` change is preserved but excluded from the staged B3 deliverable. | Not a fail-closed assertion. | `git diff --cached --name-only`; focused source inspection after staging the exact B3 deliverable. | No B3 stop condition fired; the staged path list contains no operator settings file. |
