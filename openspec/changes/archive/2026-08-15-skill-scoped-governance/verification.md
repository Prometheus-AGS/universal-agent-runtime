# Verification — `skill-scoped-governance`

Date: 2026-08-15
Profile scope: `server-full` only. These results transfer to no other profile.

Status: **READY TO COMMIT; PHASE TIER 2 CORRECTLY DEFERRED.**

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| Every skill has durable global, agent, and conversation state with conversation > agent > global precedence. | `scoped_state_resolves_conversation_then_agent_then_global_both_directions` exercised both enable-over-disable and disable-over-enable directions. `Skill::enabled` remains the compatibility global value because existing persisted rows and clients already read it; new global writes also create/update the explicit Global record so both representations stay synchronized. The real-run assertion proved a conversation enable widens a global disable. | The live-effect control forced every matching conversation record to enabled; the next run activated the disabled skill and failed. Literal output is in `evidence/live-effect-negative-control.md`. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::runtime::skills::service::tests -- --test-threads=1` | Actual output: `test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 573 filtered out; finished in 0.17s`. Full literal output is in `evidence/positive-verification.md`. |
| Scoped state survives a cold restart and builtin re-registration does not overwrite it. | `scoped_state_and_user_deletion_survive_cold_restart` ran separate seed, reopen-delete, and verify-deleted processes against one SurrealKV directory. It observed a global disable and a separate per-agent disable after both reopens. | Removing the stored-config merge made the reopen-delete child fail. Literal output and identical current-candidate SHA-256 values are in `evidence/fail-closed-negative-control.md`. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test skill_scoped_governance scoped_state_and_user_deletion_survive_cold_restart -- --exact --test-threads=1` | Actual positive output: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 1 filtered out; finished in 0.31s`. Actual inversion: exit 101, `global and per-agent disables survive a cold reopen`. |
| A scoped change affects the next matching pass without mutating an existing binding. | `conversation_enable_widens_global_disable_and_in_flight_binding_is_stable` used an embedded SurrealKV runtime and gated driver: it disabled the conversation after the first run bound the skill, released that run to completion with its activation and overlay intact, then observed no activation or overlay on the next run. | Forcing the single domain conversation-precedence branch to enabled made the second run activate the disabled skill and fail its exact assertion. Literal output and current-candidate restoration hashes are in `evidence/live-effect-negative-control.md`. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test skill_scoped_governance conversation_enable_widens_global_disable_and_in_flight_binding_is_stable -- --exact --test-threads=1` | Actual positive output: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 1 filtered out; finished in 0.29s`. Actual inversion: exit 101, second run emitted `SkillActivated`. |
| Origin is exposed and deletion follows origin. | The origin test serialized `builtin`. The cold-restart test refused builtin deletion, removed the API-created user skill from SurrealKV and the filesystem, and observed it still absent after another process reopen. | Not a new fail-closed guard; the builtin deletion guard pre-existed and was consumed rather than reimplemented. | Origin command and cold-restart command are pasted in `evidence/positive-verification.md`. | Actual origin output: `test result: ok. 1 passed; 0 failed; ... 578 filtered out`. Actual cold-restart output: `test result: ok. 1 passed; 0 failed; ... 1 filtered out`. |
| Scoped configuration round-trips through the filesystem provider format. | `scoped_config_round_trips_through_skill_markdown` serialized global, agent, and conversation records to SKILL.md and parsed the same values. | Not a fail-closed assertion. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::runtime::skills::storage::filesystem::tests::scoped_config_round_trips_through_skill_markdown -- --exact --test-threads=1` | Actual output: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 578 filtered out; finished in 0.00s`. |
| Existing agent-binding API and matching semantics are retained. | Both pre-existing replacement assertions pass. `agent_binding_set_before_load_filters_future_skills` observes that pending IDs affect later-loaded matching, non-selected later skills remain excluded, and a conversation override still wins. | This is a compatibility assertion, not a fail-closed guard. | API command plus the seven-test service command are pasted in `evidence/positive-verification.md`. | Actual API output: `2 passed; 0 failed`. Actual service output: `7 passed; 0 failed`, including the pending-binding matching assertion. |
| Tier 0 passes for the current B4 slice. | The package check and exact package/lib/no-deps Clippy command exited 0 after the final restored source. The short-format replay emitted existing diagnostics in files B4 touches, but none points at a line added by B4. | Not a fail-closed assertion. | `cargo check --locked -p universal-agent-runtime --no-default-features --features server-full`; `cargo clippy --locked -p universal-agent-runtime --no-default-features --features server-full --lib --no-deps` | Actual check output: `Finished dev profile ... in 8.98s`, exit 0 with three pre-existing warnings. Actual Clippy output: `573 warnings`, `Finished dev profile ... in 9.76s`, exit 0. |
| The required artifact-refiner gate passes after adversarial correction. | Iteration 3 retains both rejected earlier iterations, corrects their restart and matching-compatibility claims, and passes manifest, constraints, and state schemas; the referenced summary exists; four blocking constraints match the converged state. | Not a fail-closed assertion. | Exact replay and finalization commands are in `evidence/artifact-refiner-validation.md`. | Actual replay output: `manifest schema PASS`, `constraints schema PASS`, `state schema PASS`, `blocking constraints PASS: 4/4`, `state consistency PASS`; finalization exited 0 at `.refiner/history/skill-scoped-governance/2026-08-15_08-03-54Z`. |
| Independent artifact review accepts the corrected candidate. | The final history-free critic and judge each returned PASS after rejecting earlier iterations. | The rejected findings are retained rather than overwritten. | Review scope and final literal verdicts are in `evidence/adversarial-review.md`. | Critic: `PASS — no blocker found.` Judge: `PASS`. Neither ran a broad suite or edited files. |
| Phase-level Tier 2 remains deferred. | The pinned live capability suite was not run because B4 and B5 are not complete. | Not a fail-closed assertion. | No Tier 2 command executed. | Correctly deferred by the execution contract. |
