# Verification — `wire-orchestrator-delegation`

Date: 2026-08-18
Profile scope: `server-full` on macOS. The real-backend row used the operator's
local OpenAI-compatible proxy. These results transfer to no other profile,
platform, proxy, or model.

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| The orchestrator agent delegates to sub-agents. | The orchestrator descriptor is distinct; the run executed RouterNode then the selected local AgentNode, made two driver calls, emitted four step boundaries, and streamed `[rust-reviewer]` plus the specialist contribution. | `attached_graph_does_not_change_default_agent_path` observed one driver call and the unprefixed direct answer, proving a configured graph does not divert default-agent. `test_agent_node_local_empty_output_is_error` observed the graph reject a specialist stream with no text. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test test_chat_completion orchestrator_run_routes_and_streams_delegated_answer -- --exact`<br>`cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test test_chat_completion attached_graph_does_not_change_default_agent_path -- --exact`<br>`cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test test_agent_node test_agent_node_local_empty_output_is_error -- --exact` | Orchestrator: `1 passed; 0 failed; 8 filtered out`.<br>Default control: `1 passed; 0 failed; 8 filtered out`.<br>Empty-output control: `1 passed; 0 failed; 4 filtered out`. |
| Delegation works through the server with recorded and real backends. | The same HTTP/SSE scenario observed attribution, `runtime.step` start/finish events, and non-whitespace specialist content; recorded mode additionally required the exact fixture contribution. | The empty-output control above proves prefix-only output fails, and the default-agent control proves the trace is not emitted for every run. | `UAR_LIVE_INTEGRATION_BACKEND=recorded cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test integration live::capability_cases::l2_c06_orchestrator_delegates_with_trace -- --exact --test-threads=1`<br>`UAR_LIVE_INTEGRATION_BACKEND=live cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --test integration live::capability_cases::l2_c06_orchestrator_delegates_with_trace -- --exact --test-threads=1` | Recorded: `1 passed; 0 failed; 92 filtered out; 9.63s`.<br>Live: `1 passed; 0 failed; 92 filtered out; 14.12s`. |
| Local and remote AgentNode result paths remain usable. | Local delegation stores non-whitespace driver text; remote A2A delegation stores the task plus returned non-whitespace agent text; graph execution records the exact node traversal. | The empty-output control above observed local fail-closed behavior. The existing unreachable-URL test remains the remote request-error control; it was not rerun because this correction did not alter that branch. | Exact focused commands and outputs are recorded in `evidence/positive-verification.md` and `evidence/negative-controls.md`. | Local, remote, trace, and empty-output focused tests passed. |
| Change-level gates pass. | Rust formatting, Tier 0, scoped Clippy, strict OpenSpec, scoped diff checking, and finalized artifact-refiner schemas/references are required. | Not a fail-closed product assertion. Independent critic and judge both reviewed the corrected candidate after blocking iteration 1. | Exact commands and outputs are recorded in `evidence/positive-verification.md` and `evidence/artifact-refiner-validation.md`. | Local gates pass. Clippy exits 0 with 571 warnings; no warning-free claim. Critic PASS and judge PASS. Active and history artifact states are finalized and schema-valid. Phase Tier 2 remains deferred. |
