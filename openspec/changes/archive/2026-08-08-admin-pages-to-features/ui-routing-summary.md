# C-14a UI/UX routing summary

The required memory consult ran and produced its documented unreachable-endpoint stub. UI Pro Max classifies these surfaces as a data-dense drill-down operations dashboard and prioritizes visible keyboard focus, announced/actionable errors, explicit loading feedback, stable responsive layout, restrained motion, and 4.5:1 text contrast. Its generic Fira/code-green palette recommendation is not adopted because the higher-ranked UAR design authority already fixes Slash Gate typography, semantic ember/cyan/status tokens, Base UI primitives, Flat 2.0, and light/dark behavior. `frontend-design` reinforces an intentional industrial/utilitarian console rather than a generic admin template; in this behavior-preserving change that means retaining the established visual system rather than restyling it. Vercel React guidance reinforces state-decoupled UI, narrow effect dependencies, and avoiding broad barrels; C-14a therefore preserves UI → hook → store → API flow and uses small explicit feature entry exports only where the architecture requires a public seam. The composition guidance does not justify refactoring the existing `AdminShell` render callback during a path migration. The mandated Impeccable audit/critique commands and `ux-designer` skill are not installed, so their intent was applied manually: source inspection found the existing shared error projection, loading/empty states, 100 page buttons, 47 responsive-class sites, and exactly 307 legacy token expressions across the five planned files. Accessibility redesign and touch-target certification remain C-15-owned; C-14a must preserve current labels, keyboard behavior, non-color status cues, and responsive structure while relocating ownership and converting only the recorded token expressions.
