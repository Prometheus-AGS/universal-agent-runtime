## 1. Workflow and budget command bootstrap

- [x] 1.1 Record canonical KBD C-13 as in-progress and scaffold the `ci-bundle-and-perf-budget` OpenSpec change
- [x] 1.2 Measure and document the current 977,298-byte gzip entry closure, existing trace fixture, cold PGlite path, Markdown pipeline, and primary frontend CI job
- [x] 1.3 Define one repository-owned budget inventory for the 250,000-byte bundle limit and the 1,000ms, 100ms, and 250ms latency limits
- [x] 1.4 Add explicit local package commands for bundle analysis, deterministic proof fixtures, and serial Chromium performance verification without adding a dependency

## 2. Fail-closed production bundle gate

- [x] 2.1 Implement the Vite-manifest loader and single-entry static-import traversal with schema validation, cycle handling, shared-file deduplication, and build-root confinement
- [x] 2.2 Measure the actual emitted JavaScript with deterministic gzip settings and emit stable human-readable plus JSON evidence containing files, raw bytes, gzip bytes, limit, exclusions, and verdict
- [x] 2.3 Integrate the existing Markdown engine graph evidence so missing, malformed, or eager Mermaid/Shiki entries fail even when the byte total passes
- [x] 2.4 Prove exactly one named PGlite JavaScript chunk remains statically reachable and reported with its WASM/data assets outside the charged application total
- [x] 2.5 Add synthetic passing, exact-limit, over-limit, malformed-manifest, missing-import, path-escape, cycle, shared-import, and eager-engine fixtures against the production checker

## 3. Honest startup loading boundaries

- [x] 3.1 Move the non-default Admin and About route surfaces behind named React lazy boundaries with one shared accessible Suspense fallback
- [x] 3.2 Preserve `/threads`, the shell, thread-list hydration, and all JavaScript required for first thread rendering in the counted static entry closure
- [x] 3.3 Add focused route tests proving default navigation, lazy Admin/About resolution, accessible loading state, and unchanged resolved content
- [x] 3.4 Use manifest evidence to retain only behavior-preserving loading boundaries that materially reduce startup bytes without removing C-14-owned dependencies or features

## 4. Browser latency budgets and negative proofs

- [x] 4.1 Implement the shared performance assertion and stable result format, with deterministic tests for below-limit, exact-limit, above-limit, negative, and non-finite observations for every named budget
- [x] 4.2 Add a dedicated Playwright cold-start fixture using a fresh Chromium context and real `idb://uar-threads` PGlite initialization from the production versioned schema seed, measuring navigation start through the first browser-frame callback after the hydrated thread-list commit within 1,000ms
- [x] 4.3 Connect the existing 500-event Chromium trace fixture to the shared result contract while preserving the 100ms limit, distant selection, 500-event semantics, and fewer-than-40 mounted-row proof
- [x] 4.4 Add a Chromium Storybook fixture that commits representative 2,000-line finalized content through `MarkdownBubble`, proves its sentinel/content structure, and enforces the 250ms limit without Mermaid or Shiki startup
- [x] 4.5 Add one serial performance command that runs all three supported Chromium measurements and retains their raw named results for CI diagnosis

## 5. Ordinary pull-request CI enforcement

- [x] 5.1 Update the existing primary frontend CI job to emit one production manifest and run deterministic negative proofs before trusting positive measurements
- [x] 5.2 Evaluate the same production build with the bundle/engine gate and run the three Chromium budgets serially so any regression fails the pull-request job
- [x] 5.3 Verify the existing Storybook/Chromatic workflow, configured token handling, and four explicitly deferred tasks remain unchanged and independent from C-13

## 6. Verification, review, and closeout

- [x] 6.1 Run frontend typecheck, lint, architecture-boundary, Flat 2.0, and focused C-13 proof/route tests after implementation is wired
- [x] 6.2 Run the completed C-13 production build with manifest, require an honest `/threads` closure at or below 250,000 gzip bytes, and require Mermaid/Shiki graph integrity
- [x] 6.3 Run all three supported Chromium performance fixtures and preserve evidence that each product structure rendered within its exact binding limit
- [x] 6.4 Run the full frontend suite and production build at the C-13 verification boundary, disclosing any tier deviation or pre-existing external failure accurately
- [x] 6.5 Run strict OpenSpec change and `frontend-build-tooling` capability validation, artifact refinement, and scoped diff-integrity checks
- [x] 6.6 Run isolated adversarial review on the completed C-13 artifact packet and resolve every critical finding or record an evidence-backed nonblocking disposition
- [x] 6.7 Write `files.txt` and verification evidence, mark completed tasks, and confirm provider, backend, realtime, C-14, `.gitmodules`, skill-system submodule, and staged license-deletion surfaces remain untouched
- [x] 6.8 Transition canonical KBD C-13 to complete, append the `.prometheus` waypoint, archive the OpenSpec change, emit the step-16 completion signal, and advance automatically to C-14a
