# C-14d UI-routing summary

The phase memory consult remains the graceful-degradation stub because the memory endpoint is unreachable. UI/UX Pro Max, Impeccable, ux-designer, and the named Vercel skills are unavailable in this harness; the available frontend-design guidance and binding in-repo Base UI/Flat 2.0 contracts were used instead. This is a behavior-preserving primitive verification, not a visual redesign: keep the established tokenized surfaces, focus treatment, 44px host controls, responsive shell, and stable feature call sites. Current Base UI documentation says Combobox is a filterable selection control and recommends Autocomplete for search widgets; because the live `Command*` consumers activate actions and some remain open for repeated additions, the adapter uses Base UI Autocomplete so selection does not overwrite the query. Verification must cover filtering, empty state, pointer and Enter activation, real agent/model selectors, the shared shell, dialogs/selects, full frontend tests/build/budget, and accepted third-party Radix ownership.
