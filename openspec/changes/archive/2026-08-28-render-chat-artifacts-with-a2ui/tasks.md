## 1. Runtime surface

- [x] 1.1 Add a pure effective-policy A2UI v0.9.1 surface builder.
- [x] 1.2 Emit the surface from the run manager with A2UI media metadata.

## 2. Chat renderer

- [x] 2.1 Add the canonical A2UI workspace packages to the frontend app.
- [x] 2.2 Parse and process display artifacts with `MessageProcessor`.
- [x] 2.3 Render surface models with `UarSurface` and add an explicit source fallback.
- [x] 2.4 Recognize current v0.9.1 message names in custom AG-UI envelopes.

## 3. Verification

- [x] 3.1 Add focused Rust and React regression coverage.
- [x] 3.2 Run the authorized verification tier and the final UI quality gate.
