# Verification — `adopt-entity-management-3-0-2`

Date: 2026-08-23

Source baseline: `19e0ffc2fad984fc74047fdb4431d501089397a5`

Profile scope: local UAR React frontend dependency and architecture boundary
only. These results transfer to no runtime, deployment, or provider profile.

| requirement | assertion observed | negative control observed | command | result | limit |
|---|---|---|---|---|---|
| Use the reviewed Entity Management release. | `frontend/package.json` declares exact `@prometheus-ags/prometheus-entity-management` `3.0.2` and exact `@prometheus-ags/entity-graph-core` `3.0.2`. Both lockfiles contain the reviewed registry integrities: PEM `sha512-5Zg5E7rB6ceWBGbaakjLDt/p7jV4GRW2AXJ93ADAnDw6QOeHB/bMHlRrPvTs+EvU8Ut/sY3nIgAeu2EABlrJsQ==`; core `sha512-p+TbB6XEmk7N748v5P7+IOKqrxi7UEStT14Ubi71mK/iniYUoc8hiNF9IRQEL3waXsGiiw7+WMWnCqbzOoAKaw==`. The application importers contain no product `link:` target. | Before this change the application manifest used `workspace:*`, and both lockfile importers resolved `link:packages/prometheus-entity-management/packages/entity-graph-react` at local version `3.0.0-rc.1`. | In a clean scratch worktree at the source baseline: `pnpm install --lockfile-only --filter uar-frontend`; `pnpm -C frontend install --lockfile-only --filter uar-frontend`. In the change worktree: `pnpm --config.minimum-release-age=0 install --frozen-lockfile --lockfile-only`; `pnpm -C frontend --config.minimum-release-age=0 install --frozen-lockfile --lockfile-only`; then the recorded Node importer/integrity assertion. | Both filtered lockfiles added only the two registry packages and their required snapshots. Both frozen-lock checks exited 0 and reported the lockfiles current. The assertion printed exact registry snapshots and integrity verified for both files. | Exactly version `3.0.2`; no range, application `link:` target, or unrelated resolution change. |
| Preserve one application core runtime. | Both roots show UAR depending on core `3.0.2` directly and PEM `3.0.2` consuming that same core as its peer. Each `why` command prints `Found 1 version of @prometheus-ags/entity-graph-core`. | The initialized vendored workspace remains separately visible as its development-only `3.0.0-rc.1` project, proving the graph inspection distinguishes workspace tooling from the UAR application importer. | `pnpm --filter uar-frontend list @prometheus-ags/prometheus-entity-management @prometheus-ags/entity-graph-core --depth 2`; `pnpm --filter uar-frontend why @prometheus-ags/entity-graph-core`; `pnpm -C frontend list @prometheus-ags/prometheus-entity-management @prometheus-ags/entity-graph-core --depth 2`; `pnpm -C frontend why @prometheus-ags/entity-graph-core`. | Exit 0 for all four commands; each application graph reports PEM/core `3.0.2`, and each `why` prints `Found 1 version of @prometheus-ags/entity-graph-core`. | One core version in the UAR application graph. |
| Preserve the frontend entity boundary. | The production boundary checker reported zero violations, and all negative fixtures were rejected. | The existing ten intentionally invalid fixtures were observed rejected by the negative fixture runner. | `node scripts/check-frontend-boundaries.mjs`; `node scripts/test-frontend-boundaries-negative.mjs` | Both exited 0; `Frontend boundary gate passed (0 production violations). Frontend boundary negative fixtures passed (10 rules rejected).` | Zero production violations; all 10 negative rules rejected. |
| Keep the frontend type- and lint-clean at Tier 0. | TypeScript project references and ESLint completed without diagnostics. | Not a fail-closed requirement; no negative control required. | `frontend/node_modules/.bin/tsc -b frontend`; `frontend/node_modules/.bin/eslint frontend` | Both exited 0 with no diagnostics. | Local frontend Tier 0 only; no unit, build, browser, or broad suite run. |
| Preserve the repository release-cooldown policy. | Neither `pnpm-workspace.yaml` file changed. A command-local override was used only after exact versions and registry integrities were independently verified. | `pnpm install --frozen-lockfile` exited 1 with `ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION`, naming core published `2026-08-23T19:16:12Z` and PEM published `2026-08-23T19:17:06Z`. A normal `pnpm typecheck` was also intercepted before TypeScript and exited 1 at the same dependency-status check. | `pnpm install --frozen-lockfile`; `pnpm typecheck`; positive verification: `pnpm --config.minimum-release-age=0 install --frozen-lockfile --lockfile-only` and the corresponding `pnpm -C frontend` command. | Both normal commands exited 1 before product execution at the 24-hour cooldown. Both command-local frozen-lock validations exited 0. The repository policy remains unchanged; a normal install becomes eligible after the package-age cutoff. | No persistent policy exception and no file outside the permitted product surface. |
| Keep the OpenSpec change structurally valid. | Strict validation reported the change valid. | Not a fail-closed product assertion; no negative control required. | `openspec validate adopt-entity-management-3-0-2 --strict` | Exit 0; `Change 'adopt-entity-management-3-0-2' is valid`. | This change only. |

No product unit, integration, browser, build, performance, or soak suite was run
in this dependency-adoption change. The phase's one installed-service functional
proof remains owned by `prevent-session-configuration-regressions`, after all
UAR code is complete.
