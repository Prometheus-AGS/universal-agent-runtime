# Verification — `fix-jwt-crypto-provider`

Date: 2026-08-14
Baseline: `a5978c038a97d499c219d55dd4e43feea3268e91`
Runtime scope: `server-full` only. The iOS and Android rows are separate
`embedded-mobile` target checks and transfer to no other profile or target.

Status: **READY FOR A0 FOLLOW-UP COMMIT.** Dual-provider execution on 2026-08-14
proved that the attempted pointer-identity correction accepted AWS-LC. The
pinned source confirms why: `install_default` delegates to `OnceLock::set`,
whose error returns the attempted value, while the installed-provider getter is
crate-private. The enforceable rule is UAR-owned first installation. The shared
server-startup funnel acquires RustCrypto before routes or readiness; guarded
operations reuse only that UAR-owned success. Any earlier provider installation
fails closed.

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| All UAR-owned `jsonwebtoken` dependencies inherit exactly `11.0.0` with defaults disabled and RustCrypto selected. | Root and proxy manifests inherit the workspace entry; `cargo tree` shows `jsonwebtoken feature "rust_crypto"` from both packages and no `aws_lc_rs` edge for 11.0.0. | Before reconciliation, the same tree showed `aws_lc_rs` from the runtime and `rust_crypto` from the proxy simultaneously. | `cargo tree --locked --offline --workspace -e features -i jsonwebtoken@11.0.0` | Exit 0. Final tree contains `rust_crypto` and `use_pem`; it contains no `aws_lc_rs`. `Cargo.lock` required no package change after regeneration. |
| UAR owns first provider installation; its repeated initialization is idempotent, while any earlier process owner returns `JwtError::ProviderConflict`. | `uar_owned_rustcrypto_initialization_is_idempotent` passed. Isolated processes installed AWS-LC and RustCrypto separately before the final UAR guard; both observed `ProviderConflict`. `src/server.rs` calls the guard at the beginning of the shared startup funnel, before telemetry, configuration, routes, or readiness. | The intermediate pointer guard's AWS-LC conflict assertion failed, proving the pointer comparison accepted AWS-LC. After restoring first ownership, `cargo test --offline tests::negative_control_assumes_preinstalled_aws_lc_is_accepted -- --exact --nocapture` failed with `ProviderConflict`, exit 101. Literal scratch source, commands, and outputs: `evidence/negative-controls.md`. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::security::jwt::tests::uar_owned_rustcrypto_initialization_is_idempotent -- --exact --test-threads=1`; scratch positives: exact `conflicting_aws_lc_provider_returns_structured_error` and `preinstalled_rustcrypto_returns_structured_error` tests | Runtime idempotence: 1 passed, 0 failed. AWS-LC-first positive: 1 passed, 0 failed. RustCrypto-first positive: 1 passed, 0 failed. False AWS-LC acceptance: 0 passed, 1 failed. |
| Guarded HS256 middleware verification succeeds and rejects a token signed with the wrong secret. | `test_resolve_user_context_valid_token` and `test_resolve_user_context_rejects_token_signed_with_wrong_secret` both passed through the guarded wrapper. | A provider-disabled exact-v11 scratch round trip failed at `crypto/mod.rs:124` with the missing-provider panic. A final-source scratch test deliberately expecting wrong-secret acceptance failed with `Token(Error(InvalidSignature))`, exit 101. Literal scratch sources, commands, and outputs: `evidence/negative-controls.md`. | `RUSTC_WRAPPER= SCCACHE_DISABLE=1 cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::security:: -- --nocapture`; negative: `cargo test tests::negative_control_assumes_wrong_secret_is_accepted -- --exact --nocapture` in `/tmp/uar-jwt-wrong-secret-control` | 25 passed, 0 failed. Wrong-secret negative control: 0 passed, 1 failed. Provider-disabled control: 0 passed, 1 failed with the documented provider-selection panic. |
| API-key exchange mints through the wrapper and adds service context to provider/token errors. | `uar::security::api_keys::tests::test_create_and_exchange` passed in the final security slice; source routes issuance through `jwt::encode` and prefixes errors with `issuing API-key exchange JWT`. | The provider-conflict negative control above proves the guarded encode precondition rejects a foreign provider rather than minting. | Same final security-slice command; source inspection of `src/uar/security/api_keys.rs`. | `test_create_and_exchange` passed; guarded error propagation is present on the executed issuance path. |
| `uar-jwt-proxy` installs RustCrypto before startup minting and remains idempotent. | Both proxy tests passed, including idempotent initialization and HS256 token minting. | The dual-provider negative control above exercises the same install contract and fails when acceptance of AWS-LC is assumed. | `cargo check --locked -p uar-jwt-proxy`; `cargo test --locked -p uar-jwt-proxy -- --test-threads=1` | Check exit 0. Tests: 2 passed, 0 failed. |
| Tier 0 passes for final `server-full` source without a warning introduced by A0. | Package check and package/library/no-deps clippy exited 0. The earlier formatting check remains unchanged by the surgical ownership correction. | Not a fail-closed assertion. | `cargo check --locked -p universal-agent-runtime --no-default-features --features server-full`; `cargo clippy --locked -p universal-agent-runtime --no-default-features --features server-full --lib --no-deps` | Check exit 0 with three pre-existing warnings. Clippy exit 0 with the recorded 578-warning repository baseline and no A0-owned warning. |
| Full iOS `embedded-mobile` library graph accepts the RustCrypto standard. | The checked A0 source was preserved exactly after the rejected pointer experiment. | Not a fail-closed assertion. | `RUSTC_WRAPPER= SCCACHE_DISABLE=1 cargo check --locked -p universal-agent-runtime --no-default-features --features embedded-mobile --target aarch64-apple-ios --lib`; follow-up identity check: `git diff --exit-code db1abfd3 -- src/uar/security/jwt.rs tools/uar-jwt-proxy/src/main.rs` | Target check exited 0 after 1m59s with two pre-existing fetch-guard warnings. The follow-up identity check exited 0 with no output, proving the current A0 provider source is byte-identical to the source checked for iOS. Result applies only to `embedded-mobile` on `aarch64-apple-ios`. |
| Full Android `embedded-mobile` library graph accepts the RustCrypto standard. | The checked A0 source was preserved exactly after the rejected pointer experiment. The target check used the installed NDK compiler and a temporary vendored Android OpenSSL sysroot for the pre-existing `native-tls` graph. | The unconfigured command first failed before UAR code because `aarch64-linux-android-clang` was absent; after adding the NDK path it failed in the unrelated `openssl-sys` path because no Android sysroot was configured. These environment failures were preserved and not repaired in A0 source. | Exact target command and output are retained in `evidence/android-embedded-mobile.md`; follow-up identity check: `git diff --exit-code db1abfd3 -- src/uar/security/jwt.rs tools/uar-jwt-proxy/src/main.rs` | Target check exited 0 after 1m46s with two pre-existing fetch-guard warnings. The follow-up identity check exited 0 with no output, proving the current A0 provider source is byte-identical to the source checked for Android. Result applies only to `embedded-mobile` on `aarch64-linux-android`. The temporary sysroot changed no repository manifest or lockfile. |
| The A0 OpenSpec change remains strict-valid. | Strict validation completed against the final proposal, delta, and tasks. | Not a fail-closed assertion. | `openspec validate fix-jwt-crypto-provider --strict` | Exit 0: `Change 'fix-jwt-crypto-provider' is valid`. |
| The artifact-refiner deterministic and independent adversarial gates pass. | The validator replay passed manifest/constraint schemas, referenced files, all four blocking constraints, and state consistency after the first-owner correction. The history-free critic and judge both reported no concrete A0 blocker after the evidence correction. | Not a fail-closed assertion. | `/refine-validate fix-jwt-crypto-provider`; history-free artifact-critic and judge replay after evidence correction | Schema PASS; files PASS; constraints 4/4 PASS; consistency PASS; critic PASS; judge PASS. Receipt: `.refiner/artifacts/fix-jwt-crypto-provider/`. |
| Canonical handoff leaves A0 complete and A1 pending next. | Canonical KBD state at revision 91 reports `fix-jwt-crypto-provider` complete, `gap-02-jwks-token-verifier` pending, active phase `uar-1-0-readiness`, and exact next work `/kbd-execute uar-1-0-readiness`. A1 has 18 pending tasks and passes strict validation. | Not a fail-closed assertion. | `prometheus kbd status --json`; `rg -c '^- \[[ x]\]' openspec/changes/gap-02-jwks-token-verifier/tasks.md`; `rg -c '^- \[ \]' openspec/changes/gap-02-jwks-token-verifier/tasks.md`; `openspec validate gap-02-jwks-token-verifier --strict` | KBD revision 91, plan revision 6; A0 complete; A1 pending; exact next command set. A1: 18 total, 18 pending; strict-valid. |
| A0 source changes remain inside the contract's permitted surface. | Final diff inspection found only `Cargo.toml`, the narrow provider-install call in `src/server.rs`, `src/uar/security/api_keys.rs`, `src/uar/security/jwt.rs`, `src/uar/security/middleware.rs`, `src/uar/security/mod.rs`, `tools/uar-jwt-proxy/Cargo.toml`, and `tools/uar-jwt-proxy/src/main.rs`. Each is listed by the amended A0 Track A surface. `Cargo.lock` has no final diff. | Not a fail-closed assertion. | `git diff --name-only -- Cargo.toml Cargo.lock src tools tests \| sort`; `git ls-files --others --exclude-standard -- src tools tests \| sort` | Exit 0. No changed or untracked source file appears outside the A0 permitted surface. |

Tier 2 was not run. It remains reserved for completion of all six phase changes.
