The memory consult produced the recorded unreachable-endpoint stub, so no prior Surreal Memory context is available. The binding KnowMe standard and C-15 plan define a compact industrial runtime console whose accessibility work must preserve the existing Base UI/Assistant UI architecture, semantic ember/cyan/status tokens, Flat 2.0 surfaces, and light/dark behavior rather than redesign it. The available `a11y-gate` requires resolved AA contrast, semantic landmarks, accessible control names, keyboard operation with visible focus, polite live regions, reduced motion, 24px targets, media alternatives, and status conveyed beyond colour. The named UI/UX Pro Max, Impeccable audit/critique/polish, frontend-design, ux-designer, and Vercel skills are not installed in this session; their routing intent is therefore covered by direct source inspection, fail-closed Storybook axe tests, integrated Playwright axe and keyboard probes, computed-style checks, and the repository's strict UI → hook → store → service boundary gate. Implementation remains surgical: fix only failures reproduced by those probes, then certify the four required widths in both themes and classify rather than overclaim checks that need a real backend or a Flutter application surface.
