# Verification — `prevent-session-configuration-regressions`

Date: 2026-08-23

Source baseline: `6d3bc5466504d5efb8dff0070c30cfb6803ec984`

Profile scope: the locally installed macOS LaunchAgent built from the UAR
`server-full` release profile and served at `http://127.0.0.1:1906`, plus the
React source and repository guardrails in this change. Results transfer to no
other host, browser, deployment, build profile, or runtime configuration. No
aggregate or runtime-level verdict is made.

| requirement | assertion observed | negative control observed | command and observed output | source SHA | profile | limit |
|---|---|---|---|---|---|---|
| Use the operator-required Entity Management release. | The installed frontend resolves both `@prometheus-ags/prometheus-entity-management` and `@prometheus-ags/entity-graph-core` to exactly `3.0.2`. The production bundle contains the dedicated `vendor-entities-BcN9xY4K.js` chunk after the Vite package-path rule was reconciled for registry packages. | The first production build after the 3.0.2 registry migration did not produce a `vendor-entities` chunk because the old rule recognized only the former workspace path. The corrected rule matches the scoped npm paths and the rebuilt asset was observed. | `pnpm_config_minimum_release_age=0 pnpm -C frontend list @prometheus-ags/prometheus-entity-management @prometheus-ags/entity-graph-core --depth 0` → both `3.0.2`; `pnpm_config_minimum_release_age=0 pnpm -C frontend build` → exit 0 in 2.49s and emitted `vendor-entities-BcN9xY4K.js`. | baseline + this change | React production bundle | Exact local dependency and bundle evidence only; no later Entity Management release is claimed or consumed. |
| Enforce the React/entity-state architecture in durable instructions. | `AGENTS.md` and `.claude/rules/typescript.md` require typed platform domain hooks for graph-owned records, narrow subscriptions, atomic fetched-list ingestion, and restrict `useState`/Zustand to UI-local or transient workflow state. | Independent forbidden fixtures for render-body setters, facade bypass, per-row writes, and named duplicate entity caches were rejected while the allowed local-state/domain-action fixtures passed. | `node scripts/test-frontend-boundaries-negative.mjs` → `Frontend boundary fixtures passed (19 independently checked forbidden rules rejected; allowed UI/domain paths accepted).` | same | Repository source policy | Static architectural enforcement; it does not measure React render cost or prove every future behavior is correct. |
| Keep production frontend ownership within the permitted boundaries. | The production scanner reported zero boundary violations with stable path, line, and rule reporting enabled. | The same scanner logic rejected each isolated forbidden fixture for its intended rule before the production pass. | `node scripts/check-frontend-boundaries.mjs` → `Frontend boundary gate passed (0 production violations).`; negative runner above → 19 forbidden cases rejected. | same | React production source | Rules cover the named ownership and mutation patterns, not arbitrary semantic defects. |
| Build and install the completed release once before functional verification. | The React bundle and locked `server-full` release binary built successfully; the installer preserved identical source/installed binary SHA-256 and the LaunchAgent returned healthy and ready. HTTP listened only on `127.0.0.1:1906` and `[::1]:1906`; gRPC listened only on `127.0.0.1:50051`. | Readiness was not inferred from the process label: `/healthz`, `/readyz`, installed hashes, and listeners were checked separately. | `cargo build --locked --release --no-default-features --features server-full` → exit 0 in 13m40s with three pre-existing warnings; `packaging/native/macos/install.sh --binary target/release/universal-agent-runtime --static-dir static` → exit 0; source and installed binary SHA-256 both `89f0922d1424d601b1f20fdbbd6991adcbc736932878e995606d5f2e88bb7053`; `/healthz` → `ok`; `/readyz` → `ready`. | same | macOS LaunchAgent, `server-full` | Local installed macOS service only. The build warnings and transitive future-incompatibility notices are outside this change. |
| Keep the Session Configuration sheet responsive and bounded. | The installed sheet opened in 81 ms, below the host-local 2,000 ms limit. The graph emitted 16 publications against a limit of 23 and exposed 12 configured models. No `/api/models` request occurred. | The scenario fails if open time exceeds 2,000 ms, publications exceed configured providers plus configured models plus six, the legacy endpoint is requested, or the model projection is empty. The observed run passed all gates. | `pnpm_config_minimum_release_age=0 pnpm -C frontend exec playwright test -c playwright.installed-session-config.config.ts` → `1 passed (10.9s)`; embedded report → 81 ms, 16/23 publications, 12 configured models, zero `/api/models` requests. | same | Installed Chromium against local `server-full` | Timing is a host-local interaction ceiling, not a general performance claim. One Chromium run, one worker, no retry. |
| Preserve session editor behavior and responsive spacing. | Save/reopen retained the selected model, cancel did not persist the draft, and computed measurements showed 16 px left/right padding plus a 24 px content gap at 320, 768, 1024, and 1440 px. Sheet width was 240 px at 320 and approximately 384 px at the other widths. | The scenario performs both a persisted save and a canceled edit; it fails if the canceled value survives or if any measured padding/gap violates the stated values. | Same focused Playwright command → passed; embedded proof records all four viewport measurements and the save/reopen/cancel assertions. | same | Installed Chromium against local `server-full` | Four explicit viewport widths only; no other browser, zoom level, font override, or assistive-technology claim. |
| Apply Global→Agent→Conversation→Turn model precedence to genuine inference. | Genuine inference completed through the installed service with explicit-turn `local-openai-proxy/gpt-5.4`, saved-session `local-openai-proxy/gpt-5.4-mini`, and agent-default `local-openai-proxy/gpt-5.5`; both UI submission and direct saved-session submission completed. | Each inference assertion requires the recorded effective model to equal its distinct expected precedence source. The local proxy's advertised `gpt-5.4-nano` was separately observed to reject this account, so it was not substituted for a passing model. | Same focused Playwright command → passed; `playwright-report.json` records the three effective models; `server-log-interval.jsonl` records four matching completed inference lifecycles without prompts or credentials. | same | Installed local `server-full` plus local OpenAI proxy | Confirms these three currently usable proxy model routes on this host only; advertised inventory is not treated as proof of account usability. |
| Treat an absent new-session configuration as an expected negative control. | Three initial GETs for the new session returned 404, followed by POST and GET status 200 after creation. No other HTTP request failed. | The 404 responses are the explicit absence control; the scenario would fail if a pre-existing configuration were returned or if persistence did not change the later result to 200. | Embedded network proof in `playwright-report.json` → three matching 404 GETs, then 200 POST/GET; `.prometheus/evidence/session-configuration/README.md` documents the interval. | same | Installed local `server-full` | The browser console therefore contains three expected resource errors; they are not generalized away as clean-console evidence. |
| Keep functional evidence credential-free. | The retained Playwright report and projected server interval contain no API key, authorization header, bearer token, secret, or `sk-` material; the JSONL parses successfully. | The credential scanner was configured to fail on any named sensitive pattern. No match was observed. | Scoped credential scan → `credential scan passed: no key, authorization, bearer, secret, or sk-* material found`; JSONL parse → exit 0. | same | `.prometheus` evidence | Pattern-based scan of these retained artifacts only; it is not a repository-wide secret audit. |
| Preserve required Tier 0 and OpenSpec validity without moving product tests to GitHub Actions. | TypeScript typecheck, ESLint, boundary checks, production frontend build, locked release build, and strict OpenSpec validation passed locally. `.github/workflows/**` has no diff. | The boundary fixture runner supplies the fail-closed static negative controls. No product verification was run in GitHub Actions. | `pnpm_config_minimum_release_age=0 pnpm -C frontend typecheck` → `tsc -b`, exit 0; `pnpm_config_minimum_release_age=0 pnpm -C frontend lint` → `eslint .`, exit 0; `openspec validate prevent-session-configuration-regressions --strict` → `Change 'prevent-session-configuration-regressions' is valid`; `git diff --name-only -- .github/workflows` → no output. | same | Local source, OpenSpec, and `server-full` build | No broad unit, regression, soak, security, load, or release-certification suite ran. |
| Apply adversarial review to the architectural correction. | After earlier blocking findings were corrected, the history-blind artifact critic returned PASS and the independent judge returned APPROVE for the session entity flow and its regression guardrails. | Earlier critic/judge rejections caught lossy whole-config writes, fire-and-forget agent selection, and insufficient boundary coverage; those rejections establish that the gate did not always pass. | Reviewer results retained in the KBD execution history for this phase. | same | Source/process review | Review is not runtime evidence and does not replace the installed functional scenario. |

## Unverified limits

- No Linux, Windows, remote-host, mobile, `minimal`, or non-`server-full`
  behavior was exercised.
- No browser other than the locally installed Playwright Chromium was used.
- No general render benchmark, broad frontend suite, soak, or long-duration
  inference run was performed.
- Entity Management 3.0.2's remaining split fetched-list publication path is
  an upstream defect and is not represented as fixed by this UAR change. Its
  correction is the phase's separate upstream track; UAR remains pinned to
  release 3.0.2 for this phase.
