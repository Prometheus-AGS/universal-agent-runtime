# Verification — `gap-02-jwks-token-verifier`

Date: 2026-08-14
Profile scope: `server-full` only. These results transfer to no other profile.

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| Token verification uses one `TokenVerifier` abstraction and preserves the HS256 lane. | The final security slice executed the shared-secret verifier, guarded JWT wrapper, JWKS verifier, and middleware through the same abstraction. Valid HS256 and wrong-secret rejection both passed. | Inverting only the required-token verification-error branch made the wrong-secret assertion fail with the literal anonymous-context panic in the focused group: exit 101, `5 passed; 5 failed`; `556 filtered out`. The branch was restored exactly and the focused group passed 10/10. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::security:: -- --test-threads=1`; literal grouped control command and output in `evidence/fail-closed-negative-controls.md` | Exit 0. `33 passed; 0 failed`; `533 filtered out`. Result is limited to the `server-full` library security slice. |
| JWKS verification accepts RS256 tokens, retains two simultaneous `kid` values, refreshes on rotation, and refreshes at most once for a still-unknown `kid`. | `jwks_token_authenticates_through_middleware_resolution`, `accepts_two_cached_keys_and_refreshes_after_rotation`, and `unknown_kid_refreshes_once_then_rejects` passed. Request-count assertions observed one initial fetch, one rotation refresh, and no second refresh for the missing key. | Under the verification-error inversion, `jwks_unknown_kid_maps_to_unauthorized` produced the literal anonymous-context panic in the grouped exit-101 control. After exact restoration the focused middleware group passed 10/10 and retained the one-request assertion. | Same final security-slice command; literal grouped control in `evidence/fail-closed-negative-controls.md`. | Exit 0. All named JWKS tests passed. Cache scope is per JWKS URL and contains a `kid -> DecodingKey` map plus refresh timestamp. |
| `security.jwt_required` is the configured value used by middleware; required requests reject absent and invalid tokens while explicitly disabled requests remain anonymous. | The hardcoded literal was removed. Required/no-header and required/bad-signature tests passed; disabled/no-header and disabled/invalid-header tests passed. | Inverting the independent no-header branch made the required/no-header exact assertion fail with an anonymous context: exit 101, `0 passed; 1 failed`; `565 filtered out`. The complete source diff matched the retained pre-control diff after restoration, and the exact assertion passed. | Same final security-slice command; `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --bin uar-sidecar -- --test-threads=1`; exact control in `evidence/fail-closed-negative-controls.md`. | Security assertions passed within `33 passed; 0 failed`. Sidecar result: `3 passed; 0 failed`, so the contract stop condition did not fire. |
| Configured issuer and audience are enforced in addition to signature validity. | `rejects_wrong_issuer_and_audience`, `jwks_wrong_audience_maps_to_unauthorized`, and `jwks_wrong_issuer_maps_to_unauthorized` passed with correctly signed tokens carrying the wrong `iss` or `aud`. | Under the verification-error inversion, the audience and issuer middleware assertions produced their literal anonymous-context panics in the grouped exit-101 control. Both passed in the restored 10/10 focused group. | Same final security-slice command; literal grouped control in `evidence/fail-closed-negative-controls.md`. | Exit 0. Verifier-level and separate middleware-level claim mismatch assertions passed and mapped to unauthorized. |
| Verification fails closed when JWKS is unreachable and no key is cached, and records the fetch failure at error level. | With `jwt_required=true`, `unreachable_jwks_with_no_cache_fails_closed` returned `StatusCode::UNAUTHORIZED`. Its error-only tracing capture observed output containing both `ERROR` and `JWKS refresh failed`. | Under the verification-error inversion, the assertion produced its literal anonymous-context panic in the grouped exit-101 control (`5 passed; 5 failed`). The branch was restored with `apply_patch`; `diff -u` against the retained complete positive source diff exited 0 with no output; the focused group then passed 10/10. | `cargo test --locked -p universal-agent-runtime --no-default-features --features server-full --lib uar::security::middleware::tests:: -- --test-threads=1`; source comparison and literal output in `evidence/fail-closed-negative-controls.md` | Inverted source: exit 101, `5 passed; 5 failed`; `556 filtered out`. Restored source: exit 0, `10 passed; 0 failed`; `556 filtered out`. Complete pre-control diff SHA-256: `95bed2720b8ef8638c79e7545a5fa1148355b9a5a80d1f2e6fcc1d87447f146b`. |
| Final A1 source passes Tier 0 without a new A1-owned warning. | Package check and package/library/no-deps clippy both exited 0 on final source. | Not a fail-closed assertion. | `cargo check --locked -p universal-agent-runtime --no-default-features --features server-full`; `cargo clippy --locked -p universal-agent-runtime --no-default-features --features server-full --lib --no-deps` | Check exit 0 with the three recorded pre-existing warnings. Clippy exit 0 with the recorded repository baseline of 578 warnings; no A1-owned warning was introduced. |
| The OpenSpec delta and task ledger remain strict-valid. | Strict validation completed with all 14 execution tasks marked from observed evidence and all four non-fired stop conditions intentionally unchecked. | Not a fail-closed assertion. | `openspec validate gap-02-jwks-token-verifier --strict` | Exit 0: `Change 'gap-02-jwks-token-verifier' is valid`. |
| The artifact-refiner deterministic and independent adversarial gates pass. | Manifest, constraint, and state schemas passed; the referenced receipt exists; all four blocking constraints and state consistency passed. The history-free critic and judge both returned PASS after the literal-output and ledger corrections. | Not a fail-closed assertion. | Deterministic schema/file/constraint/consistency replay; history-free artifact-critic and judge replay | Schema PASS; files PASS; constraints 4/4 PASS; consistency PASS; critic PASS; judge PASS. Receipt: `.refiner/artifacts/gap-02-jwks-token-verifier/`. |
| A1 stays inside its permitted surface and does not introduce a crate, tenant concept, or `docs/SPECIFICATION.md` edit. | Changed implementation files are limited to `src/config.rs`, `src/uar/security/middleware.rs`, `src/uar/security/mod.rs`, new `src/uar/security/verifier/mod.rs`, and the required `tests/settings_persistence.rs` literal update, plus the change's OpenSpec/KBD/verification artifacts. `Cargo.toml`, `Cargo.lock`, and `docs/SPECIFICATION.md` have no A1 diff. | Not a fail-closed assertion. | `git diff --name-only`; `git ls-files --others --exclude-standard`; focused source inspection. | No A1 stop condition fired. Tenant identity remains A2 scope. |

Tier 2 was not run. It remains prohibited until all six phase changes complete.
