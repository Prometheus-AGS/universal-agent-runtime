# Verification — `implement-native-service-runtime-support`

Source baseline: `64cdcad6519a39b2575a5113b4edd0ab156a958f` plus the pending change-2 worktree.

This artifact records compile and source-structure evidence only. The phase plan intentionally defers functional fail-closed controls and installed-service behavior until all five implementation changes are code-complete.

| requirement | assertion observed | negative control observed | command and observed output | source SHA | profile / platform | limit |
|---|---|---|---|---|---|---|
| Service environment and log-file inputs compile in startup order. | `selected_env_file` discovers the CLI/environment selector before `Cli::parse`; the selected file is loaded before telemetry and `ConfigManager`. `UAR_LOG_FILE` opens an append writer and propagates open errors. | Deferred by the phase's code-complete-first rule. Missing/malformed env-file and unwritable-log controls remain required in change 5. | `cargo check --locked -p universal-agent-runtime --no-default-features --features server-full` → exit 0, `Finished dev profile`; retained at `.prometheus/logs/uar-native-service-deployment/change2-cargo-check.txt`. | `64cdcad6519a39b2575a5113b4edd0ab156a958f` + change-2 worktree | `server-full`; macOS aarch64 compile. | Compile/source evidence only; no startup behavior claim. |
| Windows SCM adapter compiles with the exact dependency pin. | Full `server-full` reached and checked `windows-service v0.8.1` and UAR for `x86_64-pc-windows-msvc`; exit 0. | The originally planned GNU control failed before UAR type-checking: `ort-sys@2.0.0-rc.13: no prebuilt binaries available for target x86_64-pc-windows-gnu`. A direct cargo-xwin attempt also failed when global Windows compiler variables contaminated a macOS host build dependency. The corrected target-scoped environment passed. | Target-qualified MSVC/SDK variables with `cargo check --locked --target x86_64-pc-windows-msvc -p universal-agent-runtime --no-default-features --features server-full` → exit 0, `Finished dev profile`; retained at `.prometheus/logs/uar-native-service-deployment/change2-windows-msvc-check.txt`. | `64cdcad6519a39b2575a5113b4edd0ab156a958f` + change-2 worktree | Windows x86_64 cross-compile from macOS. | Compile-only. No Windows SCM runtime or installer claim. |
| HTTP and A2A gRPC share configured host selection. | Completed source resolves gRPC with `lookup_host((config.server.host.as_str(), grpc_port))` and has no wildcard fallback at the listener construction. | Deferred until the installed macOS service exposes both listeners; the final control must reject zero listener rows and wildcard/non-loopback addresses. | `rg -n 'lookup_host|server.host' src/server.rs` → the gRPC listener resolves `config.server.host`; macOS and Windows full-profile checks exited 0. | `64cdcad6519a39b2575a5113b4edd0ab156a958f` + change-2 worktree | `server-full`; source structure. | Does not prove an opened socket or listener isolation. |
| Windows Stop/Shutdown joins the process cancellation path. | The Windows handler accepts Stop/Shutdown, cancels a process token, reports Stop Pending, and awaits `start_server_with_shutdown`, which enters the existing shutdown coordinator. | Deferred until a Windows host can send SCM controls; macOS cannot provide a Windows service-runtime control. | `rg -n 'ServiceControl::Stop|ServiceControl::Shutdown|start_server_with_shutdown|StopPending' src/server/windows_service.rs src/server.rs` → all named call sites present; Windows full-profile check exited 0. | `64cdcad6519a39b2575a5113b4edd0ab156a958f` + change-2 worktree | Windows source/cross-compile. | No Windows runtime claim. |
| YAML providers are catalog-enriched before initial registration. | `ProviderRegistry::seed_from_configs` now calls `enrich_provider_config(&mut config)` before logging and insertion. | Deferred until the final installed API/UI provider visibility check. Persisted API/UI-created settings must remain authoritative. | `rg -n 'seed_from_configs|enrich_provider_config' src/llm/registry.rs` → enrichment is present inside the YAML seed loop; macOS and Windows full-profile checks exited 0. | `64cdcad6519a39b2575a5113b4edd0ab156a958f` + change-2 worktree | `server-full`; source structure. | No provider registration or inference claim. |
| Runtime implementation meets cheap static gates. | Cargo check and package-scoped Clippy both exited 0; package version is `1.0.0`; source inspection found every named interface. | N/A — static validation is not a fail-closed product assertion. | `cargo clippy --locked -p universal-agent-runtime --no-default-features --features server-full --lib --no-deps` → exit 0 with 571 pre-existing warnings, retained compressed at `.prometheus/logs/uar-native-service-deployment/change2-cargo-clippy.txt.gz`; `rg -n '^version = "1.0.0"' Cargo.toml` → line 26. | `64cdcad6519a39b2575a5113b4edd0ab156a958f` + change-2 worktree | `server-full`; macOS aarch64. | Warnings were not treated as errors and adjacent warning cleanup is outside scope. |
| OpenSpec change remains structurally valid. | Strict validation reported the change valid after the observed Windows-target correction. | N/A — schema validation is not a fail-closed product assertion. | `openspec validate implement-native-service-runtime-support --strict` → `Change 'implement-native-service-runtime-support' is valid`. | `64cdcad6519a39b2575a5113b4edd0ab156a958f` + change-2 worktree | OpenSpec contract only. | Does not prove runtime behavior. |
