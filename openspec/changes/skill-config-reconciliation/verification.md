# Verification — `skill-config-reconciliation`

Date: 2026-08-15
Profile scope: `server-full` only. These results transfer to no other profile.

Status: **IMPLEMENTED AND INDEPENDENTLY ACCEPTED; B5 COMMIT PENDING.**

| requirement | assertion observed | negative control observed | command | result |
|---|---|---|---|---|
| API/config provenance remains reliable across filesystem cold reload and upgrade residue. | The filesystem reconstruction test observed `api` beneath reserved `dynamic/` and `fs-skills` outside it. The write boundary rejected a non-API skill, the real config source won over a stale dynamic copy with the same ID, and a scoped update to a config-managed skill did not create `dynamic/`. | Removing the write-boundary check exited 101. Reversing duplicate precedence exited 101 with `left: "api"`, `right: "fs-skills"`. | Skills unit command in `evidence/positive-verification.md`; boundary and precedence inversions in `evidence/negative-controls.md`. | All four filesystem tests passed within `46 passed; 0 failed`; both negative controls exited 101; final hashes restored exactly. |
| Configuration skills are added, changed, tombstoned on removal, and restored without hard deletion across cold process restarts. | Four child processes reopened one SurrealKV directory for seed, change, remove, and restore. The durable row remained present while tombstoned and became active again on restore. | Removal safety is covered by the fail-safe and provider-discriminator inversions below. No hard-delete path was introduced. | Skills unit command and restored service command in `evidence/positive-verification.md`. | `reconciliation_survives_cold_process_restarts` and the detailed round trip passed; service slice `11 passed; 0 failed`. |
| Tombstones are distinct from enabled state, hidden from matching/default/refresh listings, retrievable for audit, and restoration preserves scoped configuration. | Domain, registry, and service assertions observed disable-at-all-scopes, hidden list/refresh/search/match results, durable retrieval, and preserved agent disable after restoration. Vector search obtains enough ranked candidates to filter tombstones before applying its visible top-five limit. | Restoring the old fixed pre-filter vector limit exited 101 with `left: 5`, `right: 6`. Returning raw refresh rows exited 101 with `left: 2`, `right: 1`. The provider inversion also tombstoned an API record and failed. | Domain and skills unit commands in `evidence/positive-verification.md`; three inversions in `evidence/negative-controls.md`. | Domain `1 passed; 0 failed`; skills `46 passed; 0 failed`; all three negative controls exited 101; final hashes restored exactly. |
| Only `provider_id = "fs-skills"` records are reconciliation candidates; API-created and built-in skills survive empty configuration. | The positive test created an API skill through `SkillService::create_skill`, cold-reloaded its dynamic file, registered a built-in, reconciled an empty source, and observed both active. | Removing the `provider_id` filter tombstoned the API skill and failed at `assertion failed: !api_skill.tombstoned`. | Exact positive and negative commands in the evidence files. | Positive passed; negative control exited 101; final source and diff hashes restored exactly. |
| An empty configuration source cannot tombstone an existing config-managed catalogue and reports the refusal at error level. | The fail-safe test persisted config, API, and built-in records, reconciled an empty source, observed all three not tombstoned, and emitted `ERROR ... configuration skill source is empty; refusing to tombstone stored skills stored_config_skills=1`. | Removing the fail-safe failed at `stored.iter().all(|skill| !skill.tombstoned)`. | Exact positive and negative commands in the evidence files. | Positive passed with the error log observed; negative control exited 101; the later test-only subscriber did not change runtime behavior. |
| Every tombstone is attributable in logs. | The focused test emitted `skill_id=config-removed reason="absent_from_configuration"` at info. | This is an observability assertion, not a fail-closed guard. | Attribution command in `evidence/positive-verification.md`. | Actual log observed; focused test `1 passed; 0 failed`. |
| Tier 0 passes for the current B5 slice. | Final package check and package/lib/no-deps Clippy both exited 0. The single B5-introduced warning was fixed, returning Clippy to the prior 573-warning baseline. | Not a fail-closed assertion. | Tier 0 commands in `evidence/positive-verification.md`. | Check exit 0 in 6.86s; Clippy exit 0 in 8.09s. |
| Strict OpenSpec validation and formatting checks pass. | Strict validation, `cargo fmt --check`, and `git diff --check` exited 0. | Not a fail-closed assertion. | Commands in `evidence/positive-verification.md`. | OpenSpec reported the change valid; formatting checks emitted no output. |
| The required artifact-refiner gate passes. | Iteration 3 of the direct-content artifact incorporates the implementation defects and the critic-required observed-log receipt; manifest, constraints, state, reference, and four-constraint consistency checks passed. | Not a fail-closed assertion. | Exact replay and finalization commands are recorded in `evidence/artifact-refiner-validation.md`. | Replay exited 0; finalization created `.refiner/history/skill-config-reconciliation/2026-08-15_09-37-33Z`. |
| Independent adversarial review accepts the final candidate. | The history-free judge returned PASS. The critic's evidence-only BLOCK was corrected with an observed error log; its exact replay and re-review returned PASS. | Not a fail-closed assertion. | Review verdicts and replay command are retained in `evidence/adversarial-review.md`. | Judge PASS; final critic PASS. |
| Phase-level Tier 2 remains deferred. | The pinned live capability suite was not run before the final B5 artifact review and commit. | Not a fail-closed assertion. | No Tier 2 command executed. | Correctly deferred until all six change implementations are committed. |
