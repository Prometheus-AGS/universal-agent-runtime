# Draft scenarios (candidate tests)

> **Current authority:** [Contributing guide](/docs/contributing/intro). Drafts
> are non-executing specifications until their steps exist and they move into the
> active feature set.

Feature files here are committed drafts whose step definitions are not yet
implemented. They live outside the `features/*.feature` generation glob so the
BDD suite stays green; implement the steps and move the file back up.

- `a2ui-live-update.feature` — added in 7a5d38b (StatePatch replay backbone)
  without steps; covers live surface updates + reconnect replay history.
