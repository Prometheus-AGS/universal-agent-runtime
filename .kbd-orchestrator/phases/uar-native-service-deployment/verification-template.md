| requirement | assertion observed | negative control observed | command and observed output | source SHA | profile / platform | limit |
|---|---|---|---|---|---|---|
| _requirement text_ | _actual observation_ | _actual failing control, or `N/A — not fail-closed`_ | _command plus concise unredacted non-secret output_ | _full SHA_ | _server-full and exact platform claim_ | _timeout, count, scope, or transfer limit_ |

Credentials and bearer values are represented only as `<redacted:env-present>`. A row is incomplete if it reports expected rather than observed behavior.
