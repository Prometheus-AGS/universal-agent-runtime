# harness-protected-context task 2.3

Execute added a pure, immutable budget planner that computes `I = min(L, H, C - O - R - M)` and returns typed fit, eligible-prose compression, protected overflow, invalid-limit or unsupported-count outcomes. Only counts marked exact or validated upper-bound with a non-empty revision can certify fit. The planner treats the complete rendered request as protected input and never performs I/O or mutates request data.

The minimum production boundary now carries an explicitly host-resolved contract through initial, iterative, retry, failover, cloned child, graph-hosted, resume and non-streaming orchestrator preparation. Liter enforces the contract immediately before dispatch for the supported synthetic exact destination. It binds the exact model and a non-secret fingerprint of the explicit base URL, applies the required output ceiling, reconstructs Liter's generic OpenAI-compatible final JSON after stream insertion and allowed `extra_body` merging, counts the complete bytes, and validates the final count. Model text alone never activates a contract. Anthropic and external production leaves reject attached contracts they cannot prove before network or handler I/O.

Protected envelope fields cannot be replaced through `extra_body`. Contracted message conversion proves that every source field survives typed Liter adaptation, and tool schemas fail explicitly when typed conversion would lose them. The captured outbound fixture proves full model identity, tool-call ID, raw argument string, tool result, output cap, stream flag and allowed metadata reach the final HTTP body unchanged. Additional fixtures prove endpoint mismatch, malformed schema, reserved-field collision, lossy message metadata, protected overflow and approximate counts all block before dispatch. Orchestrator fixtures prove the contract survives initial and iterative requests, retry attempts and failover preparation.

Final verification on the recorded source state:

- Tier 0: `RUSTC_WRAPPER= cargo check --locked --no-default-features --features server-full` finished warning-free in 57.32s.
- Tier 1: `RUSTC_WRAPPER= cargo test --locked --no-default-features --features server-full --test context_history_integrity` passed 27 tests, failed 0, and ignored 2 frozen baseline diagnostics after a 7m03s build.
- Tier 1: `RUSTC_WRAPPER= cargo test --locked --no-default-features --features server-full --test test_context_strategy` passed 4 tests, failed 0, and ignored 0 after a 7m56s build.
- `cargo fmt --check`, `git diff --check`, and `openspec validate harness-protected-context --strict` passed.
- The final isolated artifact review returned PASS with zero findings.

The correction loop is retained rather than hidden. An early captured-wire fixture exposed the explicit-base-URL model representation. The first critic found seven critical gaps, including reconstructed versus dispatched bytes, model-only selection, unsupported leaf bypasses, unchanged failover contracts and silent schema loss. A second review found two remaining critical envelope/message-loss gaps and one coverage gap. The implementation added endpoint binding, fail-closed leaves, explicit schema conversion, orchestrator contract propagation, reserved-field rejection and lossless contracted-message checks. One subsequent fixture still attempted to override `stream`; that run passed 26 tests and failed the one success fixture, which was corrected to retain the separate rejection case. The final critic passed.

No dependency or provider profile was added. The guards trace to the explicit final-request/protected-data requirements and the critic-reproduced bypasses. Exact real-provider model templates, aliases, tokenizer/output semantics and production destination resolution remain the next `harness-model-profiles-routing` change. This unit certifies only the explicit synthetic exact destination and explicit rejection elsewhere; it does not claim live-provider, profile-wide or release certification.
