# Routing 2.3 host-resource blocker — 2026-09-19

Source revision: `84586cafbd161370437700e03d3f7af3ef14cfc4`

KBD task `harness-model-profiles-routing-2-3` is blocked at canonical revision
2582. Its half-edited files are:

- `src/llm/orchestrator.rs`
- `src/llm/mod.rs`
- `src/uar/runtime/manager.rs`
- `tests/model_path_resiliency.rs`
- `tests/prompt_assembly.rs`

Tier 0 passed before the Tier 1 attempts:

```text
RUSTC_WRAPPER= cargo check --locked --no-default-features --features server-full
Finished `dev` profile [unoptimized + debuginfo] target(s) in 1m 10s
exit 0
```

The required Tier 1 target did not complete on this host:

```text
RUSTC_WRAPPER= cargo test --locked --no-default-features --features server-full --test model_path_resiliency
```

The first invocation reached 26.30 GiB used of 26.624 GiB swap. Four `rustc`
children remained blocked in filesystem I/O with no diagnostics. It was stopped
with exit 130 after more than 20 minutes.

After the UAR compiler children exited, the preflight found one unrelated orphan
`rustc` attached only to the global `sccache` daemon. The orphan was terminated,
and the preflight then observed `no cargo/rustc writers`.

The serial retry was:

```text
RUSTC_WRAPPER= CARGO_BUILD_JOBS=1 cargo test --locked --no-default-features --features server-full --test model_path_resiliency
```

It started with 25.85 GiB used of 26.624 GiB swap. Its sole `rustc` process then
remained at 0% CPU for more than 14 minutes, had no open source or output files,
and could not be sampled beyond the sampler header. It was stopped with exit 130.

No Tier 1 pass, task completion, commit, or production-readiness claim is made.
The next valid attempt requires enough free memory and swap for the exact
`server-full` target to complete. `CARGO_BUILD_JOBS=1` should remain set unless
the operator supplies a second build host or materially more local capacity.

The `karpathy-progress-memory` skill is unavailable in the active skill roster.
The canonical KBD transition and `.prometheus/session-log.md` are the fallback
progress records required by the operator correction.
