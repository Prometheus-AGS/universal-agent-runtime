PLAN: fix-container-rust-toolchain-pin-consistency
Project: universal-agent-runtime
Date: 2026-08-22
OpenSpec available: YES
Changes to implement: 1

CHANGE LIST (ordered)
1. fix-container-rust-toolchain-pin-consistency: Make every production Docker Rust build consume the dated repository toolchain pin and reject a floating selector.
   - Scope: Dockerfile backend command, one narrow local verification script, one new runtime-image-toolchain-consistency OpenSpec capability, child KBD/evidence artifacts, and append-only project history
   - Depends on: NONE
   - Recommended agent: Codex
   - Est. complexity: S
   - Customer value: HIGH
   - Details: Replace only cargo +nightly in the production backend build with cargo +"${RUST_TOOLCHAIN}", retaining nightly-2026-07-18 and all current image features. Add a local contract check that compares Dockerfile's default RUST_TOOLCHAIN with rust-toolchain.toml, requires the backend to select that variable, and rejects an unqualified floating nightly selector. Prove the observed ARM64 compatibility pair, run Dockerfile syntax validation and a complete local production-image build, validate OpenSpec strictly, obtain history-free critic and judge approval, and commit a replacement candidate before returning the parent to a fresh 10,800-second certification.

EXECUTION ROUND ORDER
Round 1 (serial): fix-container-rust-toolchain-pin-consistency

PERMITTED SURFACE
- Dockerfile
- scripts/verify-runtime-image-toolchain-pin.sh
- openspec/changes/fix-container-rust-toolchain-pin-consistency/**
- .kbd-orchestrator/phases/perform-the-soak-run-candidate-tag-external-installs-and-ga-promotion/children/fix-container-rust-toolchain-pin-consistency/**
- .prometheus/gotchas.md
- .prometheus/decisions.md
- .prometheus/session-log.md
- Canonical KBD projections generated by prometheus kbd

VERIFICATION ORDER
1. Preserve the source-bound Docker cargo +nightly / diskann-wide E0283 failure and the isolated floating-nightly negative control.
2. Write the OpenSpec proposal, runtime-image-toolchain-consistency delta, design, and tasks; validate strictly and obtain independent artifact-only critic and judge approval before Execute.
3. Change Dockerfile only at the backend toolchain selector and run the local pin-consistency check immediately.
4. Run bash -n on the check, its positive case, and a contained mutated-input negative control that must fail.
5. Run the pinned nightly-2026-07-18 ARM64 diskann-wide 0.54.0 check.
6. Run docker buildx build --check -f Dockerfile . and the full source-bound production docker build locally. Do not use GitHub Actions.
7. Run repository Tier 0 only as required by the Dockerfile/script surface; no Rust source changes are planned, so do not spend time rebuilding unrelated host Rust units outside the image proof.
8. Write row-form verification evidence, strict OpenSpec result, scoped diff checks, and artifact-refiner evidence.
9. Commit the child change, complete Execute and Reflect canonically, restore the parent with a new immutable SHA, and restart the parent certification from zero.

STOP CONDITIONS
- Stop if the dated channel itself must change, diskann-wide must be upgraded or patched, Cargo.toml/Cargo.lock must change, or another production Docker command uses a distinct Rust selector.
- Stop if RUST_TOOLCHAIN is unavailable in the derived builder stage or the explicit selector does not use the installed dated toolchain.
- Stop if the complete production image fails after the selector repair for a cause not already covered by this child.
- Stop if implementation requires GitHub Actions, deployment changes, public APIs, feature changes, or edits outside the permitted surface.

EXPLICIT SCOPE CUT
Do not upgrade Rust, diskann-wide, SurrealDB, Wasmtime, or any other dependency. Do not broadly rewrite the stale gke-deployment capability in this release child; add a narrow current runtime-image toolchain contract and record the older capability conflict for later reconciliation. Do not run the parent three-hour soak until the child is committed and reflected.

COMMANDS TO RUN
/opsx:new fix-container-rust-toolchain-pin-consistency
/opsx:apply fix-container-rust-toolchain-pin-consistency

PLAN COMPLETE
