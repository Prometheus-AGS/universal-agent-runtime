# Runtime Console UX Plan

1. Refresh OpenSpec and install shared KBD workflow support.
2. Fix frontend entity/typecheck blockers.
3. Extend runtime entity types and schemas.
4. Add realtime ingestion scaffolding for AG-UI/A2UI/run/provider events.
5. Replace the current admin-first shell with a compact runtime console shell.
6. Add focused runtime cockpit screens for runs, approvals, providers, memory,
   skills/MCP, and protocols.
7. Validate OpenSpec, frontend checks, and targeted backend checks.
