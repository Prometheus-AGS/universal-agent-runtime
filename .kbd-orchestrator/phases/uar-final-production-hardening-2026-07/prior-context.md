# Prior Context — active execution filter

Historical context is available in Git history but is not part of the active work queue. Apply the operator execution lock in `current-waypoint.json`; use only changes 20–24 and their current evidence/publication requirements.
