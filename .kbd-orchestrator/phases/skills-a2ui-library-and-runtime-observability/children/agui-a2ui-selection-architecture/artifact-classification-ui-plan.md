# Presentation artifact classification repair

Phase: Execute. Scope: selection-host-snapshots integration defect, before the separate provenance workspace. Mode: Operate/Read. Target: `frontend/src/stores/chat-message-store.ts`, the existing display-artifact projection. No new page, visual world, dependency or chat-state migration.

## Observed defect and exit

The AG-UI adapter sends every display artifact to the synthetic __a2ui_display__ projection. That projection currently creates an A2UI chunk unconditionally. A text-mode effective-policy artifact is valid JSON without an A2UI profile; the current UI incorrectly reports a missing-profile rendering error. The host already rejects model-controlled reserved synthetic tool names before tool execution.

Classify declared artifact type/language/profile at the existing projection boundary. Explicit A2UI declarations retain the current validated A2UI path, including visible rejection of malformed/incompatible A2UI. Non-A2UI output becomes an ordinary read-only artifact: application/json for declared JSON, text/plain otherwise. Do not infer executable HTML, images, downloads, surfaces or actions from source contents. Preserve exact artifact ID, title and content in the existing canonical message.content and corresponding derived chunk, so ordinary session persistence/reload retains it. Do not keep a second business-state cache or publish only a transient runtime chunk. No rendered/displayed claim is added.

## Task-specific skill distillation

Declaration precedence is explicit: artifact type `a2ui`, `display`, or `a2ui/*`; language `a2ui`, `application/a2ui+json`, or `application/vnd.uar.a2ui+json`; or a declared profile beginning `uar.a2ui/` always selects A2UI validation, even alongside language `json` or a missing/incompatible profile. These forms come from the existing host gate, stream adapter and canonical MIME. Generic JSON never overrides explicit A2UI intent. Content is not sniffed. Ordinary JSON classification accepts declared type/language `json` or `application/json`; all other ordinary declarations remain inert text.

Canonical artifact titles require a backward-compatible optional `title` field in shared/content/content-block.ts and propagation through shared/content/to-chunks.ts and any actual decoder that reconstructs that field. The chat-message-store writes the original title, ID and source into canonical content and its derived ordinary artifact chunk. Do not wrap or rewrite the original source. This bounded contract addition preserves old records without titles. Verify canonical-only reconstruction as well as ordinary persisted chunks at phase end.

Impeccable4.2.0 Operate mode takes precedence: retain incumbent KnowMe Flat2.0 tokenized surfaces, Geist typography, ordinary JSON source affordances and the current component hierarchy. Audit and dual isolated critique examine this narrow data-to-view decision before implementation; a fresh adversarial review checks the resulting contract. No fabricated visual score or browser acceptance is permitted before the user's phase-end test boundary. frontend-design supports intentional consistency rather than a new visual treatment. UI/UX Pro Max's targeted UX database returned Error Messages (announced errors), Heading Clarity and Submit Feedback; only accurate/error-distinct feedback is relevant here, not new forms or typography. Vercel React guidance keeps derived display state out of effects; Composition Patterns favors the existing explicit artifact/A2UI variants. Prometheus entity guidance forbids new parallel business records, component I/O or graph imports: this repair changes only the existing message projection and preserves its canonical serialized artifact. Full provenance records in task1.3 will use typed platform domain hooks.

The active prior-context.md is the existing unavailable-memory stub. Impeccable context was already run for this continuous phase session and is not repeated. frontend/PRODUCT.md records the operator workspace, reusable template decision and phase-boundary testing constraint; the repository-root PRODUCT.md is absent. This narrow refinement proceeds from that context and incumbent source, not a new initialization or redesign. The ux-designer skill remains unavailable; the explicit independent critics supply the review perspective without inventing that skill. The project UI roster is consulted; devtools/Chrome research is not applicable to this artifact flow.

## Verification boundary

Tier0 after implementation: pnpm typecheck and pnpm lint, plus scoped diff check. No tests run now. At whole-phase end: text-mode JSON policy remains readable before/after reload; real and malformed A2UI still render/reject through the validator; reserved tool names cannot create forms; keyboard/zoom/narrow-screen review uses the existing artifact renderer. The uncomfortable limit is that routing readable JSON is not the planned human-readable Presentation provenance UI, and it does not prove client display or persistence until those phase tests run.
