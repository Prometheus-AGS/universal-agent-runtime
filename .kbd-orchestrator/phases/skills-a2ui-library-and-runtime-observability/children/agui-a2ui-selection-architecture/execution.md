# EXECUTION: agui-a2ui-selection-architecture

Project: Universal Agent Runtime. Date: 2026-09-04.
Selected backend: openspec. Dispatched to: SELF (Codex/Astra).
Rationale: three existing KBD change identities now have explicit spec-driven artifacts and inspectable tasks. Canonical progress remains KBD; OpenSpec checkboxes record implementation evidence.
Entrypoint: openspec-apply-change establish-presentation-catalog-workspace.
Source plan: plan.md in this directory. OpenSpec available: YES.

## Scope and ledger

- IN_PROGRESS: establish-presentation-catalog-workspace — contract/storage, API, typed graph/UI, acceptance.
- PENDING corrective work: scope-presentation-capabilities — scoped policy, host/delegation, controls, acceptance.
- PENDING execution: select-and-observe-presentations — negotiation, host projection, observable UI, acceptance.

All work is self-executed; isolated artifact critics review artifacts without generation history. No publication or deployment actions are authorized by this execution contract. Preserve other contributors' dirty worktree edits.

## Gates and verification

The user answered the Presentation domain question. No domain blocker remains. Independent plan findings are recorded in plan-review.md and incorporated in the plan/spec. All three OpenSpec changes validate. UI implementation still requires the ordered project design routing, isolated critiques and fresh adversarial review. This is not waived by backend implementation.

Tier 0: compilation/static checks after each cohesive edit. Tests only at the end of the complete phase, including the ownership/persistence, negotiation, every-publication-path and browser cases in plan.md. Acceptance tasks remain unchecked until their evidence exists. Run kbd-status after every completed task/change/phase. Do not archive before independent review and actual exit evidence.

## Reflection handoff

Compare code and observed results with goals.md, the plan, and the three spec deltas. Preserve deferred real-provider 429 coverage, existing runtime archive limitations and the cancelled release-tail decision. A compile pass proves compilation, not a working Presentation workflow.

EXECUTION READY

## 2026-09-04 — Catalog implementation checkpoint

Delivered owner-scoped Presentation templates, safe instantiation, memory/PostgreSQL/Surreal persistence and migrations, and authenticated revision-aware host CRUD. GET catalog includes the verified owner even for an empty result. The owner envelope admits the frontend cache without decoding credentials as authority.

Independent backend review identified root-patch incompatibility, host/client schema mismatches and repeated-child expansion. Instantiation now emits escaped top-level data patches; templates require the first-party profile, matching component constraints and one parent per non-root component. Follow-up review found explicit null action context and executable text in generated data-key pointers; both are rejected. These guards trace to the template/renderer trust boundary and concrete reviewer examples.

Tier 0 `cargo check --locked --no-default-features --features server-full` passed in 53.04s after the first corrections and 20.51s after the null-context/key corrections, without warnings. An intermediate attempt failed because regex was not a declared crate; the implementation was corrected to use standard-library checks without adding dependencies. PostgreSQL/memory feature and live persistence behavior are not yet verified.

The production workspace now has normalized records, graph-owned drafts, authenticated read admission, one-shot writes, search, editor, local preview, conflict/uncertain-write recovery and production navigation. A2UI testing remains development-only. Public field hooks own narrow subscriptions; one atomic ingestion publishes fetched rows. The pure protocol was extracted with a compatibility export, and the renderer gained an optional DOM ID prefix so preview controls cannot collide with editor fields.

Impeccable 4.2.0 and UI/UX Pro Max informed the approved plan and source reviews. Independent implementation findings/dispositions are in ui-implementation-review.md. Typecheck and lint passed after domain and full workspace integration; one type-index error and four forbidden outline variants were corrected. The repository-wide boundary command exits 1 with 16 findings in untouched Providers/Settings code; none are in this task's files. Final focus/exit-protection repeat checks are pending in this checkpoint.

No tests, browser captures, build/package workflows, commits, publications or dependency updates were run for this Presentation phase. Catalog acceptance remains pending alongside scope and negotiation implementation. Archived real-provider 429 deferral, coverage limits and cancelled release-tail decisions remain unchanged.

Final catalog source correction gate: `pnpm typecheck && pnpm lint` exited 0 after the disabled-target focus fallback and admission-independent exit-warning fixes. Task catalog-production-workspace is implementation-complete; catalog-phase-acceptance is not complete. Continue scope policy code, then selection code, before the consolidated phase-end test/visual sequence.

## 2026-09-04 — Scoped policy contract

Added Presentation ResourceSelection to RunPolicy, enabled-ID universe and immutable EffectiveRunPolicy; agent extensions merge it and ordinary scope resolution intersects it. Existing selected-resource warnings now also record requested IDs excluded by restrictions, keeping missing-ID warnings unchanged. Old requested policies inherit; old effective snapshots deserialize to empty/None Presentation authority. Delegated intersections, concrete remote serialization and kernel binding admission retain/check the captured Presentation ceiling.

Tier 0 `cargo check --locked --no-default-features --features server-full` exited 0 in 1m01s without warnings. An isolated artifact critic found no concrete defect in this bounded policy contract. Host universe constructors remain closed until task scope-host-eligibility; no claim of selectable templates yet. Session DTO preservation and assignment UI follow. Tests still wait for phase end.

Registering corrective tasks under the historically completed scope change re-derived the canonical implementation ledger to109/120 and the parent to0/3. This exposes the observed missing implementation; it does not discard completed catalog code. Do not repair the count by marking unsupported historical work complete.

## 2026-09-04 — Verified host eligibility checkpoint

Host catalog admission now uses verified subject plus tenant, validates owner/content, excludes disabled records and fails closed on catalog/global-policy/conversation-policy read errors. Root runtime, remote constraint and discovery policy paths pass verified context; inherited children retain captured ceilings. The strict settings admission getter propagates database/deserialization errors instead of turning them into missing global restrictions.

The independent critic exposed a collision between encoded principals and unrestricted legacy subject strings in a shared policy namespace. Principal conversation policies now have a separate memory map, PostgreSQL table/migration and Surreal table/migration. Surreal principal operations do not invoke legacy migration helpers or swallow missing-table failures. A nonnumeric cache prefix separates verified cache keys from numeric length-prefixed legacy keys. Legacy rows are untouched and cannot grant Presentation intent. Durable reset markers shadow legacy values; redundant agent-config fallback is removed. A verified reset without persistence returns503 rather than claiming success. Both editor GETs propagate read unavailability as503 instead of successful stale/null responses.

Tier0 `cargo check --locked --no-default-features --features server-full` exited0 in1m03s after namespace changes and20.42s after editor-read corrections, without warnings. Scoped `git diff --check` exited0 with no output. Final independent source review cleared the concrete findings. These are compilation/source receipts only: supported database migrations, owner/reset regressions, functional UI and all phase tests remain unverified until phase end. No unrelated features, dependencies, releases or deployments were added. The new guards protect verified identity and persisted policy trust boundaries. Assignment DTO preservation and graph-backed controls are next.

## 2026-09-04 — Assignment implementation, session slice

Task scope-assignment-workspace remains IN_PROGRESS. Delivered code, not whole-task acceptance:

- `src/uar/api/discovery.rs`: session Presentation wire field; explicit Inherit versus omitted/null preservation; flattened conversation update DTO; principal write baselines;409 for conditional conflicts; no-persistence session cache preservation under one lock.
- `src/uar/persistence/mod.rs` and providers `{memory,postgres,surreal}.rs`: conditional principal policy writes; conditional global Presentation-field writes against the full stored JSON; conditional merged-agent writes. Memory uses one lock; PostgreSQL conditions raw JSON updates; Surreal conditions record content. Raw agent baselines accommodate omitted legacy defaults. Surreal first-insert CREATE refuses overwrite; a backend/uncertain error is not mislabeled as a proven conflict.
- `src/uar/persistence/presentations.rs`: validated global field overlay preserves unrelated JSON fields and rejects active IDs outside Selected mode. `src/uar/settings/manager.rs`: admission reads committed storage instead of potentially stale cache; global conditional update refreshes ordinary settings cache from committed storage.
- `src/uar/api/settings.rs`: admin-protected GET/PUT `/presentation-policy` below `/api/uar/settings`; GET `{policy}`, PUT `{expected_policy,presentations}`,409 on changed/missing baseline,422 for invalid intent and redacted unavailable/uncertain errors.
- `src/uar/domain/agent_store.rs`: merged PATCH uses conditional persistence and a typed conflict; discovery maps conflict to409. Full replacement endpoint semantics are unchanged.
- `frontend/src/platform/entities/presentation-assignments/contracts.ts`: typed selection schema/copying. Session configuration contracts/API/domain/hooks/index carry assignment, inactive draft IDs, current-owner admission and narrow subscriptions. Saves omit Presentation unless dirty, including agent selection. Admission errors have separate generation-guarded state; uncertain POSTs block further saves/edits until explicit saved-state reconciliation. A confirmed POST commits independently of the derived prompt-caching read. The session error summary receives focus after failed submit.
- `frontend/src/features/presentations/ui/presentation-selection-control.tsx`, feature index and chat session-config panel: modes/help, search, requested counts, unavailable selection removal, exclusions in every mode, explicit reset, new-tab template management, reload and uncertainty recovery. All controls use public platform hooks; no new business useState.
- Agent admin store now sends the editor's known fields through PATCH; the transport encodes the agent ID. This preserves independently assigned run-policy extensions instead of dropping them via full replacement. It does not rewrite the existing unrelated agent-store architecture.

Impeccable4.2.0 dual critiques and fresh adversarial review revised assignment-ui-plan.md before UI code: inactive selected IDs stay in draft metadata, global writes require true conditional preservation, Automatic/All explanations differ, global owner-versus-instance consequences are explicit, and the template-management link cannot discard session drafts. Subsequent source review identified and cleared late-admission save unlocking and missing uncertainty reconciliation. The final focusable-summary correction passed the same static gates. No browser score or visual acceptance is claimed.

Observed Tier0 receipts: backend session DTO check exited0 in26.32s; conversation DTO check1m02s; conditional conversation check1m04s; global API/persistence check1m48s; cache correction27.29s; final agent conditional check1m19s, all without warnings. `pnpm typecheck && pnpm lint` exited0 after contracts/domain, after session UI, after recovery corrections and after the focus-summary fix. Scoped `git diff --check` exited0 with no output. No tests ran. No dependency pin, build/deployment workflow or publication changed.

The uncomfortable remaining work: standalone agent/global assignment graph records and panels do not yet exist; selection negotiation, frozen template snapshots, all publication paths and provenance UI are also not implemented. All database execution, migrations, compatibility/concurrency regressions, browser behavior, Impeccable polish/finish and DESIGN.md remain phase-end evidence. Update Rust/TS test fixtures for the new DTO/methods at that boundary; old agent-admin tests still expect full-replacement calls and need the intended merge expectation. The existing16 unrelated Providers/Settings boundary findings and archived429/coverage limitations are not cleared by these passes.

Canonical position after this checkpoint: revision2335,109/120 overall, parent0/3; catalog3/4 tasks, scope2/4 with assignment in progress. Exact next work points to standalone agent/global assignment UI, then negotiation. Do not mark task1.3 or either acceptance task complete from this session slice.

## 2026-09-04 — Assignment task implementation complete

This checkpoint supersedes the preceding standalone-panel gap, without rewriting that receipt. Task scope-assignment-workspace now has all three planned surfaces; acceptance remains pending.

- `frontend/src/platform/entities/presentation-assignments/contracts.ts`: owner/scope-partitioned assignment, draft and runtime view records. `api.ts`: authenticated one-shot reads/conditional writes, redacted errors and uncertainty classification. `domain.ts`: admitted graph drafts, retained inactive IDs, reset, save/reload/discard/recovery, preflight conflict detection, generation-aware admission and explicit stale-read retry states. Runtime sets hold only operation/draft identifiers, not business records. `registration.ts`: schemas and read-only transport; no queued mutation transport. `use-presentation-assignments.ts` and index: narrow typed subscriptions/actions. Platform index and entity bootstrap register/export this domain.
- `frontend/src/features/presentations/ui/presentation-assignment-panel.tsx` and feature index: scope-first controls, mode explanations, owner/instance warning, search, unavailable-ID removal, active exclusions, reset, dirty navigation retention, explicit discard, uncertainty reread, global narrowing confirmation and focusable save/admission errors. Agent detail and Settings registry/navigation/custom-panel admission expose the controls. Settings adds a scrolling container, not a console redesign.
- `src/uar/api/discovery.rs` and `src/server.rs`: strict GET `/api/agents/{id}` returns only persisted intent,404 when absent and503 on unavailable storage; legacy list fallback remains unchanged. HTTP execution resolves persisted artifacts before built-ins, including the persisted default for legacy unknown-ID fallback, and propagates storage failures. Source review demonstrated that the earlier built-in-first path ignored saved Presentation restrictions. Session-agent/effective-config callers carry the same fallible result.

Independent UI source review identified preflight admission loss, erased inactive selections, overbroad foreign-ID warning wording and a stale-load verification state. All were corrected and cleared on re-review. The failed-save focus path also reaches the unavailable-state paragraph when admission loss unmounts the footer. Backend review cleared the strict-read and persisted-first HTTP correction. Impeccable4.2.0/Flat2.0 lint rejected three line separators; they were changed to surface fills and spacing. No visual score is claimed. UI/UX Pro Max guidance continues to inform labels,44px targets and recoverable error states.

Observed verification: strict-read Cargo check initially exited0 with one unused routing import warning; the import was removed immediately. Repeat `cargo check --locked --no-default-features --features server-full` exited0 in26.37s without warnings; after persisted-first execution correction it exited0 in28.74s without warnings. Frontend `pnpm typecheck && pnpm lint` passed after data/hook registration, then the initial panel lint failed on the three separators; integration/correction passes and the final admission-focus pass all exited0. Scoped `git diff --check` exited0 with no output.

No tests, browser, releases, deployments, dependency additions or unrequested product features were added. Guards trace to the owner/policy boundary and concrete source-review failures. Surreal/PostgreSQL/memory execution, migrations, uncertain/conflicting/interleaved mutations and live keyboard/responsive appearance remain unverified until whole-phase end. The16 prior unrelated boundary findings and archived real-provider429/coverage warnings remain. Negotiation, frozen snapshots, governed publication and provenance are next; neither acceptance task is complete.

## 2026-09-04 — Negotiation contract implementation checkpoint

Registered the four existing OpenSpec selection tasks under their original change. Two registration commands initially collided at the causal frontier because their shell processes overlapped; they were rejected without modifying the conflicting task. Retried those registrations serially against the current frontier. All four tasks are now recorded; revision2342 marks selection-negotiation-contract in progress. No projection was hand-edited.

`src/uar/a2ui/presentation_selection.rs` defines optional presentation_mode/client_rendering, an explicit Legacy effective mode, compatible-profile resolution, fallback reasons, a non-widening parent text ceiling and output guidance that never asserts display. The module export and `RunExecutionRequest` carry this typed contract. Primary chat, the OpenAI adapter, native run/resume DTOs and ACP parsing preserve it. Manager context records negotiation and admission selection separately from publication. A2UI interaction continuation restores negotiation; invalid saved values return an error instead of becoming Legacy.

The selection-contract.md artifact passed independent source review before code. Tier0 `cargo check --locked --no-default-features --features server-full` exited0 in2m01s after the pure resolver and43.88s after ingress/context wiring, without warnings. Scoped `git diff --check` exited0. No tests ran. Final implementation source review is pending at this checkpoint.

The uncomfortable gap is explicit: accepting these inputs and recording mode intent does not yet gate any output. Full template-content capture, inherited/remote output ceilings, legacy/native/direct/graph policy publication and durable event/UI provenance remain tasks1.2/1.3. Existing a2ui_render returns a premature rendered claim before host publication; that observed receipt defect must be corrected in task1.2. No renderer or run-detail UI was changed in this contract slice.

Final bounded implementation review found no blocking task1.1 findings and traced negotiation through graph-root adapters as well as each ingress above. Task1.1 is implementation-complete; no output-enforcement or durable-provenance claim follows from it. All12 Presentation tasks are now defined/registered, with7 complete (catalog3,scope3,selection1). Tests and UI finish remain at the whole-phase boundary. The overall counter remains109/120 until changes meet their full completion criteria and cancelled release-tail rows are reconciled honestly.

## 2026-09-05 — Frozen host preparation and publication checkpoint

Task selection-host-snapshots is IN_PROGRESS at revision2345. This is partial implementation, not whole-task or phase acceptance.

- `src/uar/persistence/presentations.rs`: validated eligible full records, with owner, enabled/content/id/revision checks; the former ID-only helper projects those records. `runtime/presentations.rs` and runtime module export: immutable owner-qualified contents, identities/revisions, narrowed local-child snapshots with no storage refresh, binding/default catalog data, and pure template preparation using a host-selected surface ID. Run context exposes identities, not content or display claims.
- `runtime/turn/bindings.rs`, `runtime/thread/kernel.rs`, `runtime/turn/resolved.rs` and manager: retain captured templates through root/local-child and normal/shadow turns. Exact owner/tenant identity is required for inherited snapshots. Tools and effective Presentation IDs are narrowed to actual captured resources. Removed renderer IDs force a nonempty Auto/All tool selection to Selected so registry filtering actually applies. Typed ArtifactStage in `runtime/turn/builtin.rs` retains host output-mode instructions; catalog labels/bindings remain retrieved data, not host instructions.
- `runtime/native_skills/presentation_render.rs` and native registry: strict template ID/data preparation, no owner/run override and no writes by the tool. Legacy a2ui_render now says prepared, not rendered. `runtime/native_skill.rs` and `src/llm/orchestrator.rs`: opaque public per-call context preserves external struct-update construction. The trusted execution boundary retains untruncated output under the exact tool-call ID before model-history formatting, then removes surface messages from the model-facing receipt. Sequential/parallel paths carry call IDs; MCP adapter context remains correctly bound.
- `runtime/a2ui_output.rs`, graph tools/transcript and manager: consume the host-owned preparation once, verify frozen identity/revision and validate the entire batch before publication. Captured Text and pre-publication cancellation reject output. Model ToolResult JSON is not a source of surface contents. Publication metadata says published, never client-displayed.
- Manager RunStreamState and event emitters retain the snapshot for non-tool output and replay lifetime. The shared event ceiling rejects A2UI artifacts/input requests/state patches when capture is absent or Text. Reserved model-controlled ToolStart names `__a2ui_input__`/`__a2ui_display__` are rejected before the Legacy/mode early return. Text policy artifacts use JSON without A2UI metadata. `src/uar/a2ui/routes.rs` checks the captured mode before direct submissions, actions, artifact responses and test triggers; replay checks exact owner/tenant without renegotiating. Verified anonymous legacy context retains its existing anonymous namespace.

Observed Tier0 checks: `cargo check --locked --no-default-features --features server-full` passed after capture (1m25s), preparation (1m50s), filtering/prompt correction (27.48s), binding metadata (21.77s), initial publication (1m20s), receipt/context integration (1m38s with one missing-Debug warning), warning correction (1m26s), event/direct gates (1m17s), and reserved projection correction (21.81s). Receipt integration first failed E0061 on an MCP context call site; it was fixed before the successful passes. Final passes were warning-free. Scoped `git diff --check` passed. No tests, browser checks, builds, dependency additions, releases or deployments ran.

Independent source reviews cleared capture, tool filtering/catalog metadata, untruncated host receipt provenance and public context compatibility. Review of the event/direct slice found the reserved ToolStart bypass (now corrected) and a still-open frontend adapter defect: `chat-stream-store.ts` routes every agui.artifact to A2UI, so the new JSON policy artifact displays a profile error instead of readable policy data. The latter must be corrected through the mandatory UI routing, not counted as accepted. Cancellation/replay/database regressions and actual large-surface execution remain unverified until phase end.

The remaining work is concrete: review and implement authenticated UAR-to-UAR output negotiation/ceilings without falsely claiming compatibility for older non-concrete resource contracts; fix chat artifact classification; add durable requested/effective/publication provenance and narrow UI hooks; then whole-phase tests and Impeccable4.2.0 polish/finish with UI/UX Pro Max. Remote design is appended to selection-contract.md but no remote transport code changed in this checkpoint. Existing429/coverage warnings, unrelated16 boundary findings and cancelled release-tail rows remain. No unrequested product features were added. New guards protect real owner, protocol, event-projection and host-receipt boundaries; no speculative retry/fallback was introduced.

## 2026-09-05 — Remote ceilings and ordinary artifact projection

`api/a2a/contract.rs` now preserves wire presence of the Presentation policy field and historical typed serialization order. Negotiation participates in contract digest/equality; explicit null is rejected rather than treated as an omitted legacy extension. Concrete resource validation includes explicit Presentation selections. Target execution overlays None only on a separate copy of a received legacy policy. Outgoing construction selects the historical wire only for absent negotiation and concrete zero Presentation authority; real grants/restrictions are never stripped or retried unconstrained. `runtime/presentations.rs`, thread kernel/service, A2A thread service and governed actor system/host carry frozen parent/child output restrictions into every target turn. Source content is not copied into a peer catalog. Historical digest and real-peer interoperability remain phase-end verification, not proven by compilation.

`frontend/src/stores/chat-message-store.ts` now classifies display artifacts by explicit declarations: A2UI type/language/profile wins over generic JSON even if invalid. Other artifacts use only escaped JSON/plain-text views. `shared/content/content-block.ts` adds optional title, `shared/content/to-chunks.ts` projects it and `platform/pglite/legacy-chat-content.ts` preserves it on decode. Canonical content and derived ordinary chunks retain original artifact ID/title/source. The host `runtime/a2ui_output.rs` gate now recognizes the same A2UI type prefix and canonical MIME, preventing Text-mode mismatch. This is a real event/projection trust-boundary fix, not speculative fallback.

Impeccable4.2.0 audit and isolated dual critique identified two P1 contract issues (classification precedence and canonical title); the corrected plan and four-file implementation passed fresh adversarial source review. Source-contract score22/32 excludes interaction and browser acceptance. Detector scans of the two incumbent chunk views returned[]/exit0; no visual score or overlay is claimed. UI/UX Pro Max error-clarity guidance informed the narrow repair. The full critique is persisted under .impeccable/critique/2026-09-05T05-16-08Z__frontend-src-stores-chat-message-store-ts.md. No new UI surface/style/dependency was added.

Tier0: remote transport compilation passed in2m09s; outbound zero-authority legacy selection passed in2m40s; host classifier alignment passed in21.21s. Each used `cargo check --locked --no-default-features --features server-full`, exit0, no warnings. `pnpm typecheck && pnpm lint` exited0 after the four-file frontend repair. Scoped `git diff --check` exited0 with no output. No tests or browser verification ran. Task1.2 remains in progress until the final bounded host-path source review; task1.3 durable provenance/UI and all three acceptance tasks remain. No unrequested feature or release work was added. Existing429/coverage warnings and cancelled release rows are preserved.

Final task1.2 source review found no implementation blockers. KBD revision2347 completed selection-host-snapshots; revision2348 refreshed next work and kbd-status reported8/12 Presentation tasks,0/3 changes,109/120 overall. OpenSpec task1.2 was checked with the same implementation-only evidence. Revision2349 started selection-provenance-workspace. Acceptance tasks remain unchecked.

## 2026-09-05 — Host provenance and replay implementation checkpoint

Task1.3 remains IN_PROGRESS. provenance-contract.md defines client run-event durability, not backend restart persistence, and exact publication-versus-display semantics. The backend design review identified bounded-history loss, subject-only stream authorization and early stream termination on renderer diagnostics; all are addressed in this code slice.

- `domain/events.rs`, `api/adapters.rs`, `api/sse.rs` and `runtime/a2ui_output.rs`: PresentationDiagnostic is a nonterminal CUSTOM extension. Surface/receipt/reserved-projection rejections no longer falsely end a continuing kernel run. Genuine Error remains terminal. Renderer failure evidence is recorded in `runtime/native_skill.rs` after actual execution failure, or in the publication helper only after a host preparation was consumed; provider-originated ToolResult success flags and missing receipts cannot manufacture that evidence.
- `runtime/presentations.rs`: history-owned version1 PresentationObservation separates requested/effective mode, admission versus terminal fallback, running/finished/failed/cancelled, eligible frozen identities, actual published template identities/revisions, generated surface publication and unconfirmed client display. Host-held publication receipts are written only after the validated consumed batch publishes. The policy summary does not count as a generated surface. A poisoned receipt store is explicitly unavailable, never an empty success result.
- `runtime/manager.rs`: EventHistory.record is shared by emitters and emit_to_run. Ordinary publication observations follow the actual event; terminal provenance precedes terminal closure. Emit complete /presentation state records only on relevant observation changes. Preserve the latest full record and assigned event ID beyond the512-event ring; merge it into history in sequence without inventing another ID. Generic producers cannot write the reserved key. Root object replacements without it are followed by restoration; destructive/nonobject roots are rejected at the host boundary.
- `api/routes.rs` and manager get_run_for_context: stream authorization compares middleware-verified subject plus tenant without requiring a successful Presentation snapshot. Authorized anonymous and pre-capture failure streams remain possible.
- `api/sse.rs`: replay selects only provenance at/before the requested cursor. When unrelated state reconstruction fails, emit the independently retained full record as CUSTOM uar.presentation.snapshot with a dedicated stable event-ID suffix; do not claim synchronized global state. If historical evidence is unavailable, do not backfill future state.

Observed Tier0 `cargo check --locked --no-default-features --features server-full` passes, no warnings: nonterminal diagnostics1m55s; exact-owner stream gate39.35s; host observation/history1m48s; independent replay28.00s; source-review corrections33.66s. Scoped `git diff --check` passed. Independent final backend recheck found no remaining blocker in this slice. No tests/browser/acceptance ran; eviction/cursor/terminal ordering and forged-result regressions remain mandatory phase-end evidence.

UI direction is ready, not implemented: provenance-ui-plan.md adds one read-only Presentation section to the existing run inspector through a new typed normalized platform domain. Impeccable4.2.0 isolated dual critique yielded27/40 as a provisional design-contract score only; detector on incumbent inspector returned[]/exit0. Two findings were corrected: latest-run versus selected-event time scope, and permission versus actual output wording. Disclosure/retry use44px targets and visible focus. UI/UX Pro Max's contextual-status guidance recommends one meaningful announcement without focus movement. Fresh adversarial artifact review found no blockers. Full critique saved at .impeccable/critique/2026-09-05T05-34-02Z__frontend-src-features-chat-ui-run-inspector-tsx.md; temporary body removed. No new provenance UI/domain files exist yet.

Next: implement platform/entities/presentation-provenance typed decoder, transport subscription to existing PGlite run-event snapshots, atomic graph domain/actions and narrow hooks; mount presentation-run-details in run-inspector. Decode STATE_DELTA, STATE_SNAPSHOT and CUSTOM uar.presentation.snapshot with version/run/sequence correlation; distinguish missing/unsupported/error/pending from actual empty receipts. Then task1.3 source review/status, all phase-end tests and bounded Impeccable polish/finish. All12 tasks remain defined,8complete; no narrowed goal or extra release scope. Preserve429/coverage caveats and cancelled-tail reconciliation.

## 2026-09-05 — Provenance domain and inspector implemented

Task1.3 implementation is complete; phase acceptance is not. The six new platform/entities/presentation-provenance files provide strict version1 decoding, same-run wire-order projection of STATE_DELTA/STATE_SNAPSHOT/CUSTOM snapshots, a local-history transport, ref-counted generation-guarded subscription actions, normalized schema registration and leaf field hooks. Runtime admission IDs reject stale hydrated records. Whole-record atomic writes preserve unchanged receipt-array identity. Only subscription mechanics live outside the graph. No new remote endpoint or persistence database was introduced.

entities/bootstrap.ts registers the schema; platform/entities/index.ts exports the public domain. features/chat/ui/presentation-run-details.tsx adds the reviewed read-only section; run-inspector.tsx only imports/mounts it. Requested, admitted and run-outcome rows subscribe independently. Latest-run wording avoids selected-event-time confusion; publication receipts remain separate from host permission and client display. Missing, unsupported, loading and local-history error states are explicit; retry and native disclosure retain44px targets and visible focus. Full template IDs/revisions wrap rather than truncate. Incumbent tokens and existing inspector controls remain untouched.

Applied Impeccable4.2.0 craft-floor immediately before UI implementation, with the prior dual critique/fresh adversarial direction and UI/UX Pro Max contextual-status guidance. Vercel React/Composition and entity-management guidance kept subscriptions at rendered leaves and I/O beneath domain actions. Context7 Zod3 documentation verified strict schema/safeParse usage; no dependency changes. Independent domain and UI source reviews found no actionable blockers.

Tier0 evidence: first typecheck failed on an interface not satisfying the graph Record contract; changed the entity declaration to a type alias. Subsequent domain `pnpm typecheck && pnpm lint` exited0, then the UI run of the same command exited0. Scoped `git diff --check` exited0 with no output. No tests or browser checks ran before completion of phase implementation. Next is the consolidated phase-end acceptance sequence: storage/ownership/revisions, policy/negotiation/legacy peers, native publication and replay, frontend domain/component/ordinary artifacts, then browser catalog/assignments/run details and bounded polish/finish review.

No unrequested feature was added. New decoding and safe-integer revision guards protect the persisted wire-data boundary; lease/generation/admission guards address retry, unmount and reload races. Runtime and browser behavior, persistence restart and real-peer compatibility remain unverified. Preserve all deferred429/coverage warnings, unrelated16 boundary findings and cancelled release-tail decisions.

## 2026-09-05 — Phase acceptance started; gates remain open

Revision2351 completed selection-provenance-workspace,2352 refreshed the waypoint and kbd-status reported9/12 tasks,0/3 accepted Presentation changes,109/120 overall. Revision2353 started selection-phase-acceptance. All tests below ran only after all nine implementation tasks were present. No acceptance task is complete.

New frontend regression files: platform/entities/presentation-provenance/presentation-provenance.test.ts (22passing), features/chat/ui/presentation-run-details.test.tsx (12passing), platform/entities/presentations/presentations.test.ts (28passing in the broad suite), and platform/entities/presentation-assignments/presentation-assignments.test.ts (7passing). These69 tests cover strict wire projection, replay order, missing/unsupported records, subscription lifecycle and stale admission, UI evidence wording/states, authoring validation/catalog transport, and assignment intent/conflict/unknown-write behavior. The UI stale-callback test was strengthened after independent review to assert the old graph record remains idle/null, not merely that the new run's display stays unchanged; its12 tests passed again. Sparse catalog metadata in assignment tests is a domain fixture, not persistence/renderer proof.

Every cohesive frontend edit passed `pnpm typecheck && pnpm lint`. First full unit run (`pnpm -C frontend test --project=unit --maxWorkers=2`) reported400passed/8failed across78files. Corrected three observed test-fixture contracts: nav-destinations.test.ts now asserts the production Presentation route while retaining DEV-only A2UI; session-config-panel.test.tsx mocks the new uncertainty hook and isolates Presentation controls from its prompt-cache tests; agents-admin-store.test.ts expects policy-preserving PATCH and forbids full replacement. Targeted rerun:25passed across3files. Second broad run:436passed/1failed across79files; the only failure was the unchanged run-trace500-event mount budget,101.848ms versus100ms, during concurrent Rust compilation. Do not waive the threshold; recheck without concurrent compilation. One new catalog test initially expected script markup as inert data; the existing protocol explicitly rejects it. Corrected the fixture to ordinary literal interpolation-looking text and added explicit executable-markup rejection, without relaxing production validation.

`pnpm build` exited0,10472modules,11.61seconds. It refreshed static output and emitted four direct-eval warnings from pinned PGlite0.5.4. Vitest reports two unmatched MDX story patterns. The first focused pnpm test invocation automatically reconciled installed packages against the existing lockfile and ran supply-chain checks; tracked package manifests and pnpm-lock.yaml remained unchanged. No dependency upgrade was authored.

Rust phase suite attempt1 exited101 before tests: old UarDelegationContract fixtures in api/a2a/task_execution.rs and tests/mcp_projection.rs lacked the wrapper/new negotiation field. Updated both to explicit zero Presentation authority, converted policy with Into and added negotiation None. `cargo check --locked --no-default-features --features server-full` then exited0 in35.85s without warnings. Attempt2 also exited101 before tests because tests/turn_shadow_parity.rs referenced the now-archived change path. The fixture exists unchanged under openspec/changes/archive/2026-09-04-typed-turn-assembly/parity-report.json; the include path is now corrected. No Rust test pass is claimed. `cargo fmt --all -- --check` exited1 with outstanding phase formatting; no wholesale formatter or cleanup was run.

At the continuation after interruption, no Cargo/rustc writer remained. A third full frontend unit run with maxWorkers1 was started without concurrent compilation to recheck the timing failure. Next: finish that receipt, add explicit host snapshot/publication/negotiation/legacy-peer/replay and persistent-backend regressions, correct scoped Rust formatting, then consolidated Rust tests and browser catalog/assignment/run-detail workflows with bounded Impeccable4.2.0 polish/finish and UI/UX Pro Max.69 focused frontend passes and a production build do not establish durable backend restart, actual host publication, real-peer compatibility, browser layout or accessibility.

## 2026-09-05 — Frontend phase suite green; Rust test targets compile

The third full frontend run, `pnpm -C frontend test --project=unit --maxWorkers=1`, exited0:80 files and444 tests passed in171.98s. No concurrent compilation ran; the existing100ms mount budget and production implementation were not changed. This supersedes the pending third-run receipt, not the browser acceptance requirement.

After correcting the archived parity fixture include, `cargo check --locked --no-default-features --features server-full --tests` exited0 in4m03s. This is test-target compilation, not a Rust behavioral pass. The new runtime/presentations_tests.rs cohort targets immutable admitted content, tenant isolation, single-use host preparations, atomic batch validation, cancellation, publication receipts and output ceilings. Its initial check exposed that the real in-memory provider is feature-gated out of server-full; providers/mod.rs now also exposes it under cfg(test), leaving production feature selection unchanged. Corrected cohort compilation passed in1m04s. Independent artifact review found three test-evidence gaps: ordering asserted only after publication returned, missing artifact classification branches, and foreign/stale identity not exercised through publication. Strengthened those assertions; recheck and actual test execution remain pending. No acceptance task or overall counter advanced.

## 2026-09-05 — Browser-observed create callback defect; bounded correction plan

The isolated local browser rendered an authenticated empty Presentation catalog. Clicking either shared New Presentation control did not open the editor, with no console error. Source establishes the mechanism: Button forwards the click event to onOpen; PresentationsPage.open treats its optional argument as a template ID; the owner-safe domain correctly rejects that nonexistent ID. Correct only NewPresentationButton to call onOpen without event arguments; do not weaken the domain guard. Add presentations-page.test.tsx assertions for both creation controls and preserved existing-row identity. Impeccable polish prioritizes this blocked task; its incumbent-world/craft-floor guidance keeps visuals and copy unchanged. Previously consulted Vercel React/Composition and Prometheus entity guidance retain leaf subscriptions and domain-owned business state. UI/UX Pro Max interaction guidance requires the primary action to perform its named action, with existing44px focusable controls preserved. Isolated direction critique found no blocker; independent implementation review and live confirmation remain required. No browser acceptance is claimed from a DOM snapshot.

## 2026-09-05 — Phase suite and browser correction receipts

The full `cargo test --locked --no-default-features --features server-full` run exited0. Observed: library744passed/1ignored; BDD9scenarios49steps passed; broad integration94passed/1ignored; doctests26passed/17ignored. The new SurrealKV catalog contract passed ownership, stale revision, concurrent-writer and four-process seed/update/delete/empty restart checks. Added host selection, frozen snapshots, actual publication/replay ordering, strict receipts, output ceilings, history eviction/cursors, tenant authorization, SSE fallback and old-wire digest regressions passed. The old-wire oracle is a hand-authored compatibility fixture, not a live old-peer certification. The first behavioral attempt had six failures: five incorrectly shaped context_strategy test fixtures and one obsolete a2ui_render 'rendered' assertion. Corrected the fixtures to the actual tagged strategy and host preparation contract; no production semantics were relaxed. Scoped rustfmt and full `cargo fmt --all -- --check` passed before the later API-test addition.

New ordinary-artifact regressions passed18/18 in chat-message-store.test.ts, including9new cases. The New Presentation callback fix passed source review, typecheck/lint and all3 page regressions. Actual browser creation saved revision1, browser reload recovered the catalog record, mobile editing saved revision2, and Cancel restored row focus. Agent selected-template intent and global None both saved and read back; the global restriction required confirmation and was reset to inheritance. Conversation selection reappeared on reopening configuration. Desktop1440x1000 and narrow390x844 captures are in /tmp/uar-presentation-evidence.gBZ6BG. No horizontal document overflow was measured at390px; editor save/cancel/preview are reachable. No screenshot establishes numerical contrast, zoom or every error state.

Independent Impeccable polish review found the closed Assignment mode selector displaying wire values. Added Base UI Select items mappings in presentation-assignment-panel.tsx and presentation-selection-control.tsx, preserving all existing option text, dynamic inheritance/exclusions and domain actions. Context7 primary Base UI docs verified this mechanism. Dual direction review and fresh artifact review cleared the production mappings. Six closed-label regressions passed after strengthening inheritance assertions to exact text; typecheck/lint passed. These are bounded local defects, not a console redesign. The neighboring memory-control mobile clipping is outside this feature and was not changed.

Four route regressions were added in api/presentations_tests.rs with cfg(test) registration: anonymous/inconsistent principals, owner CRUD/revisions, same-subject cross-tenant denial, and invalid/forged-owner drafts. Independent review strengthened the stale update to submit different content before asserting no overwrite. Their compilation/execution is pending in the serialized Cargo gate. All three acceptance tasks remain open; overall109/120,9/12tasks and0/3acceptedchanges are unchanged.

Browser-fixture isolation incident: main.rs unconditionally loads cwd .env before --env-file. Launching from the repository imported legacy LLM_* values into the temporary database, and a settings DOM snapshot exposed an existing key. No chat request was sent and no original .env was edited. lsof confirmed the server held only /tmp/uar-presentation-browser.x2ZwWo/data.db for persistence, not the operator database. Stopped owned PID36486 with SIGTERM. Retained the contaminated temporary database for investigation; it must not be published. Relaunched with cwd at the isolated directory, explicit test-only LLM_* values and a new clean-data.db; no ancestor .env exists. The user was told to rotate the exposed key. Subsequent run evidence requires this clean fixture. Do not repeat or archive the credential.

PostgreSQL acceptance is still unverified. A disposable loopback fixture was prepared from already-installed image2dbfde49c067, container uar-presentation-phase-pg-20260905, port54379, tmpfs data; it reports PostgreSQL16.15/pgvector0.8.6. No application test ran against it yet. It is not the operator's existing PostgreSQL container. Preserve the one-build-profile rule: server-full does not compile postgres-backend, so do not silently claim PostgreSQL provider coverage from this run. Existing429/coverage, loopback governance-inactive and SurrealKV-close warnings remain; no release/deployment/publication occurred.

## 2026-09-05 — Final acceptance follow-up and inspector correction direction

The four API regressions now pass4/4 (0.01s); `cargo check --locked --no-default-features --features server-full --tests` passed16.00s, and full formatting passed. The first API attempt was3pass/1fail because its inconsistent-context fixture altered raw tenant claims instead of the middleware-verified tenant newtype. Corrected the fixture to mismatch claims.sub and user_id, matching ActorOwner's actual boundary; production authorization was not changed. Final frontend unit run passed462/462 across82files (106.13s), and `pnpm build` passed7.54s with the same four pinned PGlite eval warnings.

The clean isolated host's actual settings were asserted to use only the local stub URL/model/test key without printing credential content. Five HTTP/SSE runs (legacy, text, unsupported profile, A2UI and hybrid) passed strict assertions for admission, fallback, completion, surface publication and frozen template revision receipts. A2UI/hybrid emitted actual artifacts before publication receipt projections; no client display was claimed. These are real host plus deterministic stub-provider tests, not live provider/peer certification or browser negotiation. The browser's existing chat transport remains legacy and does not send negotiation fields. The PostgreSQL-specific application test is now compiling with the additive postgres-backend feature in the same unoptimized test profile, serially after server-full checks; this is the planned targeted provider acceptance, not a supported-profile release matrix.

Impeccable initial run-inspector captures exposed two defects: publication copy omitted the documented 'generated' qualifier (policy summaries deliberately do not count), and at390px the implicit grid sized the inspector to26361px from a long JSON payload. Computed Tabs direction is row, putting payload beside the tab strip. Two isolated UI critiques approved generated-surface wording and a policy-summary exclusion; fresh adversarial review approved the bounded direction. UI/UX Pro Max's Web Horizontal Scroll and Long Token Wrapping guidance calls for a shrinking container with local payload scrolling. Frontend-design keeps truthful operator language and incumbent styling; Vercel React/Composition and Prometheus entity guidance keep leaf subscriptions, typed domain ownership and existing callbacks unchanged. Touch only presentation-run-details.tsx and its tests for copy, run-trace-panel.tsx for an explicit narrow grid, and run-inspector.tsx for shrinking, vertically stacked tabs. No backend contract change, shared Tabs refactor, new state or dependency. Confirm these in one final desktop/narrow round; no acceptance inferred from the pre-correction screenshots.

## 2026-09-05 — Two acceptances closed; PostgreSQL contract passes

Scope acceptance closed at2355 and selection acceptance at2356; kbd-status rendered after each. Catalog acceptance started2357 and waypoint2358 records the remaining work. Overall111/120, Presentation11/12tasks,2/3acceptedchanges. OpenSpec scope/selection validation passes; verification reports retain their limitations. No change has been archived in this continuation. Requested approval for the three new spec sync/archive operations, distinct from the previously approved eight historical changes.

The corrected inspector measures390px document/trace width with342px local JSON pane;1440px desktop document has316px Presentation pane. Tabs compute column and keyboard ArrowRight then Enter activates Timing. Saved run details survive full page reload. Final typecheck/lint passes; targeted inspector21tests pass8.52s; build passes14.20s with unchanged PGlite eval warnings. Independent finish review found no new concrete blocker. Full200% zoom and numerical contrast remain unverified and explicitly recorded, not represented by narrow captures. The viewport override was reset.

The clean browser catalog saved a disposable template at revision1, disabled it at revision2, recovered Disabled after full reload, and removed only that template through the named confirmation dialog. Another reload retains only the separate HTTP live-report fixture. Focus returned to the catalog heading after deletion. These test-only records were never in the operator database.

`UAR_PRESENTATION_TEST_DATABASE_URL=<disposable loopback54379 database> cargo test --locked --no-default-features --features server-full,postgres-backend --test presentation_persistence postgres_catalog_contract_and_reconnection -- --ignored --exact` passed1/1,0failed,0ignored,0.31s after18m45s compilation. This proves the actual PostgresProvider contract, owner/stale-revision/concurrent-write behavior and reconnection in PostgreSQL16.15/pgvector0.8.6. It is not an all-profile release result. The dedicated in-memory integration contract now runs with additive in-memory-backend in the same unoptimized test profile; its result is still pending. Existing memory-backed API/host unit regressions already passed.

Artifact review corrected two evidence attributions in scope verification: the Presentation intersection test covers selected ceilings/exclusions, while parent None is covered by the shared resolver's MCP test; the agent assignment suite is not the conversation-save scenario. Exercised the exact conversation path in the clean browser: existing Inherit mode; changed only Prompt Caching from Inherit to On; saved; reopened; observed On from session override and Presentation Inherit/zero exclusions. Restored Prompt Caching Inherit, saved and reopened; both settings remain Inherit. No implementation fix was required. The source's null-on-unrelated-save payload and atomic host preservation agree with this behavior.

## 2026-09-05 — Presentation implementation and acceptance complete

The in-memory contract passed1/1,0failed in0.00s after6m01s compilation with `cargo test --locked --no-default-features --features server-full,postgres-backend,in-memory-backend --test presentation_persistence memory_catalog_contract -- --exact`. No production fix was needed for any backend contract. Catalog acceptance closed at2359; kbd-status rendered3/3changes,12/12tasks and overall112/120. All three OpenSpec changes validate; their verification reports distinguish source review from executed evidence and preserve warnings. Fresh artifact-only review found no remaining critical errors in catalog or corrected scope reports. The phase has not entered Reflect or archived changes yet: sync/archive approval for these three new changes is still outstanding. No previously cancelled release row was marked passed or reinstated.

Stopped only owned PIDs62544(runtime),33888(Vite),66560(stub), and disposable container uar-presentation-phase-pg-20260905; process/container checks confirmed absence. The container's tmpfs fixture data is gone. Local screenshots and temporary DBs remain; the original contaminated test DB must not be published. Original operator env/data remain unchanged; rotation of the previously exposed credential remains an operator action. No build/test process remains active. No commits, pushes, deployment, release or spec publication occurred. Only bounded observed UI defects, test fixtures and acceptance/history artifacts changed; no unrequested production guard, dependency or adjacent refactor was added.

## 2026-09-05 — Explicit local release deployment request

The operator subsequently authorized release build, install, LaunchAgent restart,
commit and push. Release compilation passed47m41s; web build passed17.92s with
the same four PGlite warnings. Installed the matching binary and bundle with a
private rollback backup; preserved the existing custom config, environment and
plist. UAR restarted as PID18125. Both health/readiness returned200, and the
served index plus11assets matched the build byte-for-byte; later readiness
returned408 at30.010136s and another probe timed out. Do not call this a
consistently healthy deployment. Dependency readiness was already failing
before installation; shared-database restart approval was requested but no
database restart occurred. No inference request or real-provider certification.
See docs/releases/local-install-2026-09-05.md for exact hashes and local gates.
KBD status ran after web build, release build, install and restart checkpoints;
overall112/120 and Presentation12/12tasks remain unchanged. Commit/push are the
remaining authorized actions; Presentation archive approval remains separate.

Source commit d6f4f862 passed all hooks and was pushed successfully to
origin/feat/context-history-integrity with upstream tracking. No PR/tag/GA
publication. GitHub reported14default-branch dependency alerts; retained without
claiming remediation. KBD status ran after commit and push. Installation remains
at PID18125 with liveness observed200, correct served assets and intermittent
readiness. Shared-database restart and consistent-readiness recovery remain
unperformed; they are not disguised as completed deployment acceptance.
