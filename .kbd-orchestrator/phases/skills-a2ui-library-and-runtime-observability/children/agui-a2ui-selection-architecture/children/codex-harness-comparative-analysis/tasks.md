<!-- generated by kbd-runtime; source revision 1537 -->
# codex-harness-comparative-analysis tasks

## context-history-integrity — context-history-integrity

- [x] 1.1 — `tests/context_history_integrity.rs`: an assistant message with two tool calls followed by one tool result is normalized to two results, the (COMPLETE)
- [x] 1.2 — A tool result whose call id matches no assistant call is removed before dispatch (COMPLETE)
- [x] 1.3 — A 60-message history under `SlidingWindow{max_messages: 20}` keeps the system message at index 0 (COMPLETE)
- [x] 1.4 — Two identical consecutive user messages ("continue") both survive `KeepFirstLast` (COMPLETE)
- [x] 1.5 — A 200 KB terminal stdout is recorded with the warning header, original token count, and line count, within the configured byte budget (COMPLETE)
- [x] 1.6 — `TokenService::count(model, text)` uses `o200k_base` for a model name the catalog maps to it and `cl100k_base` for an unknown model; the `le (COMPLETE)
- [x] 1.7 — `resume_run_from_checkpoint` restores `state` and `messages` from the checkpoint instead of a prose input (COMPLETE)
- [x] 2.1 — Add `src/uar/runtime/context/normalize.rs` with `normalize_history(&mut Vec<Message>)` enforcing the three invariants; port the reverse-inde (COMPLETE)
- [x] 2.2 — Add `src/uar/runtime/context/truncate.rs` with `TruncationPolicy::{Bytes, Tokens}` and `formatted_truncate` (middle-out, warning header); at (COMPLETE)
- [x] 2.3 — Apply truncation at the three ingest sites: MCP results, native results, terminal stdout/stderr (COMPLETE)
- [ ] 3.1 — Make `uar::domain::context::ContextStrategy` the only enum; map `TruncateMiddle` to `KeepFirstLast` and `Hierarchical` to `ProgressiveSummar (IN_PROGRESS)
- [x] 3.2 — Delete `estimate_tokens` (`strategy.rs:100-103`); route every count through `TokenService` (COMPLETE)
- [x] 3.3 — Pin the system message: reducers receive `(system, history)` and return `history` only (COMPLETE)
- [x] 3.4 — Remove the content-equality dedup in `apply_keep_first_last` (COMPLETE)
- [x] 3.5 — Call `normalize_history` once, after reduction and before the orchestrator is built (`manager.rs` block) (COMPLETE)
- [x] 4.1 — `resume_run_from_checkpoint` calls `Checkpoint::restore_state` and seeds the run's history; a deserialization failure is an error, not `unwr (COMPLETE)
- [ ] 5.1 — Tier 1: `cargo test --locked --no-default-features --features server-full --test context_history_integrity` (IN_PROGRESS)
- [ ] 5.2 — Tier 2: `cargo fmt --all -- --check` and full `cargo test --locked --no-default-features --features server-full` (PENDING)
- [ ] 5.3 — `openspec validate context-history-integrity --strict` (PENDING)

## fail-closed-tool-arguments — fail-closed-tool-arguments

- [ ] 0.1 — Verify `versions.toml` contains a `jsonschema` entry at `0.49.4` (operator-edited, outside this change's scope); if absent, stop and ask the (PENDING)
- [ ] 1.1 — `tests/tool_call_protocol.rs`: a tool call with arguments `{"path": ` (truncated JSON) produces an `invalid_arguments` result containing the (PENDING)
- [ ] 1.2 — A tool call with `{"path": 42}` against a schema requiring a string produces `invalid_arguments` with the validator message (PENDING)
- [ ] 1.3 — Two `ReadOnly` descriptors with distinct keys execute concurrently; two with the same key run in call order; a `ReadOnly` descriptor with no (PENDING)
- [ ] 1.4 — An MCP tool with `readOnlyHint: true` and `ToolApprovalPolicy::Ask` still pauses for approval (PENDING)
- [ ] 1.5 — Two MCP servers exposing the same tool name produce two namespaced descriptors; two native skills with the same name fail assembly with `Too (PENDING)
- [ ] 1.6 — A descriptor's validator is compiled once per assembly (count `validator_for` calls across ten invocations) (PENDING)
- [ ] 2.1 — Add `src/uar/tools/descriptor.rs`: `ToolDescriptor`, `ToolEffect`, `ApprovalClass`, `Exposure` (`Eager | Deferred | Hidden | ModelOnly`, onl (PENDING)
- [ ] 2.2 — Add `src/uar/tools/validate.rs`: `compile(&Value) -> Result<Validator>` via `jsonschema::validator_for`; `validate(&Validator, &str) -> Resu (PENDING)
- [ ] 2.3 — Adapt `NativeSkill` into a descriptor with `effect: Unknown` by default and an optional `effect()` trait method; adapt MCP tools with `readO (PENDING)
- [ ] 3.1 — Replace the two `unwrap_or_else(|_| {})` sites with `validate`; on error push the typed `invalid_arguments` result and continue the loop (PENDING)
- [ ] 3.2 — Replace `is_parallel_safe_tool` and `is_code_execution_tool` with descriptor fields; delete the prefix lists (PENDING)
- [ ] 3.3 — Implement the read/write-lock gate; keep `.buffered` collection in call order (PENDING)
- [ ] 3.4 — Replace `tool_requires_approval` substring scan with `ApprovalClass` from the descriptor; Cedar decision precedence unchanged (PENDING)
- [ ] 4.1 — `McpRegistry::merge` dedupes `tools` by namespaced name; identical descriptors collapse, conflicting ones return `ToolCollision` (PENDING)
- [ ] 4.2 — Tool list assembly sorts by name and is built from descriptors, not from two ad hoc JSON lists (PENDING)
- [ ] 5.1 — Tier 1: `cargo test --locked --no-default-features --features server-full --test tool_call_protocol` (PENDING)
- [ ] 5.2 — Tier 2: fmt check and full test run (PENDING)
- [ ] 5.3 — `openspec validate fail-closed-tool-arguments --strict` (PENDING)

## deterministic-prompt-assembly — deterministic-prompt-assembly

- [ ] 1.1 — `tests/prompt_assembly.rs`: assembling the same artifact, three matched skills inserted in different registry orders, and the same RAG hits  (PENDING)
- [ ] 1.2 — A retrieved chunk renders inside `Retrieved` markers and its fragment authority is `Retrieved`; a skill body renders inside `Skill` markers (PENDING)
- [ ] 1.3 — The manifest contains fragment ids, hashes, counts, and budgets and contains no fragment body text, credential, or retrieved content (PENDING)
- [ ] 1.4 — The manifest is stored in `Run.context` and emitted as a `turn_manifest` artifact; the existing `effective_run_policy` artifact is still emi (PENDING)
- [ ] 1.5 — An artifact with non-empty `instructions` renders them as a `Host` fragment after policy and before the skill catalog (PENDING)
- [ ] 1.6 — Add `insta` 1.48.0 as a dev-dependency; an `insta` snapshot records the diff between two successive rendered prompts for the same session so (PENDING)
- [ ] 2.1 — Add `src/uar/runtime/prompt/fragment.rs`: `PromptFragment`, `Authority`, `Retention`, markers, `content_hash` (SHA-256 over role, kind, and  (PENDING)
- [ ] 2.2 — Add `src/uar/runtime/prompt/assemble.rs`: fixed section enum, per-section deterministic sort, `render(&[PromptFragment]) -> String` (PENDING)
- [ ] 2.3 — Add `src/uar/runtime/prompt/manifest.rs`: `TurnManifest` with serde, redaction enforced by construction (no body field exists) (PENDING)
- [ ] 3.1 — Replace the `push_str` sequence with fragment construction: artifact system → `System`, effective policy summary → `Policy`, `instructio (PENDING)
- [ ] 3.2 — Sort skill fragments by skill id before rendering; `SkillRegistry::list` returns a sorted vector (PENDING)
- [ ] 3.3 — Emit the manifest artifact and store it in `Run.context` (PENDING)
- [ ] 3.4 — Feed `prefers_xml_envelope` and `markdown_averse` into `render` (PENDING)
- [ ] 4.1 — Compiler writes `instructions` from the agent spec or omits the field; delete the hardcoded empty vector (PENDING)
- [ ] 4.2 — Delete `src/uar/prompt_cache.rs` if no caller exists after this change (confirm with a call-site grep and record the result in tasks notes) (PENDING)
- [ ] 5.1 — Tier 1: `cargo test --locked --no-default-features --features server-full --test prompt_assembly` (PENDING)
- [ ] 5.2 — Tier 2: fmt check and full test run (PENDING)
- [ ] 5.3 — `openspec validate deterministic-prompt-assembly --strict` (PENDING)

## model-path-resiliency — model-path-resiliency

- [ ] 0.1 — Read the vendored liter-llm 1.18.2 error type and record in this file and the phase decision log whether HTTP status and `Retry-After` are e (PENDING)
- [ ] 0.2 — If 0.1 confirms a base-URL override, add `wiremock` 0.6.5 as a dev-dependency and write the `Retry-After` and status tests against a wiremoc (PENDING)
- [ ] 1.1 — `tests/model_path_resiliency.rs`: with `retry_jitter_mode: full` and a seeded jitter RNG (`with_jitter_seed`), ten retries produce the exact (PENDING)
- [ ] 1.2 — A 429 with `Retry-After: 7` waits 7 s (mock clock) when `retry_respect_retry_after` is on and the computed backoff otherwise (PENDING)
- [ ] 1.3 — A driver error typed `kind: InvalidRequest` is not retried; `kind: Overloaded` is; no error-string matching exists (PENDING)
- [ ] 1.4 — With `fallback_models: [a, b]` and `a` in cooldown, the run selects `b` without attempting `a` (PENDING)
- [ ] 1.5 — A stream that emits nothing for `stream_idle_timeout_ms` fails as retryable and the retry succeeds (PENDING)
- [ ] 1.6 — A stream that fails mid-turn persists the partial assistant text with a `TurnInterrupted` marker; the next turn's history contains the marke (PENDING)
- [ ] 1.7 — A chat SSE reconnect with `Last-Event-ID` replays only events after the cursor (PENDING)
- [ ] 2.1 — Add `backon` as a direct dependency at 1.6.0; build `ExponentialBuilder` from `ResiliencePolicy` (jitter, min/max delay, total delay = `retr (PENDING)
- [ ] 2.2 — Add `ProviderError { status, kind, retry_after }` and classify in each driver; delete `is_retryable_error_string` (PENDING)
- [ ] 2.3 — Use `Retry::adjust` to apply `retry_after` when the policy allows (PENDING)
- [ ] 3.1 — Route run model selection through `ModelRouter` with health gating; keep policy-resolved model as the preferred candidate (PENDING)
- [ ] 3.2 — Failover iterates every `fallback_models` entry, each health-gated (PENDING)
- [ ] 4.1 — Add `stream_idle_timeout_ms` to `ResiliencePolicy`, settings schema, and validation; apply to the established stream (PENDING)
- [ ] 4.2 — On mid-stream error, persist partial content with a `TurnInterrupted` marker fragment; do not persist as complete (PENDING)
- [ ] 4.3 — Chat SSE reads `Last-Event-ID` and passes the cursor to `history_since` (PENDING)
- [ ] 5.1 — Tier 1: `cargo test --locked --no-default-features --features server-full --test model_path_resiliency` (PENDING)
- [ ] 5.2 — Tier 2: fmt check and full test run (PENDING)
- [ ] 5.3 — `openspec validate model-path-resiliency --strict` (PENDING)
- [ ] 5.4 — Deferred live smoke: one real provider 429 observed honoring `Retry-After` (record command and output when run) (PENDING)

## progressive-skill-runtime — progressive-skill-runtime

- [ ] 1.1 — `tests/skill_activation_runtime.rs`: 2,000 synthetic skills render a catalog within `min(2% window, 10,000 tokens)` with every skill id pres (PENDING)
- [ ] 1.2 — A run with `skill_attachments: ["s1"]` loads s1's body before the first model call; `skill_attachments: ["disabled"]` returns a typed activa (PENDING)
- [ ] 1.3 — The model calls `activate_skill("s2")`; the next step's prompt contains s2's body and s2's MCP tools are in the tool set; `activate_skill("m (PENDING)
- [ ] 1.4 — With `max_active: 2`, a third activation is refused with a typed result naming the limit (PENDING)
- [ ] 1.5 — A keyword match scoring below threshold activates nothing in either mode; the "include anyway" log line no longer exists; an above-threshold (PENDING)
- [ ] 1.6 — After compaction, the most recent activated body is re-attached within the re-attachment budget (PENDING)
- [ ] 1.7 — An overlay-only skill activation records an outcome; with s1 and s2 both active during a 1,000-token request, `uar_skill_request_tokens_tota (PENDING)
- [ ] 2.1 — Add `src/uar/runtime/skills/catalog.rs`: eligible set → `CatalogEntry` lines, budget resolution, round-robin truncation, omission note (PENDING)
- [ ] 2.2 — Add `src/uar/runtime/skills/activation.rs`: `activate(skill_id, ctx) -> Result<ActivatedSkill, ActivationFailure>` validating eligibility, e (PENDING)
- [ ] 2.3 — Add `activate_skill` native tool with `Exposure::ModelOnly`; wire into descriptor assembly (PENDING)
- [ ] 2.4 — Add `skill_attachments` to the run request; intersect with the effective eligible set; load before the first model call (PENDING)
- [ ] 3.1 — Replace `Vec<Skill>` results with `SkillMatchResult { candidates: Vec<SkillCandidate>, accepted: Vec<SkillId> }`; enforce threshold and marg (PENDING)
- [ ] 3.2 — Read `_threshold` in `keyword_match`; enforce `max_active` and apply `prefer` as tie-break (PENDING)
- [ ] 3.3 — Shadow reducers emit `uar_skill_shadow_recall` against explicit activations and never alter the catalog (PENDING)
- [ ] 4.1 — Activated bodies are `Skill` fragments with `Retention::Reclaimable`; compaction reclaims them first and re-attaches the latest under the bu (PENDING)
- [ ] 4.2 — Record activation outcome for every activated skill including overlay-only; add the `skill` label to token and cost metrics (PENDING)
- [ ] 5.1 — Tier 1: `cargo test --locked --no-default-features --features server-full --test skill_activation_runtime --test skill_scoped_governance` (PENDING)
- [ ] 5.2 — Tier 2: fmt check and full test run (PENDING)
- [ ] 5.3 — `openspec validate progressive-skill-runtime --strict` (PENDING)

## typed-turn-assembly — typed-turn-assembly

- [ ] 1.1 — `tests/typed_turn_assembly.rs`: a `RunExecutionRequest` built from the legacy positional arguments resolves to a `ResolvedTurn` whose fragme (PENDING)
- [ ] 1.2 — A contributor that attempts to add a tool outside the effective policy is rejected at assembly with a typed error (PENDING)
- [ ] 1.3 — After `activate_skill` on step 1, the `ResolvedStep` for step 2 contains the skill's tools; the frozen legacy list did not (PENDING)
- [ ] 1.4 — A direct `start_run` call with memory hits produces a memory fragment (today it produces none) (PENDING)
- [ ] 1.5 — `tests/turn_shadow_parity.rs`: in `shadow` mode, every request in the corpus reports zero differences outside the intentional-delta allowlis (PENDING)
- [ ] 2.1 — Add `turn/request.rs` (`RunExecutionRequest`), `turn/plan.rs` (`TurnAssemblyPlan`), `turn/resolved.rs` (`ResolvedTurn`, `ResolvedStep`) (PENDING)
- [ ] 2.2 — Add `turn/contributors.rs` with the seven stage traits and `ContributorRegistry`; enforce narrow-only on policy and Cedar consultation out o (PENDING)
- [ ] 2.3 — Add `HarnessConfig { mode }` to config and settings schema (PENDING)
- [ ] 3.1 — Move artifact instructions, effective policy, memory, RAG, skills, MCP/tools, and context into contributors using the functions extracted by (PENDING)
- [ ] 3.2 — Orchestrator takes a `ResolvedStep` per model call; tool set and active skills re-projected each step (PENDING)
- [ ] 3.3 — Adapters: `start_run*` and route decoding build `RunExecutionRequest` (PENDING)
- [ ] 4.1 — Add `turn/shadow.rs`: render both paths, diff, classify each difference as allowlisted or unexpected, record in manifest, dispatch legacy (PENDING)
- [ ] 4.2 — Add the parity corpus and `intentional_deltas.json`; run at Tier 2 and produce `parity-report.json` (unexpected-difference count per request (PENDING)
- [ ] 4.3 — Default remains `legacy`; the flip is `typed-turn-default-flip`, which consumes the parity report and live-smoke evidence (PENDING)
- [ ] 5.1 — Tier 1: the two new test targets (PENDING)
- [ ] 5.2 — Tier 2: fmt check and full test run (PENDING)
- [ ] 5.3 — `openspec validate typed-turn-assembly --strict` (PENDING)

## projected-mcp-runtime — projected-mcp-runtime

- [ ] 0.1 — Evaluate porting Codex `sandboxing` (Seatbelt, Landlock, bwrap) for stdio children; record the decision and, if not ported, implement reject (PENDING)
- [ ] 1.1 — `tests/mcp_projection.rs`: two consecutive runs using the same skill-declared server reuse one connection; a config-hash change forces a new (PENDING)
- [ ] 1.2 — A skill-contributed server with a complete cached catalog is not started until its first tool call; the call waits for readiness and succeed (PENDING)
- [ ] 1.3 — A required server that fails to start aborts preflight with an error naming the server; an optional one warns and its tools are absent (PENDING)
- [ ] 1.4 — With 200 deferred tools, the eager set is bounded; `search_tools("calendar")` activates matching descriptors for the next step (PENDING)
- [ ] 1.5 — A child scope cannot replace a global server definition or loosen its sandbox policy (PENDING)
- [ ] 1.6 — Concurrent refreshes of one server produce one connection attempt; a cancelled refresh leaves the entry dirty and the next call refreshes (PENDING)
- [ ] 1.7 — State transitions emit normalized events in order and `set_mcp_server_status` receives each (PENDING)
- [ ] 1.8 — `sandboxed: true` either launches under the sandbox or fails config load; it is never a no-op (PENDING)
- [ ] 2.1 — Add `src/mcp/catalog.rs` (`McpCatalog`, `ServerDefinition` with authority and config hash) (PENDING)
- [ ] 2.2 — Add `src/mcp/projection.rs`: resolve a step to exact servers and tools; authority ordering enforced (PENDING)
- [ ] 2.3 — Add `src/mcp/binding_cache.rs`: key by owner, hash, auth identity, environment; generation invalidation; single-flight (PENDING)
- [ ] 3.1 — Lazy start for skill-contributed and child-only servers gated on cached catalog completeness; `wait_until_ready` on the call path (PENDING)
- [ ] 3.2 — Required versus optional failure handling in preflight (PENDING)
- [ ] 3.3 — State machine and normalized state events; wire the existing MCP-status recorder (PENDING)
- [ ] 3.4 — `search_tools` model-only tool and `Deferred`/`Hidden` exposure in the tool list (PENDING)
- [ ] 4.1 — Replace per-run `from_config` with catalog resolution and binding lookup (PENDING)
- [ ] 4.2 — Preserve reconnect generation guard, timeouts, and shutdown guarantees; extend existing tests (PENDING)
- [ ] 5.1 — Tier 1: `cargo test --locked --no-default-features --features server-full --test mcp_projection` (PENDING)
- [ ] 5.2 — Tier 2: fmt check and full test run (PENDING)
- [ ] 5.3 — Local integration with a real stdio server: lazy start, reconnect, cancel, shutdown (record commands and output) (PENDING)
- [ ] 5.4 — `openspec validate projected-mcp-runtime --strict` (PENDING)

## thread-native-subagents — thread-native-subagents

- [ ] 1.1 — `tests/agent_threads.rs`: a spawned child persists an `AgentThread` and `AgentEdge` in memory, SurrealDB, and PostgreSQL providers with iden (PENDING)
- [ ] 1.2 — A child spawned with `HistoryForkMode::None` sees only its delegated prompt; `LastTurns(2)` sees two turns and no tool outputs (PENDING)
- [ ] 1.3 — `tests/agent_policy_intersection.rs`: a child artifact granting a tool the parent policy denies yields a child policy without it; an unsuppo (PENDING)
- [ ] 1.4 — A child cannot cause an approval to be granted; approval requests bubble to the root run's approval channel (PENDING)
- [ ] 1.5 — The fifth concurrent child, the fourth nesting level, and the seventeenth total child are refused with typed limit errors (PENDING)
- [ ] 1.6 — Root cancellation cancels every child; a remote A2A child receives `tasks/cancel` (PENDING)
- [ ] 1.7 — Child model calls are recorded against the root run's budget; an exceeded budget refuses new spawns and new model calls (PENDING)
- [ ] 1.8 — `send_agent_message` delivers a typed message whose sender identity is metadata; the child's user turn contains no prepended identity text (PENDING)
- [ ] 1.9 — `tests/a2a_thread_service.rs`: inbound `message/send` starts a run on the named agent artifact; `tasks/get` reflects thread status; `tasks/c (PENDING)
- [ ] 1.10 — Lifecycle events carry parent id, child id, canonical path, status, and terminal outcome and no prompt text; AG-UI subagent events are emitt (PENDING)
- [ ] 1.11 — Actor endpoints without user context return 401 (PENDING)
- [ ] 2.1 — Add `thread/mod.rs` (`AgentThread`, `AgentEdge`, `AgentHandle`), `thread/spawn.rs` (`AgentSpawnRequest`, `HistoryForkMode`, fork filtering t (PENDING)
- [ ] 2.2 — Add `thread/policy_intersection.rs`: intersect parent and child artifact policies for skills, MCP servers, tools, credentials, sandbox, budg (PENDING)
- [ ] 2.3 — Persistence trait methods and three provider implementations with stable ordering (PENDING)
- [ ] 3.1 — Descriptors for `spawn_agent`, `send_agent_message`, `wait_agents`, `list_agents`, `interrupt_agent`; spawn description states that spawning (PENDING)
- [ ] 3.2 — Lifecycle event variants; AG-UI subagent mapping; live `RuntimeStep` for graph runs (PENDING)
- [ ] 4.1 — `AgentActor` as a mailbox over the kernel; actor API requires user context (PENDING)
- [ ] 4.2 — `AgentNode` spawns and waits on a real child thread; remove the synthesized two-message call and the `[route]` prefix (PENDING)
- [ ] 4.3 — A2A handler maps onto the thread service; client propagates cancellation (PENDING)
- [ ] 5.1 — Record child usage against the root run; enforce `Exceeded` on spawn and model call (PENDING)
- [ ] 5.2 — Child cancellation tokens from the run token; A2A cancel on parent cancel (PENDING)
- [ ] 6.1 — Tier 1: the three new test targets (PENDING)
- [ ] 6.2 — Tier 2: fmt check and full test run (PENDING)
- [ ] 6.3 — Local multi-agent cancellation smoke with a real model (record command and output) (PENDING)
- [ ] 6.4 — `openspec validate thread-native-subagents --strict` (PENDING)

## project-instructions-world-state — project-instructions-world-state

- [ ] 1.1 — `tests/project_instructions.rs`: a tree with `AGENTS.md` at root and in `crates/a/` yields root-then-subdir concatenation with the separator (PENDING)
- [ ] 1.2 — `AGENTS.override.md` beside `AGENTS.md` wins; an untrusted workspace yields no instructions (PENDING)
- [ ] 1.3 — A subtree `AGENTS.md` is loaded on the first file read inside that subtree, not at turn start (PENDING)
- [ ] 1.4 — `tests/world_state_diff.rs`: with a substituted clock, the first turn renders every section in full; a cwd change on turn 2 renders only the (PENDING)
- [ ] 1.5 — After a history rewrite, the next turn renders every section in full (PENDING)
- [ ] 1.6 — A project instruction that imitates a policy line renders inside `Host` markers and the `Policy` fragment hash is unchanged (PENDING)
- [ ] 2.1 — Add `project_instructions.rs`: root-marker walk, file-name list, override preference, trust check, subtree-on-read hook (PENDING)
- [ ] 2.2 — Config: `project_instructions.file_names`, `root_markers`, `trusted_workspaces`; defaults documented (PENDING)
- [ ] 3.1 — Add `world_state/sections.rs` with stable ids for environment, time, permissions, project instructions and their replacement and removal tex (PENDING)
- [ ] 3.2 — Add `world_state/merge_patch.rs`: RFC 7386 generator and applier; nulls mean deletion; arrays replaced whole (PENDING)
- [ ] 3.3 — Contributor: full render when no baseline or after rewrite; diff otherwise; baseline invalidated on compaction (PENDING)
- [ ] 4.1 — Tier 1: the two new test targets (PENDING)
- [ ] 4.2 — Tier 2: fmt check and full test run (PENDING)
- [ ] 4.3 — `openspec validate project-instructions-world-state --strict` (PENDING)

## typed-turn-default-flip — typed-turn-default-flip

- [ ] 0.1 — Attach `parity-report.json` from typed-turn-assembly showing zero unexpected differences across the corpus; record corpus size (PENDING)
- [ ] 0.2 — Run the live smoke set in `shadow` mode; record command, output, and zero unexpected differences (PENDING)
- [ ] 1.1 — Settings-default test asserts `HarnessConfig::default().mode == Typed` and that `legacy` still deserializes (PENDING)
- [ ] 2.1 — Change the default to `typed`; add the deprecation note for `legacy` to the settings schema (PENDING)
- [ ] 2.2 — Release notes entry naming the rollback setting (PENDING)
- [ ] 3.1 — Tier 2: fmt check and full `cargo test --locked --no-default-features --features server-full` with the new default (PENDING)
- [ ] 3.2 — Decision log entry with the evidence from 0.1 and 0.2 (PENDING)
- [ ] 3.3 — `openspec validate typed-turn-default-flip --strict` (PENDING)
