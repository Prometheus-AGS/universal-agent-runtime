# Goals

- Wave 0 — Decisions & authority: adopt docs/knowme-ui-ux-standard.md as the single binding design authority (R1); archive docs/UI_DESIGN.md and docs/admin-aesthetic-spec.md with superseded-by pointers; write docs/ui-design-authority.md; resolve product identity via a config-driven brand slot (R2); record R1/R2/R3 as ADRs
- Wave 1 — Design-system convergence: purge Flat 2.0 violations (622 border idioms, 50 variant="outline", 9 shadow files, 6 backdrop-blur/gradient); retire the [data-admin-theme="terminal"] CRT theme into KnowMe-compliant Console density tokens; add ESLint no-restricted-syntax style gate; Storybook visual-regression gate; Console IA regroup into 5 nav groups with dev-flag gating
- Wave 2 — State-layer completion: delete the dead TanStack Query provider and dependency (zero call sites); finish the PEM entity migration for the remaining migrate-* pages; publish the store inventory/charter table and delete retired stores; split the 3,336-line settings-page.tsx monolith by domain (no page > ~600 lines)
- Wave 3 — Chat elevation to KnowMe §7: run-scoped runtime store keyed by runId for background-run continuity across thread switches; library upgrade (PGlite FTS over titles+bodies, recency grouping, pin/archive/export, running/failed/unread badges, undo-delete); per-thread drafts; regenerate-as-variant and branch-from-message; ContentBlock exhaustiveness with compile-time enforcement; lane labeling; first-run recovery replacing the blocking No-Model gate; dedicated error surfaces
- Wave 4 — Console elevation & Home: hierarchical Runs trace tree with replay from the append-only AG-UI event record; Approvals queue UX; new Home instrument-panel destination projecting existing PEM entities; A2UI KnowMe retheming and retirement of A2UI Testing from production nav
- Wave 5 — Certification: execute screen-by-screen-validation across the 20-screen inventory with video-proof evidence bundles; close the React boundary gate to an empty production allowlist; accessibility certification (keyboard-only flows, landmarks, live regions, reduced motion, axe) and responsive sweep at 320/768/1024/1440 in both themes
- Process constraints: every change carries an OpenSpec spec delta; wave gates are hard (a wave does not start until the prior wave's gates are green); 3.1 blocks 3.2-3.5; 2.2 blocks 2.3; 1.2 blocks 1.5; concurrent Claude/Codex work runs in separate worktrees under ~/.claude/worktrees/
- Open questions requiring operator sign-off: terminal-theme retirement is a taste decision the operator commissioned (R1); brand-slot default for standalone open-source UAR identity (R2); CH-8 local-first default model depends on the Tauri/WebLLM host story; PGlite FTS schema bump must stay logically equivalent with pglite-oxide on desktop
