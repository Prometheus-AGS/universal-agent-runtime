apiVersion: v1
kind: ServiceAccount
metadata:
  name: uar-app
  namespace: uar
  labels:
    app.kubernetes.io/name: uar
    app.kubernetes.io/part-of: universal-agent-runtime
  annotations:
    # Add GCP Workload Identity annotation when ready:
    # iam.gke.io/gcp-service-account: uar-app@PROJECT_ID.iam.gserviceaccount.com
