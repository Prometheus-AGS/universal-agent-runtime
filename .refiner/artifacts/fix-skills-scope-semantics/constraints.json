{
  "artifact_type": "content",
  "constraints": [
    {
      "id": "session-policy-live-gating",
      "description": "Persisted conversation skill selection constrains the next run before activation events and prompt overlay injection.",
      "severity": "blocking",
      "type": "semantic",
      "validation": {
        "requires_code_execution": true,
        "validation_method": "Focused run-manager integration test with selected and excluded runs plus a branch inversion"
      }
    },
    {
      "id": "builtin-origin-immutability",
      "description": "Built-in and installed-pack skills reject edit and delete at the API boundary while retaining global disable and serialized origin.",
      "severity": "blocking",
      "type": "semantic",
      "validation": {
        "requires_code_execution": true,
        "validation_method": "Focused service and HTTP tests plus origin-guard inversion and UI source inspection"
      }
    },
    {
      "id": "durable-scope-foundation",
      "description": "Durable global, agent, and conversation state, precedence, cold restart, and user deletion behavior remain satisfied by the archived prerequisite implementation.",
      "severity": "blocking",
      "type": "technical",
      "validation": {
        "requires_code_execution": true,
        "validation_method": "Archived process-reopen and live-effect receipts plus current focused regression checks"
      }
    },
    {
      "id": "scope-and-evidence",
      "description": "The change is limited to the observed H2/H3/O1 reconciliation, focused tests, OpenSpec, and artifact evidence; M4 and phase Tier 2 remain deferred.",
      "severity": "blocking",
      "type": "structural",
      "validation": {
        "requires_code_execution": true,
        "validation_method": "Scoped diff audit, Tier 0, focused Tier 1, strict OpenSpec, schema, formatting, and diff checks"
      }
    }
  ],
  "global_rules": [
    "Use the OpenSpec delta and task ledger as acceptance authority.",
    "Preserve the existing durable scope and cold-restart implementation.",
    "Do not expand this reconciliation into the unrelated LLM matching algorithm.",
    "Report only observed server-full results and do not run phase Tier 2 early."
  ],
  "termination_conditions": [
    "All four blocking constraints pass.",
    "Strict OpenSpec, schema validation, focused tests, formatting, and scoped diff checks exit successfully.",
    "Independent artifact critic and judge find no implementation or evidence blocker."
  ]
}
