# Validation plan — `uar-security-operations-documentation-review`

1. Validate the exact eleven-guide manifest, dependency order, profile
   vocabulary, source classification, current authorities, routes, required
   concepts, frontmatter, heading hierarchy, diagram explanations, and four
   compatibility contracts.
2. Review authentication, credential, and tenancy statements against current
   verifier, middleware, credential-service, A2A, and canonical spec sources.
3. Review governance and approval statements against build features, server
   composition, Cedar engine/facade, middleware, runtime decision order, events,
   endpoint, and packaged UI sources.
4. Review runtime-console, run, observability, realtime, cost, shutdown, and
   recovery statements against their current process, persistence, browser,
   transport, and external-service state owners.
5. Execute the fourteen isolated security/operations negative controls before
   the complete positive source, then run Docusaurus TypeScript and bounded
   architecture, brand, product-workflow, security/operations, and composed
   publication controls.
6. Run strict OpenSpec, refiner schema validation, referenced-file checks, and
   a scoped diff audit. Persist one bounded Markdown review and terminate if all
   blocking constraints pass.

No HTML derivative or preview is required for this `direct:content` source
review. The complete Docusaurus build and browser/accessibility evidence remain
owned by the final documentation certification change.
