# Chunk Catalog Renderers Refinement Decisions

## 2026-08-08 — Iteration 1

- **Delta:** Historical rich content produced duplicate visible chunks when both its portable replacement and rich compatibility overlay projected, while repeated generic `<section aria-label>` surfaces created duplicate landmark names in the aggregate catalog.
- **Correction:** Track and replace the precise portable projection for historical rich discriminants, with all nine prior variants covered. Render generic surfaces as labeled/live neutral containers while preserving true structural landmarks only where needed.
- **Decision:** Accept deterministic convergence for the artifact-refiner gate. Keep Recharts 3.10.1 behind the closed application-owned chart schema, retain A2UI rendering through the existing policy component, and reopen only if isolated adversarial review finds a critical defect.

## 2026-08-08 — Iteration 2

- **Delta:** Independent review found that live rich chunks were correct while several portable artifact envelopes lagged behind late lifecycle updates, and that run-event persistence failures shared the transport retry exception path. A local boundary audit also found provider-authored URLs reaching DOM attributes without the markup sanitizers.
- **Correction:** Persist complete fixed-MIME A2UI envelopes on every lifecycle transition, keep thinking/reasoning chunk kinds distinct, emit terminal tool results even without payloads, contain PGlite failures in a persistence-only helper, and allowlist URL schemes/data MIME values before rendering.
- **Decision:** Converge after the post-remediation aggregate gate. Preserve the round-two `BLOCK` receipt and same-model-collision field rather than rewriting history; carry its evidence-backed resolved dispositions into OpenSpec verification because the configured review loop permits no third round.
