# Refinement Log: markdown-lazy-blocks

## 2026-08-07 — Iteration 1

- Specify: classified the change as `direct:code`; derived four blocking constraints from the strict C-09 OpenSpec delta because the project constraint file is absent.
- Plan: selected focused behavior tests, trust-boundary inspection, consolidated Wave 3 gates, and recursive production-manifest traversal as deterministic validation.
- Execute: replaced invalid package-wide engine groups with named dynamic facade entries after the first production graph showed both groups in entry preloads.
- Reflect: all four blocking constraints pass. The correction reduces the entry's recursive static closure to runtime/assistant/React/query while preserving named engine entries as dynamic imports.
- Persist: stored this receipt, structured constraints, manifest, decision, and filesystem-backed named state.
- Result: deterministic convergence for artifact refinement; proceed to isolated adversarial review. A critical review finding will reopen refinement.

## 2026-08-07 — Iteration 2

- Delta: isolated review correctly found that the manifest graph proof was only an ad hoc command and that no test held/rejected a lazy module promise. Its dependency findings reflected intentionally omitted cumulative lock/package hunks rather than absent repository changes.
- Execute: added `scripts/check-markdown-lazy-chunks.mjs`, a controllable pending/rejected lazy-module boundary test, focused dual-lock evidence, and the observed Vitest root-hoist allowance needed after frozen installation.
- Reflect: the production checker reports zero forbidden static entries, zero missing dynamic entries, and zero invalid names. The new test preserves escaped source while pending and after rejection while retaining sibling prose.
- Verification: frontend typecheck/lint, boundaries, Flat 2.0, 45 files with 214 tests, both frozen installs, strict OpenSpec, and diff integrity pass.
- Result: converged again; send the corrected packet to final isolated review.
