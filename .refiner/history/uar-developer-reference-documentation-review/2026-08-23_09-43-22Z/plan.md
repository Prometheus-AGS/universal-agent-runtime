# Validation plan — `uar-developer-reference-documentation-review`

1. Validate the exact fifteen-guide manifest, fixed order, stable routes,
   profile vocabulary, source records, current authorities, required markers,
   compatibility pages, and local links.
2. Review API, HTTP, event, MCP, A2A, and tool claims against current router,
   adapter, registry, security, and feature-gate sources.
3. Review SDK source, generated-reference staging, and registry-availability
   claims independently.
4. Review configuration, installation, deployment, upgrade, health, persistence,
   and rollback boundaries against current source and packaged artifacts.
5. Execute the isolated negative controls, Docusaurus TypeScript check, bounded
   documentation controls, strict OpenSpec, schema checks, and scoped diff audit.
6. Persist one bounded Markdown review and terminate when all five blocking
   constraints have observed evidence.

No HTML derivative or preview is required for this `direct:content` review.
The complete Docusaurus build and browser/accessibility evidence remain owned
by the final documentation certification change.
