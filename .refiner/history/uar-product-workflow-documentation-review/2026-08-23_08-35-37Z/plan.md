# Validation plan — `uar-product-workflow-documentation-review`

1. Validate the exact seven-guide manifest, dependency order, profile
   vocabulary, source classification, current authorities, routes, required
   concepts, frontmatter, heading hierarchy, diagram explanations, and skills
   compatibility contract.
2. Review provider/model/inference statements against the provider registry,
   model catalog routes, chat handler, current UI projections, canonical specs,
   and retained 1.0 evidence limits.
3. Review agent and skill workflows against durable agent storage, effective
   run-policy resolution, embedded boundaries, skill provenance, scoped
   precedence, restart durability, reconciliation, tombstone, and restore
   behavior.
4. Review knowledge and memory as separate durable authorities, including
   ingestion/processing/retrieval/citation and opt-in
   capture/recall/injection/mutation boundaries.
5. Execute the nine isolated product-workflow negative controls before the
   complete positive source, then run Docusaurus TypeScript and bounded
   architecture, brand, product-workflow, and composed publication controls.
6. Run strict OpenSpec, schema validation, referenced-file checks, and a scoped
   diff audit. Persist one bounded Markdown review and terminate if all blocking
   constraints pass.

No HTML derivative or preview is required for this `direct:content` source
review. The complete Docusaurus build and browser/accessibility evidence remain
owned by the final documentation certification change.
